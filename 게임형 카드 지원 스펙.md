# 게임형 카드 지원 — 롤백 경계 + 설계

> **동결(v0.0.27).** U1·U3·U4(챗)·U5 까지 구현하고 멈췄다. 남은 U2(답변에서 값 읽기)와
> U4 소설 모드는 착수하지 않는다 — 재개는 사용자 지시가 있을 때만.

> 상태창·선택지·애정도가 있는 "게임처럼 도는" 봇카드를 **일반적으로** 지원하기 위한 작업.
> 특정 카드에 맞추지 않는다 — 카드가 자기 규칙을 들고 오고, 우리는 그 규칙을 실행하는 기계만 만든다.
>
> **이 문서의 1부(롤백 경계)는 구현 중 계속 지키는 계약이다.** 착수 전과 각 단계 완료 시 확인한다.

---

# 1부 — 롤백 경계 (계약)

## 왜 경계가 필요한가

이 기능은 복잡해서 "해보니 아니다" 판정이 날 수 있다. 그때 **깨끗이 걷어낼 수 있어야** 한다.
코드는 git 으로 되돌아간다. 진짜 위험은 두 가지다.

1. **사용자 데이터에 남는 흔적** — 세션 파일에 새 필드를 심으면 기능을 걷어내도 그대로 남는다.
2. **기존 코드에 흩어진 훅** — 생성/컨텍스트/표시 경로 곳곳에 조건이 박히면 되돌릴 때 누락이 생긴다.

그래서 아래 세 등급으로 나누고, **C급은 애초에 만들지 않는다**.

## 등급

| 등급 | 뜻 | 롤백 방법 |
|---|---|---|
| **A** | 새 파일 / 새 데이터 파일 / **기존 파일에 대한 순수 추가** | 삭제 = 흔적 0 |
| **B** | 기존 코드의 흐름을 바꾸는 자리 | 접점 목록대로 되돌림. **예산 초과 시 멈춤** |
| **C** | 기존 스키마·저장 형식·기존 동작 변경 | **금지** — 필요해지면 멈추고 보고 |

**A/B 를 가르는 기준(U1 구현 중 확정)**: "지우면 도입 전과 완전히 같아지는가".
새 메서드·새 import·새 목록 항목·새 매크로 분기처럼 **기존 실행 경로가 통과하지 않는 추가**는
A급이다(지우면 끝). 기존 코드가 실제로 다른 값을 보게 되는 자리만 B급으로 센다.

## C급 금지 목록 (하나라도 필요해지면 멈춘다)

- `session.json` 스키마 변경 (새 필드 추가 포함)
- 본문 저장 방식(patch/offset) 변경, 저장 원문에 손대기
- 기존 표시·번역·삽화·문단 재생성의 **동작** 변경
  (호출부가 새 판단 함수를 *부르는* 것은 B급, 판단 결과로 기존 동작이 달라지는 것은 C급)
- 시나리오 카드·로어북 파일 형식 변경 (ST 라운드트립 파손)

## 데이터 경계 — 세션 파일을 안 건드리는 방법

세션 파일 형식을 바꾸지 않고도 전부 된다:

- **현재 값**은 이미 있는 `session.meta.variables`(이름→문자열)를 **그대로** 쓴다.
  단계 있는 이름은 키에 점을 찍어 표현한다 — `"children.ra.affection": "30"`.
  → 스키마 변경 0. 기존 QR `/setvar` 와 같은 저장소라 **게임형 카드가 쓰는 `/setvar` 가 그대로 맞물린다.**
  → 롤백해도 값은 남지만, 그건 원래 있던 기능(QR 변수)의 데이터라 무해하다.
- **가지별 되짚기 기록**은 세션 폴더의 **새 파일**(A급)에 둔다. 노드 id 를 키로 쓰는 방식은
  삽화(`illustrations.json`)와 같은 선례다. 롤백 = 파일 삭제.
- **전역 값**은 `PluginData` 새 필드 하나(B급, 1곳). 롤백하면 안 읽히는 필드 하나가 남는다 — 무해.
- **카드가 들고 온 규칙**(표시 변환 등)은 이미 있는 정규식 스크립트 그릇에 담는다. 새 저장소를 안 만든다.

## 확장 경계 — "끄기"가 곧 예비 롤백

전부 `plugin.extensions` 확장으로 등록한다(번역/삽화/요약/정규식과 같은 방식).
각 유닛은 **독립적으로 꺼진다**. 꺼진 상태 = 기능 도입 전과 동일하게 동작해야 한다.

즉 롤백은 2단계다: **① 확장 끄기(즉시, 되돌리기 쉬움) → ② 코드 삭제(확정)**.
①에서 기존 동작이 온전히 돌아오지 않으면 그건 경계를 이미 넘은 것이다 — 멈추고 고친다.

## 롤백 유닛 (각각 독립 삭제 가능)

| 유닛 | 내용 | 의존 | B급 접점 예산 |
|---|---|---|---|
| **U1 값** | 점 이름 해석, 전역 값, 가지별 되짚기, 변수 패널 | 없음 | **4곳** → 실제 **4곳** |
| **U2 답변에서 값 읽기** | 카드 규칙대로 AI 답변에서 값 뽑아 반영 | U1 | **1곳** |
| **U3 조건부 로어북** | 값에 따라 로어북 내용이 달라짐 | 없음(값 없으면 조건 미충족 처리) | **2곳** → 실제 **2곳** |
| **U4 표시** | 양식 덩어리 → 상태창/선택지 렌더, 안전한 HTML, 이미지 태그 | 없음 | **5곳** → 실제 **1곳**(챗) |
| **U5 QR 보강** | 부족한 커맨드 추가 | 없음 | **1곳**(추가만) |

**합계 예산 13곳.** 유닛별 예산을 넘겨야 할 것 같으면 **그 자리에서 멈추고 보고**한다.
유닛 사이에 의존이 거의 없으므로 **역순 삭제 제약이 없다** — U4만 남기고 U1~U3를 걷어내는 것도 가능하다.

## 멈춤 신호 (하나라도 걸리면 진행 중단하고 보고)

1. C급을 건드려야 풀리는 문제가 나옴
2. 유닛의 B급 접점 예산 초과
3. 확장을 껐는데 기존 동작이 그대로 돌아오지 않음
4. 소설 모드 본문 편집이 흔들림 — 한글 조합 끊김, 커서/포커스 소실, 스크롤 튐
5. 기존 번역·삽화·문단 재생성·내보내기에 회귀 발생

## 단계별 확인 (매 유닛 완료 시)

- [ ] 빌드 통과 (`npm run build`)
- [ ] 하네스 통과 (`npm run test:harness`)
- [ ] **확장 전부 끈 상태**로 기존 세션(소설/챗) 정상 — 이게 롤백 가능성의 증거다
- [ ] B급 접점 실제 개수를 이 문서 표에 기록 (예산 대비)
- [ ] 커밋을 유닛 단위로 분리 — 유닛 하나만 `git revert` 할 수 있는 상태 유지

---

# 2부 — 설계

## 무엇을 지원하는가

커뮤니티 봇카드들이 게임처럼 도는 방식은 만든 사람이 달라도 요구하는 기계가 같다.

1. **기억되는 값** — 애정도, 스트레스, 소지금, 랭크, "주사위 시스템 켬/끔"
2. **값에 따라 AI 에게 가는 내용이 달라짐** — 성장했으면 성인 설정, 주사위 끄면 주사위 규칙 제외
3. **AI 가 약속된 양식으로 뱉고, 화면에선 그게 그려짐** — 상태창, 선택지, SNS, 삽화

값이 바뀌는 길은 셋: **AI 답변에서 읽기 / 버튼(QR) / 사용자 토글.** 카드마다 이 중 하나 이상을 쓴다.

## 이름을 박지 않는다 (일반화 원칙)

카드마다 태그 이름이 다르다(`<stat>`, `<status>`, `<UpdateVariable>`, `<choices>`, …).
**우리 코드에 특정 카드의 태그 이름을 넣지 않는다.** 카드가 자기 규칙을 들고 오고
(임포트 시 함께 들어오는 정규식 스크립트), 우리는 그 규칙을 실행하는 기계만 제공한다.

## U1 — 값 ✅ 구현 완료

**확장 id `stella:variables`.** 끄면 패널이 사라지고 전송본·QR 이 예전 동작으로 돌아간다.

### 실제 접점 기록 (롤백 시 찾아갈 자리)

**B급 4곳** — 기존 코드가 다른 값을 보게 되는 자리:

| 파일 | 자리 | 되돌리는 법 |
|---|---|---|
| `util/build-session-context.ts` | 변수 출처 (`isExtensionEnabled` 분기) | `{ ...(session.meta.variables ?? {}) }` 한 줄로 |
| `util/build-session-context.ts` | `updatedVariables: stripGlobalScope(variables)` | `variables` 로 |
| `services/qr-runner.ts` | 시작 시 값 읽기 (`initialVars`) | `{ ...(session?.meta.variables ?? {}) }` 로 |
| `services/qr-runner.ts` | 종료 시 값 저장 (`useVariableLog` 분기) | else 가지만 남기기 |

**A급(순수 추가, 지우면 끝)**: 새 파일 `types/variables.ts` · `util/variables.ts` ·
`services/variables-service.ts` · `views/detail/panels/variables-panel.ts` ·
`extensions/variables-extension.ts` · `tests/variables.test.ts` /
`state/store.ts` 의 `getSessionVariableLog`·`saveSessionVariableLog`·경로 헬퍼 /
`main.ts` 의 `variables` 서비스·`globalVariables` 필드·확장 카탈로그 1항목·`applyExtensions` 1블록 /
`util/macros.ts` 의 `{{getglobalvar::}}` 분기 / `styles.css` 의 `.ggai-var-*` 블록.

**데이터**: 세션 폴더 `variables.json` (새 파일). `session.json` 스키마 변경 **없음** —
현재 값은 기존 `meta.variables` 에 그대로 있다. 롤백 = `variables.json` 삭제 +
`PluginData.globalVariables` 방치(무해).

### 알려진 한계 (다음 유닛에서)

- 가지를 옮겨도 `meta.variables` 는 즉시 갱신되지 않는다. 전송본과 QR 은 항상
  되짚은 값을 쓰므로 **동작은 정확**하고, `meta.variables` 는 값이 실제로 바뀔 때
  다시 맞춰진다. 이 자리를 읽는 다른 소비자가 생기면 그때 갱신 훅을 단다.
- `{{setglobalvar::}}` 계열 쓰기 매크로는 없다(매크로는 동기 순수 치환이라 저장을
  못 한다). 전역 쓰기는 변수 패널과 `/setglobalvar`(U5)가 맡는다.

### 설계

- 이름에 점을 허용해 단계를 표현(`children.ra.affection`). 저장 형식은 그대로.
- 범위 둘: **세션**(기존 자리) / **전역**(플러그인 설정). 카드는 둘 다 쓴다.
- **가지 되짚기**: 우리는 재생성·과거 지점 점프가 일상이다. 값이 세션에 하나뿐이면
  되돌려도 애정도가 안 돌아와 게임이 깨진다. 본문과 같은 원리로 **지점마다의 변화**를
  기록하고 현재 위치까지 되짚어 재구성한다. (ST 는 이걸 못 한다 — 우리가 더 낫다.)
- **변수 패널**: 값을 눈으로 보고 켜고 끄고 고친다. 카드들이 설정 팝업을 직접 그리려고
  JS 를 넣는 이유가 이것뿐이라, 패널 하나가 그 스크립트 전체를 대체한다.

## U2 — 답변에서 값 읽기

AI 가 답변 끝에 붙이는 변화 지시(카드마다 형식이 다름)를 카드 규칙대로 읽어 값에 반영하고,
그 덩어리는 화면에서 걷어낸다. 생성 완료 훅이 이미 있으므로 확장 등록만으로 붙는다.

## U3 — 조건부 로어북 ✅ 구현 완료

**확장 id `stella:lorebook-template`.** 끄면 항목 내용이 조건 없이 통째로 들어간다.

**임의 코드 실행은 하지 않는다** — 남이 만든 파일을 임포트하는 구조라 위험하다.
대신 실제 카드가 쓰는 만큼만 해석한다: `if / else if / else`, 비교(`=== !== > < >= <=`),
논리(`&& || !`), 값 읽기(`getvar` / `getGlobalVar`), 지역 변수(`const/let x = …;`),
문자열 거들기(`.trim() .length .toLowerCase() .toUpperCase() .includes()`),
출력(`<%- %>` / `<%= %>`), 주석(`<%# %>`), 공백 제어(`-%>` / `<%_`).

**해석 못 하는 코드는 지우고 본문은 남긴다.** 여는 중괄호 수를 세어 블록 짝을
유지하므로 뒤따르는 `<% } %>` 가 엉뚱한 블록을 닫지 않는다(이게 깨지면 그 뒤 내용이
통째로 사라지는 조용한 손실 — 하네스가 지킨다).

### 반복문은 지원하지 않는다 (당초 계획에서 좁힘)

착수 전에는 "조건 + 반복 + 비교"로 적었는데, 실물 카드의 반복은 `forEach` 콜백 안에서
배열에 밀어 넣고 `join` 하는 **진짜 JS**라 지원하려면 인터프리터를 써야 한다.
실측 카드 2종의 템플릿 항목 13개 중 이 형태는 **1개뿐**이라 그 값을 하지 않는다고 판단했다.
해당 항목은 감싸는 글만 남고 안쪽 지시문은 빠진다(위 degrade 규칙).

### 실측 결과 (마녀의 숲 + D&H, 템플릿 항목 13개)

- 11개 = **완전 해석**(건너뛴 조각 0). 값에 따라 내용이 실제로 갈린다
  (아동기↔성인기 캐릭터 설명, 언어 토글, 주사위/선택지 on-off, LI 지시 유무).
- 2개 = 부분 해석: 마녀의 숲 "성장 변수"(forEach), D&H "선택지"의 주사위 굴림
  (`Math.floor(Math.random()*20)+1` — JS). 조건 부분은 정상 동작.
- **13개 전부 태그 잔존 0** — `<%` 가 프롬프트로 새지 않는다.

### 실제 접점 기록

**B급 2곳** (예산 2곳):

| 파일 | 자리 | 되돌리는 법 |
|---|---|---|
| `util/build-session-context.ts` | `contextLorebooks` (확장 게이트 + 렌더) | 삭제 |
| `util/build-session-context.ts` | `buildContext` 인자 `lorebooks: contextLorebooks` | `lorebooks` 로 |

**A급**: 새 파일 `util/lorebook-template.ts` · `tests/lorebook-template.test.ts` /
`main.ts` 확장 카탈로그 1항목. **데이터 흔적 없음** — 원본 로어북 파일은 건드리지 않고
전송본 사본만 손본다(store 캐시 객체 변형 금지, 하네스가 지킨다).

### 순서 주의

템플릿은 **매크로보다 먼저** 돈다 — 조건이 살린 자리에만 `{{user}}` 등이 풀려야 한다.
조건 때문에 내용이 통째로 비면 그 항목은 전송본에서 빠진다(제목만 남지 않게).

## U4 — 표시 (챗 모드 완료 / 소설 모드 결정 대기)

**확장 id `stella:card-display`.** 끄면 태그가 글자 그대로 보인다(도입 전과 동일).

### 된 것 — 챗 말풍선

- 카드가 낸 HTML 을 **화이트리스트로 걸러** 그린다. 상태창·표·게이지·이미지가 화면이 된다.
- `{{img::파일명.jpg}}` — **원본 확장 `tincansimagine/character-assets` 의 태그다.**
  그쪽은 실리태번 캐릭터 폴더에서 파일명을 그대로 찾는다(`/characters/<charkey>/<파일명>`,
  태그 정규식 `/\{\{img::([^}]+)\}\}/gi`). 우리 대응은 **시나리오 폴더 루트**.
  - **확장자는 느슨하게 본다** — 원본이 WebP 자동 변환 때문에 확장자를 뗀 이름으로도
    매칭한다. `{{img::joy.png}}` 가 저장된 `joy.webp` 를 찾는다.
  - 못 찾으면 태그를 지운다(원본도 깨진 그림을 감춘다). `..` 이 든 이름은 거부.
  - `assets/` 하위도 함께 보는 건 **우리 추가**(CHARX 임포트가 카드 에셋을 풀 자리).
  - **안 하는 것**: 원본의 변주 묶기(`joy-1.png`/`joy-2.png` → `joy` 로 묶어 무작위 선택)는
    원본에서도 설정으로 켜는 기능이라 요청 없이 넣지 않았다. `{{charkey}}`
    `{{img_keywords}}` 같은 원본 매크로, ST 쪽 정규식(`src="/characters/{{charkey}}/$1"`)
    임포트 호환도 아직 없다 — 그 정규식을 그대로 들여오면 주소가 안 맞는다.
- 편집은 **수정 버튼**으로 연다(말풍선 클릭 = 편집 진입은 폐지 — 카드 화면을 누르면
  원문이 튀어나오고 링크·접기·버튼이 죽었다). 수정칸 안은 예전처럼 raw 다.
- 태그처럼 생긴 게 없으면 예전 경로 그대로(`3 < 5` 를 마크업으로 오인하지 않는다).

### 통과 정책 (= 보안 경계)

**실리태번과 같은 범위로 맞춘다.** ST 는 DOMPurify 기본 허용을 쓰므로 링크·버튼·입력
요소·접기·표·SVG 가 다 그려지고 `on*` 이벤트 속성만 사라진다. "이건 되고 저건 안 되는"
부분 지원은 카드가 깨져 보이는 원인이라 하지 않는다.

- **실행되는 것은 전부 막는다**: `script/iframe/object/embed/link/template/canvas`,
  SVG 의 `foreignObject`/`use`/`animate*` 는 내용까지 버리고, `on*` 속성과
  `javascript:`·`vbscript:`·`file:`·`data:text/html` 주소는 통과하지 못한다
  (대소문자·제어문자 회피 포함).
- **그리는 것**: `a`(항상 `target=_blank rel=noopener` — 옵시디언 창이 바깥으로
  이동하면 앱이 날아간다), `button`, `input`/`label`/`select`/`textarea`(체크박스로
  접기를 만드는 카드), `details`/`summary`, 표, `video`/`audio`, SVG 부분집합
  (기하·색 속성만). **`form` 은 원본에 있지만 우리는 버린다** — 서버가 없어 원래
  동작하지 않는 데다 제출이 일어나면 창이 이동한다. `formaction`/`action`/`ping` 도 막는다.
- **모르는 태그는 껍데기만 벗긴다** — `<stat>` `<choices>` 같은 카드 약속 태그를 통째로
  지우면 본문이 사라진다.
- **남의 이름이 우리 화면을 건드리지 못하게 한다** — 카드가 쓴 `class`/`id`/`for` 는
  `custom-` 접두사를 붙인다(ST 와 같은 처리, 우리는 id/for 까지 확장). `href="#앵커"` 도
  같이 맞춰 서로 가리킨다.
- **`<style>` 은 말풍선 안으로 가둬서 받는다** (원본·RisuAI 와 같은 처리). 선택자마다
  `.ggai-chat-bubble` 을 앞에 붙이고 class/id 이름에 접두사를 단다(`scopeCss`).
  `:root`/`html`/`body` 는 그 범위 자신이 되고, `@media`/`@supports` 는 안쪽까지 재귀,
  `@keyframes` 안쪽(`0%`)은 선택자가 아니라 그대로 둔다. `@import` 는 버린다.
- **화면을 덮는 배치는 막는다** — 인라인이든 `<style>` 이든 `position: fixed/sticky` 와
  `z-index` 는 걷어낸다. `expression()`·`behavior:`·`-moz-binding` 도 마찬가지.
- 외부 이미지(`https://`)는 허용한다 — 사용자가 직접 고른 카드이고 옵시디언도 원격
  이미지를 그린다. **CSS `url()` 도 주소가 안전하면 통과한다**(그림 배경이 카드 디자인의
  큰 부분이다 — 예전엔 통째로 걷어냈다). 원본에는 "외부 미디어 차단" 설정이 있지만
  우리는 두지 않았다(항상 허용).
- **버튼을 누르면 그 글자가 그대로 전송된다 — 우리 추가.** 원본은 `on*` 을 지워 카드
  버튼이 장식으로만 남는다(눌러도 아무 일이 없다). 선택지 버튼이 실제로 동작하는 편이
  게임형 카드의 의도에 맞아 살균 단계가 `.ggai-card-btn` 표식을 남기고, 챗 뷰가 클릭을
  받아 `applyQuickReplyText(label, true)` 로 보낸다.

정책 함수는 순수 함수라 하네스가 직접 검사한다(`tests/safe-html.test.ts`).

### 남은 결정 — 소설 모드

소설 본문은 통짜 편집 화면이라 **어디에 그릴지가 UI 결정**이다. 실물을 보고 정한다:
1. 본문 밖 고정 패널에 최신 상태창 하나만 (편집 영역 무손상, 가장 안전)
2. 삽화용 인라인 위젯 기계를 재사용해 문단 사이에 (편집 중 커서·조합 위험을 다시 검증해야 함)
3. 소설 모드는 지원하지 않음 (게임형 카드는 챗 모양이라 실수요가 없을 수 있음)

확인 전까지 소설 모드는 예전 그대로다(태그가 글자로 보인다).

### 실제 접점 기록

**B급 1곳** (예산 5곳):

| 파일 | 자리 | 되돌리는 법 |
|---|---|---|
| `views/chat-session-view.ts` | `setBubbleDisplay` 의 표시 파이프라인 | 카드 분기(`allowHtml`)만 false 고정 — 마크다운 표시는 카드 기능과 무관하게 남는다 |

**A급**: 새 파일 `util/safe-html.ts` · `tests/safe-html.test.ts` /
`chat-session-view.ts` 의 `resolveCardImageSrc` 메서드 · import /
`main.ts` 확장 카탈로그 1항목 / `styles.css` 의 `.ggai-card-*` 블록.
**데이터 흔적 없음** — 표시 계층만 손댄다(저장 원문 불변).

### 설계

- 카드 규칙에 따라 양식 덩어리를 상태창/선택지 등으로 그린다. **저장 원문은 안 건드린다**
  (본문이 글자 위치 기준이라 잘라내면 어긋난다). 이미 있는 정규식 "표시" 시점 슬롯을 채운다.
- 안전하게 걸러진 HTML 렌더 — 태그/속성 화이트리스트, 메시지 안 스크립트 실행 금지.
- 카드가 파일명으로 지정하는 이미지 태그 → 기존 삽화 표시 계층 재사용.

## U5 — QR 보강 (일부 완료 — QR 슬라이스 2d 에서 처리)

실측 카드 두 종에서 빠진 것: 전역 변수 계열, `/rand`, `/getvar`, `/addvar`, `/trigger`,
`/hide`, `/sys`, `/popup`, `/setinput`, `/re-test`, `/run`, `{{lastMessage}}`,
`/if else={: :}` 분기, `/buttons labels=<변수>`. 기존 실행기에 추가만 한다.

**된 것** (`QR 스펙.md` 슬라이스 2d): `/setglobalvar` `/getglobalvar` `/rand` `/addvar`
`/trigger` `/hide` `/unhide` + 암묵적 파이프 + QR 안에서 `{{getglobalvar::x}}` 해석.
**남은 것**: `/getvar` `/sys` `/popup` `/setinput` `/re-test` `/run` `{{lastMessage}}`,
`/if else={: :}`, `/buttons labels=<변수>`.

### 실제 접점 기록 (2d 시점)

**B급 0곳 / 데이터 흔적 0** — `session.json` 은 건드리지 않았다. 노드 단위 "AI에게 숨김"과
익명 발화자 이름표는 세션 폴더의 **새 곁파일** `node-meta.json`(A급)에 담는다.
C급 금지 목록의 "session.json 스키마 변경(새 필드 추가 포함)"에 걸리지 않으려고
노드에 필드를 다는 대신 파일을 하나 더 둔 것 — 롤백 = 그 파일 삭제.

**A급(순수 추가)**: 새 파일 `types/node-meta.ts` · `util/speaker-anchors.ts` ·
`views/speaker-label-widget.ts` / `util/node-segments.ts` 의 `spansExcludingNodes`·`nodeSpanRanges` /
`state/store.ts` 의 `getSessionNodeMeta`·`saveSessionNodeMeta`·`patchSessionNodeMeta`·경로 헬퍼 /
`util/build-session-context.ts` 의 숨김 제외(제외 집합이 비면 예전과 **같은 함수**를 탄다) /
두 세션 뷰의 표시 분기 / `styles.css` 의 `.ggai-span-nosend`·`.ggai-speaker-label`·
`.ggai-chat-*-nosend` 블록.

## 모드별 주의 (소설 / 챗)

**챗** — 말풍선마다 표시/원문이 이미 분리돼 있다. 게임형 카드가 상정한 형태라 가장 잘 맞는다.
여기서 완성도를 먼저 잡는다.

**소설** — 본문이 통짜 직접 편집 화면이다. 다만 삽화용 인라인 위젯 기계(문단 경계 삽입,
한글 조합 중 지연, 위젯 재사용)가 이미 있으므로 **그 길을 재사용한다. 새로 만들지 않는다.**
매 문단마다 상태창이 끼면 읽기 흐름이 끊기므로, 소설은 **최신 것만** 또는 본문 밖 패널이
나을 수 있다 — 실물을 보고 정한다.

**"본문이 아닌 것" 판단은 한 곳에서.** 소설에는 문단을 세는 기능이 여럿 붙어 있다
(번역, 문단 재생성, 삽화 위치, 내보내기). 양식 덩어리를 문단으로 세면 번역이 밀리고
쓸데없는 번역 비용이 든다. 판단 함수 하나를 만들고 기존 소비자들이 그것을 부르게 한다
(호출 추가는 B급, 동작 변경은 C급 — 경계 주의).
