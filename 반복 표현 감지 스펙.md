# 반복 표현 감지 — 계획서

> 상태: **1차 구현 완료(수동 검증 남음).** 배선은 `구현현황.md` "반복 표현".
> 확장 id `stella:repetition`. 사용자 노출 이름: **「반복 표현」 확장**.
> 이 문서는 설계 진실 소스 — 고치기 전에 이 문서와 `회귀금지.md` 확장/전송본 항목을 확인한다.

## 1. 목적

세션이 길어질수록 같은 표현이 반복되면서 문장이 얇아진다("아득한 눈빛", "숨을 삼켰다",
"a mix of X and Y" 류). 반복되는 표현을 **자동으로 집계**해 "이번엔 다르게 써 달라"는
짧은 목록을 전송본에 넣는다.

**핵심 제약: AI 호출을 하지 않는다.** 순수 집계라 생성 시간이 0초도 늘지 않는다.
이게 이 확장의 존재 이유이므로, 나중에도 여기에 AI 판정을 끼워 넣지 않는다.

**언어 중립**: 한국어 전용이 아니다. 실사용의 다수는 영어 세션이며, 중국어·일본어도 대상이다.
언어별 사전·형태소 분석기를 두지 않고, 언어를 가리지 않는 규칙으로만 처리한다(§3).

## 2. 범위 (1차)

- 활성 경로의 **AI 생성 본문만** 집계 대상.
- 최근 N개 노드 윈도우 안에서 n-gram 빈도 집계 (단위는 언어에 따라 자동 결정, §3).
- 임계값 이상 반복된 상위 K개를 컨텍스트에 주입.
- 확장 탭 설정 패널 하나.

### 하지 않는 것

- AI로 표현을 판정하거나 대체어를 제안하지 않는다.
- 이미 생성된 본문을 자동으로 고치지 않는다.
- 세션을 넘는 전역 통계·학습을 하지 않는다.
- **사용자가 직접 쓴 문장은 집계하지 않는다** — 사용자 문체 교정은 월권.
- 세션창에 새 버튼·패널·표시를 추가하지 않는다 (§6).
- **문형 단위 버릇("not A but B" 류)은 1차 범위 밖.** 빈도로 잡히지 않아 별개 설계가 필요하고,
  이번에는 손대지 않는다. 이 확장을 그쪽으로 확장하지 말고, 필요해지면 그때 따로 논의한다.

---

## 3. 무엇을 "반복"으로 볼 것인가

언어별 사전 없이, 본문 자체만 보고 판단한다.

### 3.1 정규화

유니코드 NFC → 소문자화(대소문자 있는 문자만) → 따옴표·구두점 제거 → 연속 공백 압축.
언어 무관하게 동일하게 적용한다.

### 3.2 집계 단위 자동 선택

본문의 글자 구성을 보고 둘 중 하나를 고른다.

| 모드 | 대상 | 단위 |
|---|---|---|
| 단어 단위 | 띄어쓰기가 있는 언어(영어·한국어·유럽어 등) | 공백 기준 2~4연속 |
| 글자 단위 | 띄어쓰기가 없거나 희박한 언어(중국어·일본어·태국어 등) | 2~6글자 연속 |

판정은 "공백 없는 표의문자·가나·태국 문자의 비율"과 "공백으로 나눈 평균 토큰 길이"로 한다.
언어명을 맞힐 필요가 없다 — 필요한 건 자르는 방식뿐이다.
설정에서 `자동 / 단어 단위 / 글자 단위` 로 **수동 강제**할 수 있다(기본 자동).

### 3.3 흔한 표현 걸러내기 (사전 없이)

"the room", "그녀는 그" 같은 기능어 덩어리가 목록을 채우면 쓸모가 없다.
언어별 불용어 사전 대신 **그 세션 본문에서 자체 학습**한다.

- 윈도우 안 단일 토큰(글자) 빈도를 세어 상위 일부를 "흔한 토큰" 후보로 본다.
- **여기에 "뒤에 오는 말이 두 가지 이상"이라는 조건을 더한다.** 빈도만 보면
  정작 찾으려는 상투구가 1순위 기능어가 되어 통째로 사라진다("아득한"의 뒤는
  언제나 "눈빛으로"라 다양성 1 — 기능어가 아니다). 실물 검증에서 이 조건이
  없으면 한국어 반복이 전부 삼켜졌다(하네스 케이스 10).
- n-gram 의 구성 요소가 **전부** 흔한 토큰이면 후보에서 제외한다.
- 상위 몇 %를 흔하다고 볼지는 설정값(하드코딩 금지).

이 방식이면 어떤 언어에서도 그 언어의 기능어가 자동으로 걸러진다.

### 3.4 후보 정리

- 최소 등장 횟수(설정) 이상만 후보.
- **겹치는 조각 잇기**: 한 문장이 통째로 반복되면 창을 밀며 센 조각이 잔뜩 나온다
  ("let out a breath" / "out a breath she" / …). 가장 긴 n-gram 끼리 한 토큰 차이로
  맞물리고 **횟수까지 같으면** 한 줄로 잇는다 → `"she let out a breath she didn't
  know she was holding" ×3` 한 줄. 횟수가 달라지는 지점에서 끊기므로 서로 다른
  문장이 억지로 붙지 않는다. (없으면 한 문장이 목록 여덟 줄을 차지한다 — 케이스 9)
- **포함 관계 정리**: 긴 n-gram 과 짧은 n-gram 의 등장 횟수가 같으면 긴 쪽만 남긴다
  ("아득한 눈빛" / "아득한 눈빛으로" 가 둘 다 오르는 것 방지). 가장 짧은 조각(2토큰)은
  더 긴 항목 안에 들어 있으면 횟수와 무관하게 버린다(긴 쪽이 이미 같은 말을 한다).
- 제외: 캐릭터·페르소나·그룹 멤버 이름과 별칭, 사용자가 설정에 추가한 단어.
  이름은 **어절 앞부분만 맞아도** 제외한다 — 라온은/라온이, Ellen's 처럼 뒤에 뭐가
  붙어도 이름이다(제외 판정만 느슨하게, 포함 관계 정리는 어절 경계 그대로).
- 아포스트로피는 낱말 안에서만 남긴다(didn't 를 "didn t" 로 쪼개지 않게).
- 노드 경계를 넘어 합산하고, 한 노드 안의 반복도 센다.

### 3.5 어형 변화 (미정 — 착수 전 확인)

완전 일치만 세면 `숨을 삼켰다` / `숨을 삼키며`, `smiled softly` / `smiling softly` 가
서로 다른 표현으로 세어져 임계값을 못 넘고 놓칠 수 있다.

- 1차는 **완전 일치만**으로 간다. 실제 세션에서 얼마나 놓치는지 먼저 본다.
- 보정이 필요하면 언어 중립 수단만 검토한다(예: 토큰 앞부분 몇 글자만 비교).
  언어별 형태소 분석기·stemmer 를 넣는 방향은 채택하지 않는다(§1 언어 중립, §8 롤백 경계).
- 미리 만들지 않는다.

---

## 4. 주입 방식

`contributeContext` → `slot: "custom"`.

- `position: at_depth`, `depth` 는 얕게(최근 맥락에 붙어야 효과가 있다), `role: system`.
- 후보가 없으면 빈 문자열 → 레지스트리가 알아서 무시한다.
- `planSessionRequest` 한 곳에서 수집되므로 **미리보기와 실제 전송이 자동으로 같다**.

지시문은 **설정에서 편집 가능한 템플릿**으로 둔다 (하드코딩 금지).
목록이 들어갈 자리는 자리표시자로 표기한다.

## 5. 설정 (확장 탭 패널)

전부 설정 UI로 노출한다 — 코드에 상수로 박지 않는다.

저장은 **전역 하나**(`PluginData.repetition`) — 세션 스키마를 건드리지 않는 게 롤백
경계라 `ActiveSettings`(세션 meta) 로 가지 않는다.

| 항목 | 비고 |
|---|---|
| 켜기/끄기 | 설정 → 확장 탭의 확장 목록 토글이 겸한다(패널 안에 또 두지 않는다) |
| 감시 범위 | 최근 몇 개 노드까지 볼지 |
| 집계 단위 | 자동 / 단어 단위 / 글자 단위 (기본 자동) |
| 최소 반복 횟수 | 몇 번 이상 나와야 후보인지 |
| 목록 최대 개수 | 컨텍스트에 넣을 상한 |
| 흔한 표현 무시 강도 | §3.3 상위 몇 %를 기능어로 볼지 |
| 지시문 템플릿 | 편집 가능, 자리표시자 포함 |
| 제외 단어 | 사용자 추가 목록 |

공용 컨트롤 킷으로 그린다.

## 6. 표시

**별도 UI를 만들지 않는다.** 지금 무엇이 걸렸는지는 기존 "현재 컨텍스트 확인"(전송본 미리보기)에
그대로 보인다. 확장이 자기 표시 화면을 새로 여는 건 규칙 9 위반이고, 미리보기로 충분하다.

## 7. 성능

- 활성 경로 본문은 전송본을 만들 때 이미 재구성된다. 집계는 그 위에서 선형 1회.
- `(세션 id, 리프 id, 노드 개수, 설정 해시)` 키로 결과를 캐시해, 같은 지점에서 재생성·미리보기를
  반복할 때 다시 계산하지 않는다.

## 8. 롤백 경계

착수 전과 각 단계 완료 시 확인한다.

- 추가되는 것은 **확장 파일 하나 + 설정 패널 하나**뿐이다.
- 세션 스키마를 바꾸지 않는다. 새 저장 파일을 만들지 않는다.
- 확장을 끄면 전송본이 이전과 **byte 단위로 동일**해야 한다.
- 기존 전송본 조립 경로에 조건 분기를 심지 않는다 — 기여형 슬롯만 쓴다.
- 언어별 사전·형태소 분석기·외부 데이터 파일을 추가하지 않는다.

## 9. 완료 검증 (자동은 하네스 `tests/repetition.test.ts`, 나머지는 수동)

1. 반복이 없는 짧은 세션 → 기여 없음(빈 문자열).
2. 확장 끔 → 미리보기가 이전과 byte 동일.
3. 캐릭터·페르소나 이름이 목록에 올라오지 않음.
4. **영어 세션**에서 반복 표현이 잡히고, `the/and/of` 만으로 된 덩어리는 안 잡힘.
5. **일본어/중국어 세션**에서 글자 단위 모드로 자동 전환되고 결과가 말이 됨.
6. 긴 한국어 세션에서 실제로 눈에 거슬리던 표현이 목록에 잡히는지 육안 확인.
7. 생성 시작까지의 체감 시간이 변하지 않음.
