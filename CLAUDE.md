# CLAUDE.md

> Claude Code와 Codex가 함께 쓰는 **상시 작업 규칙**이다. Codex 진입점은 `AGENTS.md`.
> 기능별 배선은 `구현현황.md`, 회귀 규칙은 `회귀금지.md`가 안내하는 해당 분야 문서, 진행 상태는 `ROADMAP.md`에서 확인한다.

## 작업 시작

1. `git status --short`로 기존 변경을 확인하고 다른 세션·사용자의 변경을 보존한다.
2. 코드 수정 전 `회귀금지.md` 표에서 수정 경로에 해당하는 문서만 읽는다.
3. 기존 기능을 수정·확장할 때 `구현현황.md`의 해당 도메인과 관련 스펙을 읽는다.
4. 데이터 구조·식별자·코드 개관이 필요할 때만 `docs/아키텍처.md`, 빌드·배포 작업일 때만 `docs/개발-검증.md`를 읽는다.

문서나 설정만 바꾸는 작업은 링크·설정 구문·공유 대상 여부만 검증한다. 기능 변경이 없으면 vault 수동 검증이나 커뮤니티 패치노트를 완료했다고 기록하지 않는다.

## 프로젝트 경계

옵시디언에서 NovelAI 스토리텔러 형식의 소설 생성을 여러 API로 제공하는 플러그인이다. SillyTavern·NovelAI 파일 왕복, 브랜치 세션, 로어북, 확장 기능을 지원한다.

- 모든 AI 호출은 별도 플러그인 **GGAI Core**의 공개 API를 사용한다. 이 레포에서 API 키나 provider adapter를 구현하지 않는다.
- Core 레퍼런스는 `../obsidian-ggai-core/PLUGIN-API-Guide for creater.md`.
- 같은 구상의 과거 실패 프로젝트 코드를 찾거나 가져오지 않는다. 이 레포의 코드·문서와 확인한 외부 원본만 기준으로 삼는다.
- 사용자 노출 텍스트는 한국어, 파일·폴더·코드 식별자는 영어를 쓴다.
- 한 요청에 필요한 최소 범위만 구현한다. 요청하지 않은 기능·UI·리팩터링을 함께 넣지 않는다.

## 사용자 작업 상태 보존

UI, 상태, 저장, 생성, 임포트, 편집 동작을 바꿀 때 사용자가 이미 만들어 둔 상태를 먼저 식별한다: 스크롤, 열린 탭, 접힘, 포커스, IME 조합, 초안, 선택 노드·세션, 필터·정렬, 미저장 편집.

- 동기화·새로고침을 이유로 화면이나 상태를 통째로 비우고 다시 만들지 않는다.
- 객체 정체성을 유지하는 `setActive...`, `refresh...`, `sync...` 또는 국소 patch 경로를 우선한다.
- 입력 중 재렌더 가드는 `src/views/edit-guard.ts`의 `EditGuard`만 사용한다.
- 세션 본문과 번역 보기는 직접 편집이 기본이다. 별도 편집 모드·모달을 만들지 않는다.
- 사용자가 요구하지 않은 버튼·탭·패널·표시 모드·실행 흐름을 추가하지 않는다.
- 성공 기준에는 빌드뿐 아니라 현재 작업 맥락이 보존되는지가 포함된다.

## 저장과 데이터

- 모든 데이터 mutation은 `StellaStore`를 통과한다. View에서 `vault.create`, `modify`, `trash`, `rename` 등을 직접 호출하지 않는다.
- 조회·임포트·복사·이동·삭제도 기존 store 메서드를 재사용하거나 그곳에 추가한다.
- store 이벤트는 `{ kinds, origin }` detail로 자기 에코와 settings-only 국소 갱신을 구분한다. `suppressOwn*` 플래그를 만들지 않는다.
- 열린 엔티티가 외부에서 삭제·이동되면 active leaf와 파일 참조가 고아가 되지 않게 한다.
- 확정된 JSON 엔티티(session, scenario, prompt preset, lorebook, preset, media 등)는 JSON 단일 파일 정책을 유지한다. 새 형식은 직접 편집성, 라운드트립, 구조 깊이, 외부 호환성을 기준으로 결정한다.
- 스키마와 vault 배치는 `docs/아키텍처.md` 및 각 타입·스펙이 진실 소스다.

## AI와 생성

- View는 Core에 직접 접근하지 않고 `AIService`(`plugin.ai`)만 사용한다.
- 호출 전에 `ai.isAvailable()`을 확인하고 사람이 읽을 수 있는 `label`을 전달한다.
- “생성 중” Notice는 Core가 담당한다. Stella는 중복 Notice를 띄우지 않는다. 청크 진행도와 실패·결과 안내는 Stella가 담당한다.
- 세션에서 API로 보낼 내용의 단일 진실 소스는 `planSessionRequest()`다. 생성과 미리보기는 같은 payload를 사용하며 byte 단위로 같아야 한다.
- 생성·미리보기 전 열린 세션 편집을 `plugin.flushSessionEdits`로 확정한다. 전송 직전 가공은 두 경로에 함께 반영한다.
- 세션 본문을 AI 부가기능에 넘길 때는 `src/util/ai-body-text.ts`의 공용 경로를 사용한다.
- 생성물 저장과 표시 계층을 분리해 JSON만으로도 외부 UI가 소비할 수 있게 한다.

## SillyTavern과 외부 호환

- ST에 같은 개념이 있으면 ST 이름·식별자·순서·동작·import/export shape를 우선한다.
- 사용자 UI 용어는 Stella의 시나리오·로어북·세션을 유지한다.
- Stella 전용 prompt marker를 만들지 않는다. 페르소나는 `{{persona}}` 매크로 또는 기본 위치 자동 삽입을 사용한다.
- 새 prompt/docs는 `{{loreBefore}}`·`{{loreAfter}}`를 쓰고 `{{wiBefore}}`·`{{wiAfter}}`는 호환 alias로만 둔다.
- QR·슬래시 커맨드·매크로·서드파티 확장 동작은 구현 전에 원본 소스에서 인자, 기본값, 반환값, 취소·실패, 결과 위치를 확인한다. 기억으로 정하지 않는다.
- 원본과 다르게 구현하거나 일부만 지원하면 해당 스펙에 “원본에는 있음 / Stella 추가·차이”를 명시한다.

## 구조와 확장

- 기존 store, util, section helper, event plumbing을 먼저 찾아 재사용·확장한다.
- 나중에 Core agent tool로 노출할 가치가 있는 로직은 순수 함수, JSON 직렬화 입출력, 명시적 부작용 인자, 구조화된 오류 결과를 우선한다.
- 설정 UI는 공용 컨트롤 킷(`detail/setting-controls.ts`, 미디어는 `detail/media-prompt-panel.ts`)을 사용한다.
- 설정 패널 확장은 `plugin.registerSettingsPanel(...)`, 생성 과정 확장은 `plugin.extensions.register(...)`를 사용한다.
- 새 확장 슬롯이나 추상화는 실제 소비자가 생겼을 때만 추가한다.

## 사용자 커뮤니케이션

사용자는 비개발자다.

- 사용자가 무엇을 할 수 있게 되는지를 먼저 설명한다.
- 코드 구조·파일명·타입명은 요청받았거나 판단에 꼭 필요할 때만 꺼낸다.
- 선택지는 구현 방식보다 실제 UX 차이로 설명한다.
- 구현 세부 선택과 검증은 에이전트가 책임지고 처리한다.

## 문서 지도

### 상시

- `회귀금지.md`: 수정 경로별 회귀 문서 색인
- `구현현황.md`: 현재 도메인별 진입점·배선
- `ROADMAP.md`: 완료·잔여 작업
- `README.md`: 공개 소개와 설치 안내
- `패치노트.md`: 다음 커뮤니티 게시용 최신 기능 요약이며 누적 이력이 아님

### 진행 중 스펙

- `QR 스펙.md`: Quick Reply와 ST QR v2 호환
- `양방향 번역 스펙.md`: 반영 대기함과 이중 원고
- `미디어 확장 스펙.md`: 번역·삽화 저장과 표시
- `반복 표현 감지 스펙.md`: 반복 표현 확장
- `제작 도구 스펙.md`: 대화형 제작 도구와 AI 도구 계약

완료 기능과 외부 포맷 문서는 `docs/`에 둔다. 관련 기능을 수정할 때만 해당 스펙을 읽는다. 정확한 목록과 용도는 디렉터리에서 확인하며, 루트에는 진행 중인 트랙만 둔다.

## 검증과 문서 갱신

기능 변경의 기본 완료 조건:

1. `npm run build`
2. `npm run test:harness`
3. 접근 가능한 경우 테스트 vault 수동 확인

상세 설치·배포 절차는 `docs/개발-검증.md`를 따른다.

- 로드맵 상태 변화 → `ROADMAP.md`의 기존 항목 갱신
- 구현 상태·배선 변화 → `구현현황.md`의 해당 도메인 갱신
- 새 사고 교훈 → `회귀금지.md`가 가리키는 분야 문서 한 곳에 추가
- 데이터·식별자·큰 구조 변화 → `docs/아키텍처.md` 갱신
- 사용자에게 보이는 기능 변화 → `패치노트.md`의 해당 기능 한 줄을 최신 상태로 교체

날짜별 작업 일지와 변경 이력을 문서에 쌓지 않는다. Git이 이력을 담당한다.
