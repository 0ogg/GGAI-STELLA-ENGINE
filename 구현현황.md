# 구현현황

도메인별 구현 상태·진입점·배선. 기존 기능을 수정/확장하기 전에 해당 섹션을 읽는다.

## 세션 저장·사이드바 연계

세션 원문과 번역 저장은 `StellaStore`의 파일별 `WriteQueue`를 통과한다. 같은 파일의 쓰기는 순서대로 실행하고, 실패한 최신 쓰기는 메모리의 공유 객체를 소스로 재시도한다. 저장 실패 시 앱 전역 안내에 [저장 재시도]가 나타나며, 성공 전에는 캐시를 디스크로 다시 읽어 덮지 않는다. 삭제·이동된 `session.json`은 늦은 저장이 다시 만들지 않는다.

우측 분기 이동·하위 삭제·라인 정리는 `plugin.prepareSessionTreeChange()`에서 열린 소설/챗/PRO 뷰의 원문·번역 초안을 먼저 저장한다. 이때 단순 화면 이동 때문에 양방향 AI 변환은 실행하지 않는다. 생성·변환이 세션 원문을 소유한 동안에는 분기 조작을 중단한다.

세션 제목 변경은 `plugin.renameSession()`이 같은 준비 절차를 거친다. `session-renamed` 이벤트를 받은 플러그인이 열린 모든 `SessionHostView`의 파일 경로만 `retargetSessionFile()`로 바꾸므로 본문 DOM·커서·스크롤·초안·공유 세션 객체를 재생성하지 않는다. 좌측 사이드바는 목록만 갱신하고 경로 전환을 중복 수행하지 않는다.

챗 생성은 준비·양방향 입력 변환·전송·스트리밍·결과 저장을 하나의 busy 구간으로 취급한다. 준비 중 연속 클릭과 같은 세션의 다른 탭 요청은 무시한다. 변수와 로어북 timing은 결과 노드가 남을 때만 확정하며, 결과 저장 실패 시 자동 번역·삽화·그룹 자동 연쇄는 실행하지 않는다.

## 삽화 프롬프트

`src/util/default-media-prompts.ts`의 `illustrationPromptGen` 기본값은 마지막 장면의 한 순간에 보이는 정보로 메인·인물별 프롬프트를 구성한다. 자연어는 배치·물리적 동작을 간결하게 표현하는 데 사용한다. `***`는 장면 전환이며, 고유 외모는 이어받고 의상 등 일시적 상태는 현재 묘사와 시간·장소·활동의 연속성으로 판단한다. 구체적인 의상 전환 예시로 기본 복장을 유도하지 않는다. 사용자 저장 프롬프트는 `resolveMediaPrompt`에서 기본값보다 우선한다.

## 챗 이미지 첨부

챗 입력창의 클립 버튼·붙여넣기·드롭으로 한 메시지에 이미지 최대 4장(장당 20MB)을 첨부한다. 전송 전 파일은 `ChatSessionView.pendingImages`가 메모리에 보존하고, 전송 시 `StellaStore.saveSessionAsset()`으로 세션 `assets/`에 저장한 뒤 사용자 `SessionNode.attachments`에 상대 경로·MIME·캡션을 기록한다. 말풍선은 이 참조로 이미지를 표시하고 라이트박스를 연다.

첨부 시 기존 폰 이미지 이해 경로(`PhoneService.describeImage`)로 캡션을 한 번 만들며, 비전 모델이 없으면 PNG 생성 메타 또는 파일명으로 폴백한다. `planSessionRequest()`가 모든 첨부 캡션과 편집 가능한 `chatImageReaction` 영어 프롬프트를 챗 로그에 결합한다. 활성 Core 챗 프로필의 `supports.vision`이 참이면 바로 답할 최신 사용자 턴의 원본만 멀티모달 블록으로 함께 보내고, 이후 턴과 text 프로필은 저장된 캡션만 사용한다. 컨텍스트 미리보기는 같은 payload의 텍스트와 원본 이미지 장수를 표시한다. 반응 프롬프트는 우측 기본 탭의 모델 섹션에서 선택·편집한다.

## 캐릭터 에셋

전체 플러그인 설정의 `캐릭터 에셋`을 켜면 대시보드 시나리오 상세에 임포트·미리보기 영역이 나타난다. 개별 PNG/JPEG/GIF/WEBP와 SillyTavern sprite ZIP을 받고, ZIP 내부 경로는 ST처럼 버린 뒤 파일명 그대로 시나리오 `assets/`에 저장한다. 같은 basename은 확장자가 달라도 새 파일로 교체한다. 모든 쓰기와 목록 조회는 `StellaStore.importScenarioCharacterAssets()` / `listScenarioCharacterAssets()`를 통과한다.

생성 컨텍스트는 원본 확장의 `{{img_inprompt}}`, `{{img_keywords}}`, `{{img_keywords_autogen}}`, `{{img_keywords_grouped}}`, `{{charkey}}`, `{{img_count}}` 매크로를 제공한다. 챗 원문의 `{{img::파일명}}`은 표시할 때만 시나리오 에셋으로 연결한다. ST 표시 정규식을 전체 로그에 돌리지 않으며, 말풍선에는 슬롯만 만들고 `IntersectionObserver`가 화면 근처 슬롯만 실제 이미지로 바꾼다. 그룹 챗은 멤버 시나리오들의 에셋을 함께 조회한다.

## 대시보드 안전 표시

일반 설정의 `dashboardNsfwProtection`은 항상·모바일·현지 시간대·창 포커스 상실 조건을 각각 선택하며 OR로 합친다(모두 해제하면 필터 없음). 시간대의 시작·종료는 항상 편집 가능한 `HH:MM` 입력이며 분 단위와 자정을 넘기는 구간을 지원한다. 활성 중에는 `src/util/dashboard-content-safety.ts`가 삽화 `tags`의 정확한 `nsfw` 태그를 판정하고, 구 데이터는 저장된 생성 프롬프트의 독립 태그도 호환 판정한다. `DashboardView`는 홈 최근 삽화와 갤러리에서 해당 항목을 제외하고, 이어하기 카드에는 active 여부와 무관하게 같은 세션의 최신 비NSFW variant를 대표 이미지로 고른다. 없으면 시나리오 표지로 폴백한다. 시간대와 창 포커스 변화는 열린 대시보드에서 재평가한다.

## 임포트

진입점: `src/import/index.ts` `importFile(bytes, filename, vault)` (포맷 판별은 `src/import/detect.ts` `detectFormat`, view 는 `store.importFile()` 경유). UI 는 `src/views/entity-actions.ts` `runImportPicker` (대시보드/사이드바 [가져오기]) — 결과는 `reportImportResult` 가 kind 별로 Notice.

지원 포맷 (`ImportFormat`): CCv3/V2/V1 캐릭터카드(JSON/PNG/APNG), CHARX, ST 월드인포, NAI 로어북/시나리오/스토리, ST 프롬프트 프리셋, ST 빠른 답장(QR) 세트, ST 정규식 스크립트, ST 페르소나 백업, ST 채팅(.jsonl, 별도 경로).

- **ST 페르소나 백업** (`sillytavern-persona-backup`): personas.js `onBackupPersonas` 산출물(`{personas, persona_descriptions, default_persona}`). 판별은 `personas`+`persona_descriptions` 두 딕셔너리 동시 존재. 파서 `src/import/parse-sillytavern-persona.ts`, 기록 `src/import/write-user.ts` `writeImportedPersonas` — `GGAI/USERS/<이름>.json` 로 페르소나 카드 생성. 이름·설명만 가져온다. **가져오지 않는 것**: 아바타 이미지(백업에 실제 그림 없음, 파일명만 있음), position/depth/role/lorebook 연결(스텔라는 `{{persona}}` 매크로/기본 위치 규약이 달라 개념 자체가 없음). 이름이 기존 페르소나와 겹치면 건너뜀(ST 자체 복원과 동일 정책). 백업의 default_persona 는 참고 정보로만 알림에 표시 — 활성 페르소나를 자동 전환하지 않는다(사용자 워크플로 보존).

## 그룹 채팅 — 발화자 버튼 / 세션 메뉴 버튼

세션창 리모컨 우측 위 줄:

- **발화자 버튼**(`chat-session-view.ts` `speakerBtn`, 그룹 챗에서만 노출)
  - 클릭 = `openSpeakerMenu({x,y})` — 자동 + 멤버 목록. **이름 좌클릭 = 그 멤버 지목**
    (핀은 뷰 상태, 저장 안 함). 맨 아래 `setIsLabel` 안내 줄로 우클릭 존재를 알린다.
  - 멤버 줄 **우클릭·꾹(650ms)** = `openSpeakerMemberMenu()`(2단): 맨 위에 멤버 이름표
    (`setIsLabel`, 뮤트·주인공 표시) + **다음 발화자로 지목 / 뮤트 / 그룹에서 내보내기**. 주인공은 내보내기 항목이 disabled(세션이 주인공 폴더에
    산다 — 회귀금지.md). 배선은 `attachMemberRowMenu(item, menu, id)` — 줄 DOM(`item.dom`,
    공개 타입에 없어 옵셔널 캐스트)이 있을 때만 붙고, 없으면 좌클릭 지목만 남는다.
    목록은 `setUseNativeMenu(false)` 로 DOM 메뉴 고정(네이티브 메뉴엔 줄 DOM 이 없다).
  - 쓰기는 `entity-actions.ts` `setGroupMemberMuted` / `removeGroupMember`(ConfirmModal) →
    `store.saveGroup` → `groups-changed` 로 열린 세션창이 스스로 갱신.
- **세션 메뉴 버튼**(`more-vertical`, 소설·챗 양쪽 = `session-view.ts` / `chat-session-view.ts`,
  퍼즐 버튼 바로 위 자리): `buildSessionMenu(plugin, sessionListItemOf(file, session),
  { omitOpen: true })` — 사이드바 우클릭과 같은 항목(그룹 채팅 관리·제목·복제·선채팅…),
  보고 있는 세션이므로 "이어하기"만 뺀다. 항목 추가는 `session-menu.ts` 한 곳.

**뮤트**(`StellaGroupMember.muted`, ST 그룹 disabled member 대응): 자동 발화자 판결에서만 제외한다
— 지목하면 말하고, 카드/로어북 주입은 그대로다(방에 있되 먼저 나서지 않음). 반영 지점 3곳:
챗 뷰 `chooseSpeaker`(후보 필터) · `maxAutoReplies`(기본 라운드 길이 = 뮤트 제외 멤버 수) ·
`proactive-service.ts` `resolveGroupSpeaker`(선채팅). **전원 뮤트면 전원을 후보로 되돌린다**(대화 정지 방지).
그룹 채팅 관리 모달은 뮤트를 **뱃지로 표시만** 한다(토글은 발화자 버튼 메뉴).
