# 회귀금지.md — 재발 사고 교훈 모음

> 실제로 터졌던 사고에서 나온 "하지 마라" 목록. **코드를 수정하기 전, 만지는 영역의 항목을 반드시 확인한다.**
> 형식: **규칙** — 사고 요약 — 올바른 경로. 새 사고 교훈은 여기에만 추가한다 (CLAUDE.md 에 서사로 쌓지 않는다).

---

## 렌더/이벤트/입력

- **좌우 사이드바가 세션 트리를 바꾸기 전에 열린 편집과 진행 중 작업을 확인한다.** — 우측 분기 이동·하위 삭제가 세션창의 미저장 본문/번역을 flush하지 않고 공유 세션 객체의 `activeLeafId`·노드 트리를 바로 바꾸면 편집이 취소되거나 생성 중 노드를 삭제할 수 있다. 제목 변경 때 `setViewState`로 탭을 다시 만들면 커서·스크롤·초안도 사라진다. → 분기 조작·제목 변경은 `plugin.prepareSessionTreeChange`/`plugin.renameSession` 경유, 초안 저장은 `flushSessionEdits(..., { convert:false })`, 생성·변환 중에는 중단. 이름 변경은 `session-renamed` → `SessionHostView.retargetSessionFile`로 경로만 바꾸고 DOM·공유 객체를 유지한다.

- **세션 저장 실패를 완료로 취급하거나 삭제된 파일을 늦은 저장으로 되살리지 않는다.** — 디스크 부족·동기화 오류 뒤 화면에는 생성 결과가 남아도 저장되지 않았는데 자동 번역·삽화·다음 발화가 계속 돌면 재실행 때 결과가 사라지고 파생 데이터만 남는다. 파일별 쓰기가 겹치면 오래된 저장이 우측 설정을 되돌릴 수도 있다. → `StellaStore`의 `WriteQueue`로 직렬화하고 실패한 최신 쓰기를 공유 객체 기준으로 재시도한다. 실패 중 `refreshSession`은 캐시를 유지하며, 결과 저장 성공 전에는 생성 완료 확장을 실행하지 않는다. 기존 파일이 없는 일반 저장은 create로 복구하지 않는다(신규 생성은 `createSession` 전용).

- **`suppressOwn*` 수동 억제 플래그를 다시 만들지 않는다.** — 뷰마다 손으로 억제 플래그를 관리하다 누락·중복 렌더가 반복됐고 2026-07 재렌더 개편으로 전면 폐기됨. → `store.saveSession(file, session, { kinds, origin })` detail 방식: 핸들러가 `detail?.origin === this.storeOrigin` 이면 자기 에코 skip, `kinds:["settings"]` 면 국소 갱신만. 번역/삽화 저장·서비스(opts.origin)도 동일.

- **편집 UI 재렌더 가드를 손으로 복붙하지 않는다.** — 복사할 때마다 조합/포커스/자기저장 가드가 한 조각씩 빠져 "입력 포커스 소실"이 반복 재발했다. → 공용 `src/views/edit-guard.ts` `EditGuard` 만 사용: 이벤트 핸들러 첫 줄에서 `isSavingSelf`/`isEditing()` 확인, 저장은 `runSave()`, 포커스 중·조합 중·pending 편집 존재 시 textarea 외부 덮어쓰기 금지. IME 조합 중 input 을 무시하는 필드는 반드시 `compositionend` 에서 확정값을 onChange 로 넘긴다(마지막 글자 유실 방지). store 쪽 `SELF_WRITE_GRACE_MS`(2000ms) 방어와 겹으로 유지.

- **한글 IME 조합 중 배경 뷰가 본문 DOM 이나 문서 선택영역을 건드리지 않는다 (입력 마비 사고).** — 어느 입력칸에서든 조합 진행 중에 배경 세션창이 DOM/`Selection` 을 만지면 Chromium 이 조합을 얼린다(커서 소실 + 키 전부 삼킴, 창 전환해야 풀림). 메모리/작가노트 등 디바운스 저장이 쏜 이벤트를 배경 세션창이 받아 본문을 손대는 경로가 원인이었다. → `edit-guard.ts` 전역 트래커(`installGlobalImeTracker`/`isImeComposing`/`runWhenImeIdle`) + 세션창 `deferWhileComposing`(외부 변경/매크로/번역/인라인 위젯 재배치를 조합 종료 뒤로 코얼레싱). `setCaretOffset` 은 다른 편집칸이 포커스면 선택영역을 건드리지 않는다. 배경에서 contenteditable 재렌더/선택영역 조작 코드를 새로 넣을 때 반드시 이 게이트를 태운다.

- **명시적 액션(이어쓰기/재생성/undo/redo/미리보기 flush)은 조합 중이라도 편집을 강제 커밋한다 (입력 롤백 사고).** — 세션창 `commitPending()` 은 IME 조합 중 `body.empty()` 재구성이 조합을 얼리므로 커밋을 미룬다. 그런데 이 지연을 호출자가 몰라, 조합 중 마지막 음절이 미커밋인 채 이어쓰기가 진행돼 **입력 전 노드에서 생성이 시작되고 방금 친 내용이 롤백**됐다(한글 사용자, 조합 중 클릭). → 순수 타이핑 hot-path(blur/idle/selectionchange)만 미루고, 편집을 확정하고 진행하는 명시적 액션은 `commitPending({ force: true })` 로 조합을 강제 종결(`composing=false` + `syncPendingDiff` 로 DOM 의 진행 중 조합 문자열까지 캡처)한 뒤 커밋한다. 생성·미리보기·형제이동 전 flush 는 전부 force 경로.

- **편집 확정을 `blur` 단독에 걸지 않는다 (모바일 이름 유실 사고).** — 모바일 웹뷰는 입력칸이 화면에서 사라질 때(OS 뒤로가기·탭/세션 전환·앱 백그라운드·제스처 이동) `blur` 를 신뢰성 있게 쏘지 않는다. 프롬프트 세트/항목 이름을 고치고 blur 없이 벗어나면 저장이 아예 안 일어나 개명이 유실됐다(제보: "수정 후 확인하니 그대로"). 프롬프트 편집기만 유독 ① 글자마다 저장 안 함 ② dispose flush 없음 ③ 앱 전환 flush 없음 — blur 하나에 전부 걸려 제일 잘 터졌다. → **모델 반영은 `input`(글자마다)에서** 하고 저장은 debounce, 확정은 blur **와 함께** dispose·`visibilitychange(hidden)`·`window blur` 에서도 flush. 공용 `renderEditableTitle`(제목)은 라이브로 모델에 넣고(재렌더 금지 — 입력 파괴), `form-renderer` 필드와 같은 방식. detail-view 처럼 외부발 refresh 가 입력칸을 재렌더로 날리는 섹션(`PromptsSection`)은 타이핑 중엔 저장하지 말고 모델만 갱신, 저장은 blur/`flush()` 에서만(호스트가 5개 flush 지점에서 호출). 새 편집 UI 를 넣을 때 "blur 에서만 저장" 패턴을 다시 만들지 말 것.

- **작성 중인 입력값을 DOM 에만 두지 않는다 (폰 SNS "사진만 올라감" 사고).** — 폰 SNS 작성칸은 `textarea.value` 가 유일한 저장소인데 첨부(`pendingAttach`)는 필드였다. 그래서 **글을 쓴 뒤 사진을 첨부하면**(첨부 미리보기 = `renderBody()`) 글만 지워지고 사진은 살아남아, 사용자가 [게시]를 누르면 **그림만 올라갔다**. 번역·SNS 생성이 끝나 피드 이벤트가 오는 순간에도 같은 일이 났다 — 재렌더 보류 가드(`isSnsComposing`)가 **포커스+내용**을 함께 요구해, 사진 피커 모달이 포커스를 가져간 뒤에는 가드가 풀려 있었다. → **초안은 필드가 진실 소스**(`snsDraft`/`replyDraft`): `input` 마다 필드에 넣고 렌더 때 `ta.value = draft` 로 되살린다(전송 시 비움, 다른 글의 답글칸으로 옮기면 리셋). 포커스 기반 재렌더 가드는 "타이핑 중 깜빡임 방지"용 보조일 뿐, 데이터 보존을 여기에 걸지 않는다. 새 작성 UI 를 넣을 때 입력값이 재렌더를 넘어 사는지부터 확인할 것(문자 작성칸은 `buildComposer` 로 body 밖에 한 번만 만들어져 애초에 안전).

- **세로 flex 목록의 항목에 `overflow: hidden` 을 줄 때는 `flex: 0 0 auto` 를 함께 준다 (SNS 카드 뭉개짐).** — 피드 카드에 사진 풀블리드용 `overflow:hidden` 만 넣었더니 카드들이 납작하게 눌려 서로 겹쳤다. flex 항목의 자동 최소 크기(`min-height:auto` = 내용 높이)는 **overflow 가 visible 이 아니면 0 이 된다** — 스크롤 컨테이너 안에서도 항목이 무한히 줄어든다. → 내용 높이를 지켜야 하는 항목은 `flex: 0 0 auto`(칩 바가 이미 같은 이유로 고정돼 있었다). 새 카드/행에 overflow·clip·scroll 계열을 넣을 때 항상 짝으로 확인.

- **접힘/숨김 상태를 `hidden` 속성으로 만들지 않는다 — 우리 CSS 가 그 요소에 `display` 를 주는 순간 무력화된다.** — 세션 노트 위젯(QR `/comment`)이 `body.hidden = true` 로 접었는데 `.ggai-note-body { display: block }` 이 브라우저 기본 `[hidden] { display: none }` 을 이겨(작성자 스타일 > UA 스타일, 옵시디언 app.css 에도 `[hidden]` 규칙 없음) **한 번도 접힌 적이 없었다**. 접힌 상태에선 textarea 기본 높이(2줄) + `overflow:hidden` 이라 본문이 잘려 보이고 눌러도 변화가 없어 "잘린다 / 안 접힌다"로 제보됐다(2026-07-31 수정). → 표시 상태는 **클래스 하나**(`.is-open`)가 진실 소스이고 `display` 는 CSS 가 소유한다. `hidden` 속성이나 `el.hide()`(인라인 스타일)로 상태를 표현하지 않는다. 곁가지 둘: 접이식 본문 높이를 픽셀로 고정하면(autosize) 폭이 바뀔 때 잘리므로 `ResizeObserver` 로 폭 변화 때 다시 맞춘다. 챗 뷰는 재렌더마다 `host.empty()` 로 다시 그리므로 노트 위젯도 삽화처럼 **풀에서 재사용**해야 펼쳐 둔 게 안 접힌다.

- **스트리밍 중 이미 그린 것을 지우고 다시 그리지 않는다 (생성 중 버벅임·메모리 폭증·스크롤 튐 사고).** — `renderStreamingTail` 이 조각(delta)마다 `container.empty()` 후 그때까지의 생성분 전체를 글자 단위로 다시 만들었다. ① 비용이 **생성 길이의 제곱** — 3000자 생성이면 옛 방식 112만 자 재생성 vs 새 방식 2.4만 자(47배), 8000자면 125배. 버려진 DOM 이 GC 전까지 쌓여 메모리가 치솟고 옵시디언 밖까지 끊겼다. ② 지우는 순간 tail 높이가 0으로 무너져 브라우저가 `scrollTop` 을 강제로 clamp — 다시 채우면 튕겨 돌아오는 **초당 수십 번의 위아래 떨림**이 "생성 중 화면이 위로 딸려 올라간다"의 정체였고, 흔들리는 높이를 읽는 `isNearBottom` 도 오판해 읽으려 올린 화면이 바닥으로 끌려갔다. → **덧붙이기만 한다**: 줄바꿈이 지나간 줄은 굳히고(`streamTailCommitted`) 쓰고 있는 마지막 한 줄만 다시 그린다(`planStreamTail`). 그리기 자체는 `scheduleStreamPaint` 로 **한 프레임에 한 번** — 조각마다 그린 뒤 `scrollHeight` 를 읽으면 브라우저가 모아 처리하려던 레이아웃을 매번 강제로 끊어 쓴다(세션 전체가 문단 없는 인라인 한 덩어리라 그 비용이 세션 길이에 비례한다). 새 스트리밍 표시 경로를 넣을 때 "매번 전부 다시 그리면 안전하다"를 다시 고르지 말 것 — 안전한 대신 길이의 제곱으로 터진다.

- **UI 동기화를 통짜 재렌더로 해결하지 않는다.** — 원칙이지만 반복 위반됐다. → store 이벤트 받아 자기가 표시하는 값만 국소 갱신, 스크롤·접힘·포커스·편집 중 값 보존. 전체 렌더는 DOM 소유권이 실제로 바뀔 때만.

- **목록 표시용 재스캔은 이미 캐시된 세션 객체를 건드리지 않는다 (설정 롤백 → 생성 노드 삭제, 두 번 터진 사고).** — 1차(설정 롤백): `refreshSessions()` 가 `sessionByFile` 항목을 새 객체로 **교체**해 열려 있는 SessionView 의 `this.session` 이 캐시에서 떨어져 나갔고, 디테일 뷰에서 바꾼 모델/파라미터/프롬프트/번역 설정이 다음 생성 때 세션창의 **옛 사본** 저장으로 직전 값으로 롤백됐다. 이를 `assignSessionInPlace(existing, fresh)` **제자리 덮어쓰기**로 막았더니 2차(생성 노드 삭제): 사이드바에서 시나리오를 펼쳐 두면 생성 저장마다(자기 에코도 안 거름) `session-changed` → `refreshSessions` → `scanSessions`(디스크 직접 `adapter.read`) → 활성 세션 객체를 **아직 flush 안 된 stale 디스크로 덮어써**, 방금 붙은 생성 노드가 세션창 메모리에서 삭제되고 이후 저장이 그 빈 상태를 굳혀 JSON 에서도 사라졌다(화면엔 1~2초 떴다 사라짐, 로그엔 있는데 JSON 엔 없음, 소설·챗 공통, 간헐적, 동기화 무관·새 볼트도 재현). → **목록 재스캔은 캐시 객체를 교체도 덮어쓰기도 하지 않는다**: `existing` 이 있으면 `item.session = existing` 로 그 최신 객체를 목록에 쓰기만 한다(`getSessions` 의 "이미 있으면 안 덮어씀" 과 동일 정책). 목록은 meta(이름/즐겨찾기/시각) 표시용이라 세션 본문 객체를 소유·수정할 이유가 없다. 활성 세션 객체의 갱신은 저장 경로(`saveSession`)와 외부 변경 반영 경로(`refreshSession` 단수, self-write grace 적용)만 맡는다. **"공유해놓고 가드로 막기"가 아니라 애초에 목록이 활성 객체를 공유하지 않는 게 정답.**

- **창 포커스 복귀발 `refreshSession` 이 방금 저장한 세션을 stale 디스크로 덮어쓰지 않는다 (생성 유실 사고).** — 이어쓰기를 걸고 창을 벗어난 사이(알림음이 그 증거 — 안 보는 세션만 울림) 백그라운드에서 생성이 끝나 `session.json` 저장이 시작되는데, 이때 `this.generation` 은 이미 `null`. 돌아오는 순간 window `focus` → `handleExternalChange` → `refreshSession` 이 **디스크를 강제로 다시 읽어** 캐시를 덮어쓴다. 동기화 폴더/느린 디스크에선 `vault.modify` 가 아직 안 내려가 옛 내용을 읽고, 방금 받은 노드가 메모리에서 사라져 세션뷰·분기에서 증발했다(로컬 SSD 는 창이 좁아 거의 안 걸려 재현이 특정 환경에서만 남). 디스크엔 대개 정상 저장돼 있어 세션을 닫았다 열면 되살아남. → `refreshSession` 은 `isRecentSelfWrite(file)` 이면 디스크 재읽기를 건너뛰고 캐시를 반환한다(`onVaultChange` 와 동일한 `SELF_WRITE_GRACE_MS` 규칙). 창/가시성 복귀처럼 vault 이벤트를 안 거치는 재읽기 경로를 새로 넣을 때 반드시 이 self-write grace 를 태운다.

- **디스크 재읽기는 "메모리가 디스크보다 새로우면" 절대 덮어쓰지 않는다 — modifiedAt 불변식 (우측 디테일뷰발 생성 유실 사고, 위 두 사고의 최종 봉쇄).** — 생성은 새 노드를 메모리에만 추가하고 저장은 종료 후라, 생성이 도는 수십 초 동안 디스크는 항상 메모리보다 옛날이다. 이 사이 어떤 경로든(우측 디테일뷰 시나리오 탭 로드 `resolveActive`, 창 포커스 복귀 `refreshVisibleSectionsFromDisk` 등) `refreshSession` 이 돌면 2초 self-write grace 는 이미 지나 있어 공유 세션 객체가 노드 없는 디스크본으로 덮여 생성이 통째로 사라졌다(제보: 이어쓰기 중 우측 패널을 클릭만 해도 실패 — 트리거는 화면 상태 의존이라 재현이 갈렸다). → `refreshSession` 은 디스크 `modifiedAt <= 메모리 modifiedAt` 이면 캐시를 그대로 반환한다. 자기 저장은 `saveSession` 이 호출 즉시 캐시 객체의 modifiedAt 을 올려 항상 메모리 ≥ 디스크이고, 진짜 외부 변경은 `onVaultChange` 가 캐시를 지워 비교 자체를 안 거친다(fresh 경로). **개별 트리거를 쫓아 가드를 덧대지 말 것** — "오래된 디스크로 새 메모리를 덮지 않는다"는 이 한 줄이 그 클래스 전체를 닫는다. 새 디스크 재읽기 경로를 추가해도 이 불변식 아래에서는 안전하다.

- **활성 설정을 "못 찾았으니 기본값으로" 조용히 덮어쓰지 않는다.** — `PromptsSection` 이 세션의 `promptSetId` 를 목록에서 못 찾으면 목록 첫 세트를 활성으로 **저장**해, 세트 이름 변경·삭제·레거시 필드 세션의 프롬프트가 사용자 모르게 갈아끼워졌다. → 자동 지정은 값이 **아예 없을 때만**. 지정돼 있는데 해석 실패면 표시만 비우고 저장은 건드리지 않는다.

- **편집면(`contenteditable`)의 속성을 직접 `setAttr` 하지 않는다 — 모바일 탭 게이트가 소유한다.** — 본문·번역칸·말풍선이 항상 편집 가능하게 깔려 있어, 모바일에서 스크롤하고 손을 떼면 브라우저가 그 자리에 caret 을 놓아 키보드가 올라왔다(제보: "스크롤할 때 입력바가 활성화된다"). → `src/util/tap-to-edit.ts` `attachTapToEdit(el)` 이 평소엔 속성을 떼 두고 **제자리 짧은 탭**(이동 8px·700ms 이내, 스크롤 후 300ms 정적)에만 되돌려 붙인 뒤 포커스 + 탭 지점 caret 배치를 한다(데스크톱은 게이트 없음 — 값만 기억). 그래서 이 요소들의 contenteditable 은 게이트가 소유한다: 생성 중 잠금 같은 "편집 금지"는 `setEditAllowed(el, false)`, 재렌더 뒤 포커스 복원과 메뉴발 편집 진입은 `beginEdit(el)`(속성이 떨어져 있어도 되살린다). 직접 `setAttr("contenteditable", …)` 로 되돌리면 게이트가 풀려 스크롤 오인이 그대로 재발하고, 반대로 `focus()` 만 부르면 잠긴 요소라 포커스가 안 잡혀 "입력이 씹힌다"로 나타난다. 편집이 잠긴 동안에도 글자 선택이 되도록 해당 요소에 `user-select: text`(+`-webkit-`)를 명시할 것.

- **읽는 화면을 편집칸으로 깔아 두지 않는다 — 누를 것이 있는 화면은 읽기가 기본이다.** — 챗 말풍선이 항상 `contenteditable` 이라 **탭 = 편집 진입**이었다. 그래서 (1) 카드가 그린 상태창·표를 누르면 화면이 HTML 원문 글자로 튀어나오고 (2) 링크·접기(`details`)·이미지·선택지 버튼처럼 **눌러야 동작하는 것이 전부 죽었다**(캐럿만 놓인다) (3) 모바일에서 읽다가 오탭하면 키보드가 올라왔다. 탭 게이트(`tap-to-edit`)는 (3)만 덮는 증상 처치였다. → 편집 진입은 **명시적 버튼**으로만 연다(실리태번 `.mes_edit` 과 같은 구조): 열린 하나만 편집칸이 되고 나머지는 표시본 그대로다. 판정 상태(`editing`)를 뷰가 들고 있어야 한다 — DOM 포커스로만 "편집 중"을 판정하면 편집칸을 닫은 뒤에도 포커스가 남아 재렌더가 통째로 미뤄진다. blur 는 커밋만 하고 닫지 않는다(다른 곳을 눌렀다고 편집이 사라지면 안 된다). 표시 계층에 **누를 수 있는 것을 새로 넣을 때마다** 그 위가 편집칸인지 먼저 본다.

- **읽는 글 위의 롱프레스를 우리 메뉴가 가져가지 않는다 — OS 단어 선택(사전/웹 검색)이 거기 걸려 있다.** — 말풍선 전체에 롱프레스 메뉴를 붙여, 모바일에서 단어 하나를 골라 복사하거나 사전에 넣는 것이 아예 불가능했다(제보: 영어 공부하며 노는 사용자). **코드로 만든 선택에는 iOS·안드로이드 둘 다 기본 메뉴를 띄워주지 않으므로**("단어를 대신 선택해 주기"로는 해결되지 않는다) 제스처 자체를 OS 에 넘겨야 한다. → `press-menu.ts` `attachContextMenu(..., { yieldToTextSelection: true })`: 모바일에서 **손가락이 글자 위에 떨어졌으면** 우리 메뉴를 띄우지 않는다(롱프레스 타이머 + 안드로이드의 `contextmenu` 경로 **둘 다** — 후자에서 `preventDefault()` 하면 OS 선택 메뉴까지 사라진다). 판정은 타이밍이 아니라 위치다(`pointIsOverGlyph` — caretRangeFromPoint 는 여백에서도 가장 가까운 글자를 돌려주므로 그 글자의 사각형이 좌표를 품는지까지 본다). OS·버전마다 롱프레스 임계값이 달라 타이밍으로 가르면 폰마다 동작이 갈린다. 글자를 OS 에 넘긴 만큼 **메뉴를 부를 글자 없는 자리**(아바타·이름줄)를 같이 열어둘 것 — 안 그러면 메뉴가 사실상 사라진다.

## 세션창/스크롤

- **본문 전체 재렌더 경로는 `preserveReadingPosition` 으로 보던 지점을 재중앙한다.** — 인라인 삽화 이미지 재로드로 높이가 출렁이며 스크롤이 맨 위로 튀던 회귀(2026-07-08 수정). 대상: 생성 종료·오류·중단, 문단 재생성 [적용], undo 미리보기, undo/redo/형제 이동. 위치 계산 실패 시 저장 앵커를 지우지 않고, 복원 실패 폴백은 **항상 맨 아래**(맨 위 금지). 예외는 생성 중 followTail 뿐.

- **본문/번역 패널은 통째로 다시 만들지 않는다 — 바뀐 지점부터 끝까지만 다시 그린다.** — "맨 위로 튐"의 진짜 뿌리(2026-08-16). 이어쓰기 종료·재생성·번역 완료가 전부 `body.empty()`(또는 `translationEl.empty()`) → 전체 재생성이었다. 긴 세션에서는 (1) 그리는 동안 문서 높이가 무너져 브라우저가 스크롤을 그 높이로 clamp 해 버리고(= 위로 끌려감, 복원 코드가 뭘 하든 이미 늦음) (2) 매번 수천 개 span 을 새로 만들어 화면이 멎었다. → **본문**: `util/body-runs.ts` `buildBodyRuns`(displaySpans + 숨김 구간 → 런 목록, 순수 함수)로 렌더 계획을 만들어 `renderedRuns` 에 들고 있다가, 새 계획과 비교해 **실제로 달라진 런의 DOM 만** 갈아 끼운다(`patchBodyRuns` → `spliceBody`). 런 개수가 같으면(매크로 표시값 변경 `{{user}}`/`{{char}}`, 숨김 토글) 흩어진 그 자리들만 개별 교체하고 — **뒤에서 앞으로** 고쳐 앞쪽 offset 이 밀리지 않게 한다 — 개수가 바뀌면(이어쓰기/재생성/문단 재생성) 앞뒤 공통 부분을 뺀 가운데 한 구간만 교체한다. 화면에 보이는 앞부분 DOM 은 손대지 않으므로 스크롤이 움직일 이유 자체가 없다. **번역 패널**: 문단 구성(해시/원문/offset/개수)이 같으면 `syncTranslationBlocks` 가 바뀐 문단의 글자만 갈아 끼운다. 어느 쪽이든 계획과 DOM 이 어긋나면(미저장 편집이 버려지는 경우 등) 전체 렌더로 물러선다 — 그 판단은 `renderedRuns = []` 로 명시한다. **"일단 다 지우고 다시 그린 뒤 스크롤을 되돌리자"로 되돌아가지 말 것** — 그 방식이 이 이슈를 수십 번 재발시킨 원인이다.

- **본문 재렌더는 `renderBodySpans()` 를 직접 부르지 않는다 — 스크롤 복원을 소유한 경로(`preserveReadingPosition` 또는 `redrawBodyPreservingCaret`)를 통과한다.** — "맨 위로 튐"이 수십 번 재발한 진짜 이유는 계산 실패가 아니라 **복원을 아예 안 거치는 재렌더 경로가 계속 새로 생긴 것**이다(2026-08-16 수정). 실제로 비어 있던 곳: `redrawBodyIfDisplayChanged`(시나리오/페르소나/요약 변경으로 `{{char}}` `{{user}}` `{{summary}}` 표시값이 바뀔 때), `commitPending` 의 편집 확정 재렌더, QR 버튼 결과 붙이기(`applyQuickReplyText`) — 셋 다 복원 코드가 한 줄도 없었다. → (1) 전체 재렌더는 위 두 경로만, (2) 예외는 주석 `// scroll-owner: …` 로 누가 스크롤을 책임지는지 밝히고(최초 render, 집필 프로 변환의 픽셀 유지 2곳뿐), (3) **하네스(`tests/architecture-rules.mjs`)가 이 규칙을 강제**한다 — 새 호출부는 빌드가 아니라 하네스에서 잡힌다. 개별 호출부에 복원을 손으로 다는 방식으로 되돌리지 말 것.

- **복원은 "시도했다"가 아니라 "실제로 들어갔다"까지 확인한다 — 스크롤 대입이 무효인 순간(숨은 탭/높이 0)이 곧 맨 위 방치다.** — `preserveReadingPosition` 은 보던 지점을 못 잡으면(raw=null: 숨은 패널·매핑 실패) 아무것도 안 하고 끝났고, `clientHeight===0` 이면 재중앙도 조용히 걸러져 재렌더 직후의 `scrollTop=0` 이 그대로 남았다(2026-08-16 수정). → 재렌더 **전** 픽셀을 들고 있다가, ① raw 없음 = `rescueDroppedScroll`(0으로 떨어졌을 때만 픽셀 복귀), ② 높이 0 = `restoreTarget` 에 넘겨 ResizeObserver 가 크기 잡히는 순간 재시도. 챗 뷰 `renderMessages` 도 같은 규칙(`restoreScrollTop`, 값이 들어갈 때까지 몇 프레임 재시도).

- **커서가 본문 밖이면 커서를 "0"으로 복원하지 않는다.** — `getCaretOffset()` 이 선택영역이 본문에 없을 때 0을 돌려줘, 재렌더마다 문서 **맨 앞**에 커서를 심었다(긴 세션에서 그 뒤 포커스/스크롤이 맨 위로 끌려가는 원인). → 본문 밖이면 `null`, 호출부는 복원을 건너뛴다. `0`(맨 앞)과 `null`(복원 대상 없음)을 같은 값으로 뭉개지 말 것.

- **편집 중(본문/번역칸 포커스) 재렌더는 재중앙하지 않고 픽셀만 지킨다.** — 재중앙은 브라우저가 커서에 맞춰 둔 스크롤을 빼앗아 모바일 키보드 위로 튕겨 올린다(`onResizeSettled`/`keepCaretVisible` 과 같은 판정). → `redrawBodyPreservingCaret` 은 `isEditingFocused()` 면 `rescueDroppedScroll` 만 태운다.

- **분기 패널(`BranchSection`)의 재렌더는 스크롤 위치를 담았다 되돌린다.** — `renderBody()` 가 `body.empty()` 로 다시 그려 노드 이동·카드 펼치기·즐겨찾기·외부 갱신마다 목록/지도가 맨 위(전체 보기는 좌상단)로 튀었다(2026-08-15 수정). → `captureScroll()`(지우기 직전) + `restoreScroll()`(즉시 + rAF 한 번 더). 지도 뷰포트는 가로·세로 둘 다. 기억을 버리는 경우는 `render(false)`(세션 전환/모드 전환/최초 로드)뿐이고, 최초 진입의 현재 노드 자동 중앙(`pendingAutoCenter`)이 복원보다 우선. 새 재렌더 진입점을 만들면 `renderBody()` 를 거치게 할 것.

- **"보던 지점"의 offset 은 재렌더 뒤 본문에 존재하지 않을 수 있다 — 위치를 못 찾으면 그 자리(재렌더 직후 scrollTop=0 = 맨 위)에 두지 않는다.** — 분기 이동(분기 패널 점프/형제 이동/undo·redo/본문 띠 되돌리기)은 본문이 짧아지거나 갈라져 보던 대목이 사라지는데, `charRectAtDisplayOffset` 이 null 을 돌려주고 `preserveReadingPosition` 이 폴백 없이(fallbackToEnd=false) 끝나 매번 맨 위로 튀었다(2026-08-15 수정). → (1) 끝을 넘어선 offset 은 본문 길이로 clamp 하고 앞뒤로 스캔, (2) `preserveReadingPosition` 의 **첫 시도만** 끝으로 폴백(이후 삽화 로드 재중앙은 폴백 없음), (3) `handleExternalChange`(외부발 재렌더)도 `preserveReadingPosition` 경유. 노드 앵커 복원(`restoreToAnchor`)은 앵커가 새 활성 경로에 남아 있을 때만 — 사라진 앵커면 무조건 맨 끝으로 끌고 가 preserve 결과를 덮는다.

- **집필 프로 변환 완료(`applyProConversionResult`)는 재중앙하지 않고 픽셀 스크롤을 그대로 유지한다.** — `preserveReadingPosition`(재중앙)을 쓰면, 아래로 내려가며 한국어판을 수정하던 사용자를 수정 지점이 화면 중앙에 오도록 위로 튕겨 올렸다(2026-07-21 수정). 변환은 보고 있는 화면 위쪽 높이를 바꾸지 않으므로 스크롤을 건드릴 이유가 없다. → 재렌더 앞뒤로 `activeScroller().scrollTop` 만 보존(번역 패널은 `renderTranslationBlocks` 가 자체 보존). 여기서 다시 `preserveReadingPosition` 으로 되돌리지 말 것.

- **인라인 삽화 위젯은 파괴/재생성하지 않고 풀(`inlineWidgetPool`)에서 DOM 이동으로 재사용한다.** — 매번 전부 지우고 다시 만들면 `<img>` 재로드로 높이 출렁임이 발생. → `renderInlineIllustrations` 는 안 바뀐 노드 위젯은 그대로 옮기고 `variantSig` 가 실제로 바뀐 노드만 캐러셀을 다시 그린다. 풀 비우기는 full `render()`(root.empty) 때만.

- **삽화 표시면(인라인/출력 뷰)은 "어느 노드를 그릴지"를 같은 기준으로 판단한다 — 각자 다른 규칙을 쓰면 같은 삽화가 두 화면에 갈라진다.** — 출력 뷰는 `pathToLeaf` 로 "경로에 삽화 있는 마지막 노드"를, 인라인은 `computeIllustrationAnchors` 로 "본문에서 그 노드가 차지한 구간"을 봤다. **본문 끝을 지운 직후 삽화를 만들면** 대상 노드가 `activeLeafId` = 그 `delete` 편집 노드라, 차지한 글자가 없어 인라인은 영영 안 그리고 출력 뷰만 그렸다 — 사용자에겐 "첫 삽화는 본문에, 재생성한 건 출력창에" 갈라져 보였고 출력창을 닫아도 안 돌아왔다(파일에 그렇게 저장된 상태라. 2026-08-19 수정). → 세 겹: ①붙일 때 `resolveIllustrationTargetNode` 가 글자를 안 남기는 리프를 피한다 ②그릴 때 자기 글자가 애초에 없던 노드는 그 편집 지점에 앵커를 준다(과거 데이터 구제. 글자를 썼다가 지워진 노드는 기존대로 미표시 — 이 구분을 없애지 말 것) ③출력 뷰도 `computeIllustrationAnchors` 의 마지막 항목으로 고른다. **삽화 표시면을 새로 만들면 노드 선택을 직접 짜지 말고 이 앵커를 쓴다.**

- **세션 본문에서 파생되는 모달은 `shouldRestoreSelection=false`.** — 옵시디언 Modal 기본값이 닫힐 때 이전 포커스/선택을 복원하는데, 거대 contenteditable 재포커스 스크롤로 최상단 점프가 났다. → 새 파생 모달은 `modal-shell.ts` `createModalShell` 을 쓰거나(일괄로 끔) 생성자에서 직접 끈다.

- **캐러셀 카운터는 텍스트 노드 대신 `data-count` 속성 + CSS `content` 로 그린다.** — 인라인 위젯은 텍스트 0글자 원자 블록이어야 본문 diff/offset/caret 이 안 오염된다. 텍스트 노드로 되돌리면 원문 편집 diff 가 깨진다.

- **본문(contenteditable) 안에 꽂는 위젯에 글자를 담지 않는다 — 텍스트가 필요하면 `data-` 속성+CSS `content` 또는 폼 요소의 `value`.** — 세션창은 `bodyEl.textContent` 를 **그대로** 편집 diff 기준(`syncPendingDiff`)으로 쓴다. 위젯이 텍스트 노드를 하나라도 가지면 그 글자가 본문으로 저장되고 offset 매핑까지 어긋난다. 세션 노트(QR `/comment`) 위젯이 긴 본문을 담아야 해서 걸린 문제 — 제목은 `data-title`+CSS `content`, 본문은 **readonly `<textarea>` 의 `value`**(자식 텍스트 노드가 아니라 `textContent` 에 안 잡히면서 선택·복사는 된다). 삽입 기준점을 찾는 루프는 공용 `isInlineWidgetNode`(삽화/노트/AI 시작 마커)로 위젯을 건너뛴다 — 위젯 클래스를 개별 루프에 손으로 나열하지 말 것.

- **세션창 상단 정보 헤더(썸네일/시나리오명/세션명/★)를 다시 만들지 않는다.** — 의도적으로 제거됨. 세션명·시나리오명은 사이드바와 탭 제목이, 즐겨찾기는 사이드바(세션)/하단 툴바(노드)가 담당. 뷰어 옵션 버튼은 본문 위 우측 상단에만 쌓는다(모바일=뷰 헤더 액션 / PC=같은 모양 플로팅 가로 줄, 버튼 구성·순서 1:1 유지).

- **세션 호스트 판별은 `isSessionHostView` 하나만 통과시킨다 (타입 + `getSessionFile` 존재 검사).** — 옵시디언 1.7+ 지연 로딩 탭(DeferredView)은 `getViewType()` 은 세션 뷰로 보고하지만 실제 메서드가 없어 "getSessionFile is not a function" 이 났다. 뷰 타입 문자열로 직접 거르는 코드를 새로 만들지 않는다.

- **세션창과 파생 표시(번역 보기 등)는 직접 편집이 기본 모드.** — 읽기 전용 표시, 별도 편집 버튼/모달/편집 모드를 만들지 않는다. 본문을 고치면 즉시 노드/variant 로 저장.

- **집필 프로 끝 접합에서 저자가 남긴 끝 줄바꿈(빈 줄)을 trim 으로 버리지 않는다.** — `convertAndSplice` 가 `ko.trim()`/토큰 재조립으로 초고 끝 줄바꿈을 잘라 매니스크립트가 줄바꿈 없이 끝났고, 뒤이을 생성/집필이 마지막 문단에 줄바꿈 없이 들러붙었다(특히 `***` 뒤). → append 텍스트는 `op.ko.match(/\n+$/)` 로 저자의 끝 줄바꿈을 그대로 이어 붙인다(없고 끝이 장면 전환이면 `\n` 한 줄). 끝-접합 전용, replace(중간 수정)는 대상 아님.

- **집필 프로 한국어 보기에서 편집 대상이 아닌 문단(장면 전환 `***`)은 `contenteditable=false` 원자 위젯으로 렌더한다.** — `***` 을 일반 편집 문단으로 두니 커서가 그 끝에 들어가 타이핑이 구분선에 병합됐고(→ `***안녕`), user-edit 로 잡혀 재변환되며 장면 전환이 파괴됐다. 사용자는 "뒤에 입력이 안 된다"고 느낌. → 구분자 span 과 동급으로 `contenteditable=false`(`.ggai-tr-scenebreak`) — 커서가 못 들어가 뒤 클릭 시 다음 편집 지점(대기영역)으로 넘어간다. 선택-삭제는 range/offset 기반이라 영향 없음.

## 컨텍스트/생성

- **이스케이프 해제를 매크로 치환 **앞**에서 하지 않는다 (QR 카드 내보내기 오염).** — QR 파서가 `\|` 과 함께 `\{` `\}` 까지 풀어버려, 그 뒤 실행기의 매크로 치환이 `{{user}}` 를 진짜 페르소나 이름으로 바꿔버렸다. 카드 JSON 내보내기가 매크로 대신 이름이 박힌 파일을 만들어 다른 사람이 쓸 수 없는 카드가 된다(증상은 조용함 — 파일은 멀쉬하게 나온다). → 파싱은 `\|` 만 푸고(`unescapeQr` — 파이프 분리에 필요), 브레이스는 **치환을 끝낸 뒤** `unescapeQrBraces` 가 푸는다(실행기 `expand` 한 자리). 이스케이프는 "이 글자를 해석하지 말라"는 뜻이니, 해석기보다 먼저 풀면 지켜야 할 것을 지켜주지 못한다.

- **on/off 인자를 `true|1` 로만 받지 않는다 (QR 주입 영구화 사고).** — `/inject … ephemeral=on` 을 못 알아듣고 **지속 주입**으로 처리해, 버튼을 한 번 누르면 이후 모든 생성에 그 지시가 계속 따라붙어 세션이 오염됐다(실물 짱돌이 QR 이 `on` 을 쓴다). → 공용 `isQrFlagOn` 으로 받는다 — 값 없는 플래그도 켬, `off/false/0/no` 만 끔, 인자 자체가 없으면 ST 기본(지속). 새 불리언 인자를 받을 때 이 함수를 쓴다.

- **카드에 들어 있는 값을 "매크로로만 꺼낼 수 있게" 두지 않는다 (카드 지침 3종 미전송 사고).** — 캐릭터 카드의 `system_prompt` / `post_history_instructions` / `extensions.depth_prompt` 는 임포트 때 저장만 되고 `{{charPrompt}}` 같은 매크로를 프롬프트 세트에 직접 써야만 전송본에 들어갔다. 실리태번은 이 세 칸을 **기본으로** 꾂으므로(Prefer Char. Prompt/Instructions 기본 켬짐), 같은 카드를 같은 프리셋으로 돌려도 스텔라에서만 상태창/캐시트 지시가 통째로 빠져 모델이 그냥 롤플로 흘렀다(게임형 카드에서만 드러나 오래 몰랐다). 페르소나·요약은 이미 "매크로 있으면 그 자리, 없으면 기본 자리" 규칙이었는데 카드 지침만 예외였다. → 카드·페르소나·세션이 가진 내용을 새로 받을 때는 **기본 자리를 먼저 정하고**, 매크로는 그 자리를 옮기는 수단으로만 둔다. 곑가지 둘: 깊이 프롬프트는 ST 에서 `{prompt, depth, role}` **객체**라 그대로 매크로에 넣으면 `[object Object]` 가 전송된다(문자열 카드도 있으니 둘 다 받는다). 그리고 내용이 **대체되는 항목**에 쓴 매크로는 자리 지정으로 세면 안 된다 — 대체로 매크로가 사라지고 자동 삽입까지 막혀 내용이 통째로 증발한다(`macroPlacementTexts(..., replaced)`).

- **전송본 단일 진실 소스를 깨지 않는다 (대전제).** — "API 에 보낼 그 내용"은 오직 `build-session-context.ts` `planSessionRequest()` 한 곳. 실제 생성과 미리보기는 같은 payload 를 쓰고, 미리보기는 전송본과 **byte 단위로 같아야** 한다. 미리보기/생성 전 열린 세션창의 미저장 편집을 먼저 flush(`plugin.flushSessionEdits`). 전송 직전 가공을 새로 추가하면 미리보기 경로에도 동일하게 반영 — 한쪽에만 넣지 않는다.

- **확장을 만들다가 전송본 조립의 공통 기본값·세는 방식을 바꾸지 않는다 (이중 생성 작업 중 생성 전체가 달라진 사고).** — 이중 생성 작업 도중 Core 의 NAI 토큰 계산이 근사→실측으로 바뀌며 "입력 토큰 초과" 거부가 터졌고, 그 뒷수습이 확장과 무관한 공통 경로에 들어갔다: ① `Max Context` 미지정 시 예산을 16000 → **모델 한도 전부**로 ② 토큰을 문자열 대신 메시지 모양으로 세기 ③ 완성본을 재측정해 **최대 3회 재조립**(변수 in-place 갱신 때문에 스냅샷 되돌리기까지 딸려옴). 확장을 꺼도 모든 생성이 배포판과 다른 내용을 보내게 됐다. → 확장 작업의 변경은 **그 확장이 꺼지면 전송본이 byte 단위로 이전과 같아야** 한다(롤백 경계). 한도 초과 같은 공통 문제를 만나면 그 자리에서 고치지 말고 **별건으로 분리**한다. 예산은 `resolveTokenBudget` 한 곳: 기준은 사용자의 `Max Context`(없으면 16000), 다만 모델 프로필 한도에서 **출력 길이만큼 뺀 값**을 천장으로 클램프한다(입력으로 창을 가득 채우면 답이 안 나온다). 재측정 루프 같은 사후 보정 기계를 다시 만들지 말 것.

- **끝이 있는 형식을 요구한다 — 분량 문제를 토큰 상한으로 덮지 않는다.** — 이중 생성 1차의 출력이 계속 흘러넘쳤다: 여러 줄 `이름: 생각` → 수백 줄 대본, 한 줄 `이름: 생각 | …` → 한 줄 안에서 인물을 계속 돌며 2000자. 원인은 하나다 — **종결이 없는 임의 패턴**이라 컴플리션이 무한히 이어 붙일 수 있고, "각 인물 한 번씩"은 패턴상 근거가 없다. **JSON 일 때는 이 일이 없었다**: 닫는 기호가 있는 **학습된 형식**이라 모델이 `}`/`]` 로 닫고 끝낸다. → **JSON 의 종결성을 유지하되 구분자를 줄인다: 이름을 키로 쓰는 객체**(`{"이름":"생각", …}`). 항목당 `"":"",` ≈ 5토큰으로 배열-오브젝트(≈11토큰)의 절반 이하이고, **JSON 객체의 키는 유일하므로 인물 중복이 형식으로 막힌다**(지시문으로 부탁할 일이 아니다). **모델 프로필의 출력 상한을 덮어써서 자르는 건 해결이 아니다** — 문장 중간에서 끊길 뿐이고, 프로필 값은 그 프로필 소관이다.

- **텍스트 컴플리션은 지시로 멈추지 않는다 — 멈추는 건 예시가 만든 패턴과 stop 시퀀스뿐이다.** — "Output ONE line" / "then stop" 같은 문장을 아무리 넣어도 컴플리션 모델은 **직전 패턴을 잇는 기계**라 멈추지 않는다. 실제로 멈추게 하는 것은 둘: ①**앵커 접두어까지 포함한 완성 예시**를 두 번 보여 줘서 "이 패턴은 줄 끝에서 끝난다"를 학습시키는 것(`Private thoughts: …한 줄… \n` × 2) ②**stop 시퀀스**. 한 줄 형식이면 전송본이 `Private thoughts: ` 로 줄 중간에서 끝나므로 **첫 개행이 곧 끝** → 텍스트 경로에 `"\n"` 을 stop 으로 건다. **챗에는 걸지 않는다** — 답변이 개행으로 시작하면 첫 글자에서 잘려 빈 응답이 된다(코드펜스 stop 으로 겪은 사고와 같은 형태). 지시문은 챗 모델용 보조일 뿐, 컴플리션의 통제 수단으로 세지 않는다.

- **인물별 결과를 뽑는 프롬프트는 "여러 줄 목록"으로 쓰지 않는다 — 대본이 된다.** — 이중 생성 1차를 `이름: 생각` **줄마다** 형식으로 뽑게 했더니, 모델이 목록 대신 **수백 줄짜리 대화**를 쏟아냈다(`Yuri: —what?— / Hazama: —she saw him— …`, 생성이 토큰 제한까지 감). 줄 형식이 채팅 로그와 생김새가 같아 이름 셋을 쓰고 나면 주고받기가 자연스러운 이어쓰기가 되고, "각 인물 한 번씩"이라는 제약이 어디에도 없었다. 여는 `[` 는 오히려 여러 줄로 펼치라는 신호였다. → **`default-media-prompts.ts` 의 삽화 프롬프트를 본다**(그 프롬프트만 한 번도 안 샜다): ①출력이 **한 줄**(줄 끝이 곧 끝) ②`OUTPUT FORMAT — copy this shape exactly` + 모양 박기 ③규칙마다 GOOD/BAD ④**완성 예시**(`do NOT output this`) ⑤`NOW DO IT` 로 마지막에 제약 재확인 ⑥앵커가 형식의 첫 글자까지 포함. 형용사로 "짧게/한 줄씩"을 설명하는 것보다 이 여섯 개가 훨씬 강하다. 파서도 같은 규칙으로 방어한다(같은 이름 재등장 = 대화 시작 → 절단, `<think>…</think>` 선제거).

- **`{{char}}` 를 "캐릭터 이름"으로 여기고 프롬프트에 쓰지 않는다 — 스텔라에서 그건 시나리오(카드) 이름이다.** — 이중 생성 기본 세트의 견본 줄을 `{{char}}: <생각>` 으로 썼는데, 소설 세션에서 그 매크로는 **작품 제목**으로 풀려 제목을 발화자로 박은 견본이 섰다. ST 프리셋을 눈으로 훑어 베낀 결과다 — **여기는 캐릭터챗 프론트엔드가 아니라 스토리텔링 엔진**이고, 등장인물은 본문과 카드에서 읽어야 한다. → 인물 자리는 `<name>:` 같은 자리표시자로 둔다. 새 프롬프트/기본값을 쓸 때 매크로가 **이 코드베이스에서 무엇으로 풀리는지**(`context-builder.ts` `macroCtx`) 확인하고 쓴다.

- **부속 호출(확장 기능)에 세션의 생성 수치를 물려주지 않는다 — `paramsOverride` 를 아예 안 보낸다.** — 이중 생성 1차가 `planSessionRequest` 의 `paramsOverride` 를 통째로 넘겨 세션의 출력 제한·샘플링을 물려받았다. 그래서 ①본문 분량 설정(maxOutputTokens)에 1차 결과가 잘리고 ②"입력 한도는 세션 슬라이더를 안 따르는데 출력·온도는 따르는" 앞뒤 안 맞는 상태가 됐다. → **번역·요약·로어북 선별·문단 재생성이 이미 하는 대로** `{profileId, prompt|messages, label}` 만 보낸다. 그러면 출력 길이·샘플링·stop 이 전부 **그 모델 프로필**의 설정이 되어 본문 분량 설정과 자동으로 분리된다. `settingsOverride.params` 도 `undefined` 로 넘겨 입력 예산까지 그 프로필 기준으로 통일한다. 새 부속 호출을 만들 때 이 네 서비스 중 아무거나 먼저 열어본다.

- **stop 시퀀스는 경로별로 키가 다르다 — 한 키로 보내면 한쪽이 조용히 무시된다.** — Core 어댑터 실측: **챗**(anthropic/google/openai/vertex)은 `{...프로필 params, ...paramsOverride}` 로 병합한 뒤 **`stopSequences`** 를 읽어 provider 키(`stop_sequences`/`stopSequences`/`stop`)로 매핑한다. **텍스트**(novelai/openai)는 `paramsOverride` 를 **요청 본문에 그대로 펼치므로** OpenAI 형식 네이티브 키 **`stop`** 이어야 한다(NAI 텍스트도 Core 는 `/oa/v1/completions` 로 가고, Core 자신이 프로필 `stopSequences` 를 NAI 본문 `stop` 으로 매핑한다). NAI **네이티브** `/ai/generate` 의 stop 은 토큰 ID 배열이지만 Core 는 그 경로를 쓰지 않는다(우리가 토큰 ID 를 만들 수단도 없다 — Core 에 토크나이저 API 가 없고 `countTokens` 는 계수 근사 ±10%). → `payload.kind === "text" ? "stop" : "stopSequences"`. **수치를 안 물려주는 것과 stop 을 거는 것은 별개 문제다** — 한꺼번에 빼지 않는다.

- **모델을 갈아끼우는 오버라이드는 `naiFormat` 도 함께 재유도한다.** — 이중 생성 1차가 모델만 바꾸고 `naiFormat` 을 안 넘겨, 세션의 챗 기준 값(OFF)이 따라와 **텍스트 컴플리션 모델에 평문으로** 나갔다. → `settingsOverride` 에 `naiFormat: undefined` 를 함께 실어 `resolveNaiFormat(kind, undefined)` 가 그 모델 종류 기본값(텍스트=ON)으로 떨어지게 한다. 프리셋 순환(`presetToGenerationOverride`)이 이미 같은 이유로 그렇게 한다 — 새 오버라이드 경로를 만들 때 그 함수를 먼저 본다.

- **입력 토큰 예산을 모델 한도에 "딱 맞춰" 채우지 않는다 — 완성본을 Core 방식으로 재보고 넘치면 다시 조립한다 ("입력 토큰 초과" 거부 사고).** — 조립 예산을 Core 프로필의 입력 한도와 같게 잡았더니 생성이 계속 거부됐다(`입력 토큰(N)이 프로필 "X"의 최대 입력 토큰(M)을 초과했습니다`). 우리 계산이 Core 보다 **항상 작게** 나오기 때문이다: ① Core 는 입력을 잴 때 **메시지마다 래핑 오버헤드(+4)** 를 더하는데 우리는 내용 문자열만 셌다 → 메시지 수만큼 과소 계산 ② **조립이 끝난 뒤 붙는 것들**(세션 시작 마커·이어쓰기 이음새 보정 지시문·그룹 발화 지시·텍스트 경로의 이름 프리픽스/NAI 역할 토큰)은 예산에 안 잡힌다. 예산이 16000 고정이던 시절엔 여유가 커서 안 드러났다. → ①은 주입 카운터를 **메시지 모양**으로 재게 하고(`ai.countTokens([{role,content}], profileId)`), ②는 미리 알 수 없으므로 **완성본을 Core 의 거부 판정과 같은 모양으로 실측**(`measureInputTokens`)한 뒤 넘친 만큼 예산을 깎아 재조립한다(최대 3회, `assemble(budget)` 클로저). **재조립할 때 `variables` 를 원본 스냅샷에서 다시 시작한다** — `buildContext` 가 in-place 로 갱신하므로 안 그러면 같은 `addvar` 가 두 번 적용된다. 전송 직전 가공을 새로 추가할 때 이 실측 경로가 그대로 살아 있는지 확인할 것.

- **본문이 아닌 부산물 생성에 본문의 출력 제한(maxTokens)을 물려주지 않는다.** — 출력 제한은 "본문을 한 번에 이만큼만 써라"는 **분량 설정**이다. QR `/gen` 이 `planSessionRequest` 의 `paramsOverride` 를 통째로 넘겨, 7~11 섹션짜리 외전이 본문 분량에 맞춰 중간에 잘렸다. → 부산물 경로는 `util/generation-params.ts` `withoutOutputCap(...)` 로 `maxTokens`/`max_tokens` 만 떼고 샘플링은 유지한다(키를 빼면 Core 가 그 프로필 설정값으로 떨어짐). 판단 기준은 **용도**: 본문/선채팅처럼 세션 본문을 이어 쓰는 것은 그대로 쓰고, 번역·삽화·요약·문단 재생성·`/gen` 처럼 본문이 아닌 결과물은 물려받지 않는다.

- **커맨드의 맨 인자가 비어 있으면 "값이 없다"가 아니라 "앞 결과를 쓰라"는 뜻이다 (QR 결과 증발 사고).** — ST 슬래시커맨드는 맨 인자를 **안 적으면** 파이프 값이 그 자리에 온다. 우리 실행기는 그걸 몰라 `/gen … | /sendas name="{{char}}"` 를 "본문 빈 문자열"로 보고 **조용히 통과**시켰다. 결과가 세션에도 화면에도 안 남아 가십지·자동사냥·마법 신문·비밀 일기장·극대본 작성이 전부 "눌러도 아무 일 없음"이 됐다(오류도 없어 원인 추적이 어려운 종류). → `runCommand` 의 `body()` 한 곳이 규칙을 소유한다: **원문이 비어 있으면** `state.pipe`, 적혀 있는데 매크로가 빈 값으로 풀렸으면 그대로 빈 값("안 적음"과 "비어 있음"을 뭉개지 않는다 — `/setvar` 의 값 인자 판정이 여기 의존). 인자가 내용이 아니라 **대상 지정**인 커맨드(`/hide` `/trigger` `/flushvar` `/flushinject` `/getglobalvar`)만 `rawBody()` 로 파이프를 막는다 — 안 그러면 `/gen … | /hide` 가 엉뚱한 메시지를 숨긴다. **새 커맨드를 추가할 때 이 둘 중 어느 쪽인지 먼저 정한다.**

- **`/sendas name=` 의 이름을 해석 못 했다고 버리지 않는다 (남의 발화가 캐릭터 말풍선으로 뜬 사고).** — 그룹 멤버와 이름이 안 맞으면 `speaker` 를 안 붙여 호스트 발화로 떨어졌고, "마법 신문"이 캐릭터 본인의 말풍선으로 보였다. → 멤버가 아니면 **익명 발화자**로 남긴다(`node-meta.json` 의 `speakerName`). 이때 **본문에 `이름:` 접두어를 박지 않는다** — 본문은 문자 오프셋 기준이라 접두어 한 줄이 편집 패치·번역 문단 키·삽화 앵커를 전부 밀어낸다. 이름은 표시 계층(챗 = 이름 라벨+이니셜 아바타, 소설 = 글자 0개 이름표 위젯)에서만 그린다.

- **"AI 에게 안 보낸다"를 본문 밖으로 빼서 구현하지 않는다 (`/comment` 호환 파손).** — ST 는 결과를 **항상 대화 로그에 남기고** 프롬프트에서만 뺀다. 우리는 `/comment` 를 본문 밖 노트로 저장해 "AI 엔 안 감"을 공짜로 얻었지만, QR 을 프롬프트 숏컷으로 쓰는 카드가 기대하는 자리(대화 로그)에 결과가 없어 다음 커맨드가 물리지 않았다. → 결과는 본문 노드로 남기고 **전송본 조립에서만 뺀다**(`node-meta.json` 의 `hidden` → `planSessionRequest` 한 곳: 챗은 nodeId 필터, 소설은 `spansExcludingNodes`). 표시는 클래스만 얹어 흐리게 — **본문 textContent 를 바꾸면 편집 diff/offset 이 깨진다.** 챗 로그의 꼬리 제외(`excludeTailAssistant`)는 **숨김 필터보다 먼저** 한다(갈아끼울 메시지가 숨김이면 이미 없어서, 나중에 pop 하면 멀쩡한 앞 메시지가 잘린다).
- **본문을 AI 에게 넘기는 코드가 각자 `buildSpans` 를 부르면 숨김·삭제가 새 기능마다 다시 샌다.** — "AI에게 숨김"을 `planSessionRequest` 한 곳에서만 걸렀더니 **이어쓰기에서만** 통했다. 요약·로어북 자동 생성·세션 제목·반복 표현·집필 문체 꼬리·스텔라 폰이 저마다 원문을 다시 읽어, 눈감긴 QR 결과가 그 경로로 AI 에게 갔다(요약에 숨긴 내용이 남는다는 제보). 같은 자리에 두 번째 사고가 겹쳐 있었다 — 요약·로어북은 `buildSpans(session, 경계노드)` 로 **그 시점 본문**을 되감아 재료를 만들어서, 나중에 지운 대목이 요약을 전부 지우고 다시 만들어도 계속 되살아났다. → 창구는 `util/ai-body-text.ts` 하나(`sendableText`/`sendablePassage`/`hiddenNodesOf`)이고, 확장은 `ExtensionContextInput` 의 `bodyText`/`hiddenNodeIds` 를 받아 쓰며 본문을 재구성하지 않는다. 구간 재료는 `textBetweenNodes` 로 **지금 본문**에서 자른다(`genNodeId` 귀속이라 나중 편집이 제자리에 반영된다). 하네스가 `src/services/**`·`src/extensions/**` 의 직접 호출을 막고, 표시·오프셋 예외만 `// body-raw: 이유` 로 남긴다. **"확장마다 따로 고치기"로 되돌아가지 말 것** — 그러면 확장을 하나 추가할 때마다 같은 제보가 다시 온다.

- **프리셋 랜덤 순환은 전송 1회용 오버라이드만.** — 과거 applyPreset silent 적용이 활성 설정·우측 UI 까지 바꾸는 회귀가 반복됐다. → 생성 직전 `pickRotationPreset()` → `presetToGenerationOverride`(모델/파라미터/프롬프트 세트/이어쓰기 보정만, 미디어 제외) → `planSessionRequest` 의 `settingsOverride` 로만 얹는다. 활성 설정(세션 meta/PluginData.current)과 UI 는 절대 불변. 사용자가 직접 프리셋/모델을 고르면(`notePresetRotationManualChoice`) 다음 1회는 순환 스킵.

- **기본 프롬프트 세트에 `chatSummary` 마커를 넣지 않는다.** — ST 에 없는 Stella 전용 항목이라 요약 기능에 귀속. 요약 주입은 `planSessionRequest` 게이트(켜져 있을 때만, 명시적 마커/`{{summary}}` 있으면 그 위치 존중·자동 삽입 안 함 — 페르소나와 같은 규칙).

- **Stella 전용 prompt marker 를 만들지 않는다. 특히 `personaDescription` marker 금지.** — 페르소나는 `{{persona}}` 매크로(명시 위치) 또는 기본 위치(캐릭터 설명 앞) 자동 삽입.

- **폰 공유 다이제스트(v2 §8.1)를 세션 전송본 경로에 넣지 않는다.** — `PhoneService.buildSharedDigest` 는 폰 **내부 생성**(문자/SNS/방송 답장)에만 붙는다. 세션 `phone` 컨텍스트 슬롯(planSessionRequest 경유, phone-extension `contributeContext`)에는 절대 넣지 않는다 — 다이제스트는 시간순 정렬·in-memory 방송 상태를 쓰므로 미리보기·생성 byte 동일 대전제를 깬다. 세션 주입에 들어가는 SNS "봤는지"(§8.2 `sawSnsPost`)는 반드시 **결정적**(scenarioId 기준 `hashRatio`, Math.random 금지)이라 byte 동일이 유지된다. 두 경로를 헷갈려 다이제스트를 슬롯에 흘리지 말 것.

- **"문단" 정의는 앱 공통 `tokenizeParagraphs`(연속 줄바꿈 `\n+` = 구분자 하나, 그 사이 내용 블록 = 문단) 하나만 쓴다.** — 작가노트를 "끝에서 4번째 문단" 앞에 넣는 `context-builder.ts` `storyToMessages` 가 예전엔 빈 줄(`\n\s*\n`) 기준 자체 splitParagraphs 로 문단을 셌다. 소설 본문은 줄바꿈 하나로 문단을 나누므로 본문 전체가 한 문단으로 뭉쳐 작가노트가 맨 위에 붙는 버그(2026-07-21 수정). 엔터를 한 번 띄우든 두 번 띄우든(사용자·모델마다 다름) **내용 있는 블록만** 문단으로 세야 한다(공백만 있는 블록 제외). 번역/문단재생성이 쓰는 `translate-paragraphs.ts` `tokenizeParagraphs` 를 재사용하고, 원문 줄바꿈은 토큰째 보존해 before+after 가 본문과 byte 동일하게 자른다.

- **이어쓰기 이음새 보정에서 장면 구분선(`***`)을 미완성 문장으로 취급하지 않는다.** — `anchorEndsParagraph`(`continuation-anchor.ts`)가 앵커 끝 글자만 보고 문장부호가 아니면 "문장 중간"으로 판정했다. 본문이 `***`로 끝나면 앵커가 `…문장.\n\n***`가 되는데, 끝 글자 `*`가 종결부호가 아니라 `collapseSeam=true`가 되어 (1)이어쓴 응답 앞머리 줄바꿈을 다 지우고 (2)본문 꼬리 공백 delete 패치까지 걸었다 → `***`가 다음 문단과 뭉개지고 빈 줄이 사라짐(2026-07-23 수정). `***`는 오히려 문단이 확실히 끝나는 자리다. → 앵커의 마지막 줄이 장면 구분선(`/^\*{3,}$/`, 공백 무시)이면 `anchorEndsParagraph`가 true 를 돌려 이음새 줄바꿈을 보존한다. 이 가공은 저장·전송 단계라 AI 출력 정규식으로는 못 되돌린다. 구분선 정의는 `util/pro-convert.ts` `isSceneBreakParagraph` 와 같게 유지. **같은 사고의 짝: 본문이 `***`로 끝나면 앵커 자체를 만들지 않는다**(`extractAnchorSentence` → null, 2026-08-02 수정) — 앵커 지시문은 "이 문장을 그대로 받아쓰고 이어서 계속 써라 / 진행 중인 문단이니 새 문단의 시작으로 취급하지 마라"라서, 구분선 앞 문장이 앵커가 되면 모델이 장면 전환을 무시하고 앞 장면을 계속 썼다(사용자 지적: "절대로 다음 장면을 안 쓴다"). 장면 구분선 뒤는 이을 이음새가 없는 자리다.

- **이어쓰기 중복 제거를 앵커 하나에만 걸지 않는다 — 모델은 앵커보다 앞에서부터 본문을 되받아쓴다.** — 앱커 매칭(`findAnchorEnd`/`fuzzyPrefixSkip`/`fallbackSkip`)은 응답 맨 앞에서 **앵커 그 문장**을 찾는다. 본문이 문장 중간에서 끊겨 앵커가 한 단어 조각(예: `There`)이면 모델은 그 조각만 받아쓰지 않고 **그 조각이 속한 대사·문단을 통째로 다시 쓴 뒤** 이어가서, 세 단계 매칭이 전부 실패하고 문단이 통째로 중복됐다(사용자 지적: "이젠 아예 앞 문장 덮어쓰지도 않는데?", 2026-08-19 수정). → 앵커가 아니라 **본문 꼬리 자체**와 응답 앞머리를 맞춰 보는 `tailOverlapSkip(parentText, raw)` 를 `anchorSkipFinal`/`anchorSkipStreaming` 에 추가했다(둘 다 `parentText` 선택 인자). 가장 긴 겹침을 고르고, 서식·문장부호는 양쪽에서 건너뛰며 실질 글자만 정규화 비교(퍼지 매칭과 같은 규칙), 우연 일치 방지로 최소 12자(`OVERLAP_MIN_HARD`) 이상일 때만 자른다. 스트리밍 중에는 되받아쓰기가 진행 중이면(`tailOverlapPending`) 표시를 보류해 중복이 화면에 비치지 않게 한다. 앵커 추출 규칙(`ANCHOR_MIN_CHARS`)을 건드려 해결하려 하지 말 것 — 생성 결과는 지시대로 안 온다는 게 전제다.

- **이어쓰기 이음새를 정리할 때 걷어내는 것은 줄바꿈이지 띄어쓰기가 아니다.** — `finalizeAnchorStrip`(`session-view.ts`)이 문장 중간 이음새(`collapseSeam=true`)에서 이어쓴 응답의 선행 공백을 `replace(/^\s+/, "")`로 통째로 지우고 본문 꼬리 공백까지 delete 패치로 걷어냈다 → 한국어·영어처럼 띄어쓰기를 쓰는 글에서 "천천히문을"처럼 앞뒤 단어가 들러붙었다(사용자 지적: "이걸 방지하려고 이음새 보정을 쓰는 건데", 2026-08-18 수정). 줄바꿈만 문제이고 공백 한 칸은 문장을 잇는 데 필요하다. → `continuation-anchor.ts` `seamSeparator(before, after)`(순수 함수)로 판정: 이음새에 공백이 하나라도 있었고 양쪽 인접 글자 중 하나라도 띄어쓰기를 쓰는 문자(라틴/키릴/한글/숫자)면 공백 한 칸을 남기고, 일본어·중국어(단어 사이 공백 없음)나 단어 중간에서 끊긴 이음새(양쪽 다 공백 없음)면 그대로 붙인다. 이 가공은 저장 단계라 나중에 되돌릴 수 없으니, 이음새 공백 규칙을 손댈 땐 두 언어군을 함께 확인한다.

- **이어쓰기 중복 제거(앵커 반복 절단)는 모델이 앵커를 글자 그대로 재현할 거라 가정하지 않는다.** — 중복 제거는 "모델이 다시 옮겨 쓴 앞부분을 잘라내는" 방식인데, `findAnchorEnd`(엄격 매칭)는 앵커를 **바이트 그대로** 재현해야만 잘랐다. 모델이 앵커 끝 `***`를 빼먹거나(장면 전환 없이 같은 장면 이어씀), 여는 따옴표(「")를 누락하거나, 곧은/둥근 따옴표·말줄임표(…↔...)·전각/반각을 바꿔 재현하면 매칭 실패 → **앞문장이 안 잘리고 그대로 중복**됐다(2026-07-23 수정). → `anchorSkipFinal`/`anchorSkipStreaming` 에 정규화 퍼지 접두 매칭(`fuzzyPrefixSkip`/`matchAnchorFuzzy`)을 엄격 매칭 실패 뒤 단계로 추가: 서식·문장부호(`isSoft`=LEAD_SKIPPABLE+TERMINATORS)는 양쪽에서 자유롭게 건너뛰고, 실질 글자만 전각→반각 정규화(`normHard`)해 비교. **앵커 실질 전체가 재현됐을 때만**(최소 `FUZZY_MIN_HARD`=3자) 자르고, 실질 글자가 도중에 어긋나면 안 자른다(정상 이어쓰기 오삭제 방지). 앵커가 소프트로 끝났을 때만 재현된 꼬리 문장부호를 걷어내되 **공백은 분리자로 남긴다**(앵커가 실질 글자로 끝나면 뒤 `」`는 이어쓰기이므로 안 건드림). 매칭을 더 헐겁게 할 땐 반드시 "정상 이어쓰기를 중복으로 오인해 지우는" 반대 사고를 함께 막는다.

- **자동 생성물이 자기 출력을 다시 재료로 먹는 루프를 만들지 않는다.** — SNS 갱신이 매번 "같은 세션 장면 + 직전 피드"를 재료로 "새 글 최소 N개"를 요구하니, 세션에 진행이 없으면 모델이 사건을 발명하고 다음 배치가 그 발명을 또 확장했다. 결과적으로 **SNS 가 세션보다 스토리를 앞질러 갔다**(2026-07-26 수정). → 소비 지점을 기록하는 **북마크**(`PluginData.snsProgress`)를 두고 "북마크 이후 새 진행분"만 사건 재료로 쓴다. 이전 구간은 지우지 말고 **배경(BACKGROUND ONLY)** 으로 따로 붙여 모순 방지 용도로만 남긴다 — 컨텍스트를 빼면 캐릭터가 자기 상황과 어긋나는 글을 쓴다. 진행분이 없으면 **잡담 모드**(새 플롯 사건 발명 금지)로 강등하되 생성 자체를 멈추지는 않는다. 북마크 전진은 반드시 **저장 성공 후**.

- **자동 생성 피드를 "세션 사건 반응"으로만 정의하지 않는다.** — 사건 반응만 시키면 NPC 들이 남의 세션을 전지한 관전자처럼 논평하는 "아침 드라마 시청자" 글이 나온다(사용자 지적). 세션 첨부의 목적은 **모순 방지와 개연성**이지 관전평 생산이 아니다. → 기본은 **화면 밖 일상**(플레이하지 않는 시간의 삶)이 다수(`snsDailyRatio` 기본 70%)이고 사건은 소수. 경계는 "**본편 줄거리를 진전·해결·확대하면 금지, 그 인물의 사생활·일상은 자유**" — 일상까지 막으면 리얼함이 죽는다.

- **상한을 두면 초과분에 "익명 폴백"을 두지 않는다.** — SNS 신규 계정 상한(배치당 3명)을 넘긴 작성자를 "등록 없는 익명 작성자"로 통과시켰더니 상한이 아무 의미가 없어 엑스트라가 무제한으로 판쳤다(2026-07-26 수정). 상한을 넘긴 활동은 **폐기**한다. 같은 사고의 짝: 명부(캐스트)를 프롬프트에 **명시하지 않으면** 모델은 이름을 스스로 찾아야 하고, 못 찾으면 발명한다 — "재사용하라"는 지시만으로는 재등장률이 오르지 않는다. 비율 규칙도 지시만으로는 안 지켜지므로 파서/엔진이 잘라내는 강제(`extraQuota`)를 함께 둔다.

- **"서사가 진행됐다"를 AI 생성으로만 판정하지 않는다.** — 폰 키워드 트리거와 방송 자동 감지가 `onGenerationComplete`(AI 생성) 훅에만 붙어 있어서, 사용자가 직접 "방송을 켰다"라고 쓴 전개를 통째로 놓쳤다(2026-07-26 수정). 챗 전송·소설 유저 입력도 서사 진행이다. → 확장 API 에 `onUserText` 훅을 추가하고 두 세션 뷰의 **확정된 사용자 입력 지점**에서 발화한다(챗 = 전송 1회, 소설 = 타이핑 커밋을 모아 **이어쓰기 직전 1회** — 커밋마다 부르면 AI 호출이 낭비된다). 새 자동 판정 기능을 붙일 때 두 훅 모두에 붙였는지 확인한다.

- **분기 상태를 갖는 부가 기능(방송 등)은 활성 경로에서 그 시작점이 사라진 경우를 처리한다.** — 스텔라튜브 방송은 `startedNodeId` 이후 노드에 반응을 매다는데, 되감기·분기 전환으로 시작점이 활성 경로에서 빠져도 LIVE 표시가 그대로여서 "방송 켜기 전 지점인데 생중계 중"인 모순이 났다(2026-07-26 수정). → 표시(`tubeOnActivePath`)와 생성(`generateTubeReaction` 초입 가드) 양쪽에서 경로 포함 여부를 확인하고, **파일의 live 는 끄지 않는다**(끄면 앞으로 되돌아갔을 때 사용자가 다시 켜야 한다 — 되돌릴 수 있는 상태를 파괴하지 않는다).

- **키워드 매칭만으로 서사에 개입하는 기능(방송 자동 시작 등)을 켜지 않는다.** — 폰 방송 자동 감지 초판이 생성문에 "방송/스트리밍/配信" 글자가 **포함되기만 하면** 즉시 방송을 시작하고 스트리머를 무조건 로그인 페르소나로 잡았다. "어제 방송 봤어?" 같은 언급, 남의 방송 시청, 과거 회상에도 켜졌고 서사 속 실제 방송자를 무시했다(2026-07-26 수정). → 키워드는 **판정 요청 신호**로만 쓰고, 실제 개입 여부는 모델이 장면을 읽고 판정한다(`tryAutoStartStream` + `phoneStreamDetect` 프롬프트). 판정 결과의 인물은 **장면 인물 명부**(`collectSceneCast`: 호스트 시나리오 + 그룹 멤버 + 장면에 이름이 실제 등장하는 계정, 로그인 페르소나 제외)로 거르고, 매칭 실패면 **아무 것도 하지 않는다** — 자동 경로에 "일단 페르소나로" 같은 폴백을 두지 않는다(오작동이 사용자 스토리를 납치한다). 판정 호출에는 busy 가드 + 분 단위 스로틀을 함께 건다.

- **취소(`cancelled`)를 "사용자가 누른 것"으로 단정해 조용히 넘기지 않는다.** — Core 는 자기 **요청 시간 초과**도 취소와 똑같은 `code:"cancelled"` 로 돌려준다(`generation-service.ts` 의 `setTimeout(() => ctrl.abort())` → `normalizeError`). 그래서 "취소면 토스트 생략"이 곳곳에 있었고, 그 결과 집필 변환(`performProConvert`)·챗 양방향 변환·자동 번역이 시간 초과로 죽으면 **이어쓰기/전송이 통째로 멈추는데 화면에도 AI 로그에도 아무 흔적이 없었다**(2026-07-31 수정). 판별 기준은 간단하다: **우리가 그 호출에 `signal` 을 넘겼는가.** 안 넘긴 경로(번역·집필 변환·로어북 선별·요약 등)의 `cancelled` 는 사용자 취소일 수 없으므로 반드시 알린다. 사용자가 켜 둔 자동 실행(자동 번역/삽화)의 실패도 `console.warn` 으로만 남기지 않는다 — 콘솔은 사용자가 보지 않는다.

- **생성 재진입 잠금은 "전송본이 확정된 뒤"가 아니라 "준비 시작"에 건다.** — `this.generation` 은 payload 가 확정된 다음에야 세팅되는데, 그 앞의 준비 단계(집필 변환·자동 실행 QR·로어북 AI 선별·컨텍스트 조립)만도 AI 호출을 여러 번 하며 수십 초가 걸린다. 그동안 이어쓰기 버튼은 멀쩡한 재생 아이콘이라 한 번 더 눌리고, 두 번째 흐름이 같은 뷰에서 나란히 돌며 `this.generation` 을 덮어써 서로의 스트림을 끊었다(2026-07-31 수정). → `preparing` 플래그를 준비 시작에 세우고 `generation` 을 세팅하는 순간 넘겨준다(자동 번역/삽화가 도는 뒷정리 구간까지 버튼을 묶지 않기 위함). 생성 경로에 **AI 호출을 하는 준비 단계를 새로 추가할 때는 잠금 구간 안인지 확인**한다.

- **버튼 핸들러의 `() => void this.handleX()` 는 실패를 삼킨다.** — 요청을 보내기 전 단계에서 예외가 나면 사용자에게는 "눌렀는데 아무 일도 없고 로그에도 요청이 없는" 상태로만 보인다(원인 추적 불가). 생성 경로의 진입점(`handleContinue`/`handleRegen`)은 `views/report-failure.ts` `reportSessionFailure` 로 실패를 반드시 화면에 남긴다. 새 생성 진입점을 만들면 같은 처리를 붙인다.

- **AI 보조 기능의 프롬프트·동작 수치를 하드코딩하지 않는다.** — 로어북 AI 선별 초판이 지시문과 본문 첨부량(4000자)을 코드에 박았다가 교정됨(2026-07-17). 사용자는 "내가 모르는 데서 어떻게 돌아가는지 모르게 돼 있는 것"을 명시적으로 거부한다. 새 AI 기능의 지시문은 편집 가능한 미디어 프롬프트(`default-media-prompts.ts` 버킷 + `renderMediaPromptPicker`)로, 첨부량/주기 같은 수치는 설정 UI 로 노출한다. 숨은 추가 지시문(엔진이 프롬프트 앞뒤에 몰래 붙이는 텍스트)도 두지 않는다 — 편집 화면에 보이는 전문이 그대로 전송돼야 한다. 기본 프롬프트는 텍스트컴플리션 모델 기준으로 짠다(다른 기본 프롬프트처럼 끝에 "Selection:" 류 응답 오프너, 재료는 `{{main}}`/`{{lorebook}}` 위치 매크로).

## 번역

- **번역 보기의 편집 기능을 양방향 번역(집필 프로) 전용으로 두지 않는다.** — "문단 통째 선택 → 삭제 → 원문에서도 삭제"(`performTranslationStructuralDelete`)가 `bidirectionalWriting()` 게이트 뒤에 있었다. 양방향이 꺼져 있으면 삭제 키가 브라우저 기본 동작으로 처리돼 DOM 에서만 사라지고, `commitTranslationEditSync` 는 **떨어져 나간 블록(`!contains`)과 빈 값(정규화 아티팩트 방어)** 을 둘 다 무시하므로 **아무 데도 저장되지 않는다** → 다음 재렌더에 지운 문단이 되살아난다(2026-07-31 수정, 편집 조용한 유실). 번역 보기는 "직접 편집이 기본 모드"이므로 **편집 동작 자체는 토글과 무관**해야 한다 — 양방향 전용인 것은 대기함(`pendingReflections`) 기록과 끝의 초고 영역뿐이다. 번역 보기에 새 편집 동작을 붙일 때 게이트를 어디에 거는지 확인한다.

- **contenteditable 의 삭제를 keydown 만으로 잡지 않는다.** — 모바일 소프트키보드는 Delete/Backspace keydown 을 안 보내거나 키를 식별할 수 없고, 잘라내기·컨텍스트 메뉴 삭제도 keydown 이 없다. 같은 판정을 `beforeinput`(`inputType.startsWith("delete")`)으로도 받는다. keydown 이 이미 `preventDefault` 했으면 beforeinput 은 오지 않으므로 중복 처리는 없다.

- **문단 키에서 "앞 문단"을 빼지 않는다 — 내용만으로 키를 잡으면 반복 대사의 번역이 통째로 공유된다.** — 키가 내용 해시뿐이던 시절, 챗에서 반복되는 짧은 대사(`"No."` `"응."`)가 전부 한 항목으로 합쳐졌다. 맨 처음 번역된 하나가 그 뒤 모든 위치에 그대로 재사용돼 **"남은 거 있어?"의 답 "없습니다"가 "안 와?"의 답 자리에도 나왔고**, 그 자리는 AI 를 다시 부르지도 않아 앞 문맥을 붙이든 프롬프트를 고치든 안 고쳐졌다(2026-07-29 사용자 제보 — "번역 질이 떨어졌다"로 접수됐지만 실은 원래 있던 구조 문제, 병렬화로 노출만 늘었다). → 키는 `paragraphKey(내용, 앞 문단)`. 되돌리기 스택·양방향 대기함도 같은 키를 쓴다.

- **문단 키를 쓰는 곳은 "어느 범위를 토큰화했는지"를 반드시 맞춘다 (조용한 실패).** — 키가 앞 문단에 의존하므로 본문 전체를 토큰화한 키와 조각(말풍선/구간/노드)만 토큰화한 키가 **다르다**. 어긋나면 번역이 다 되고 저장까지 됐는데 **화면에만 안 나온다** — 오류도 로그도 없어 원인 추적이 가장 어려운 종류다. 챗은 표시/편집/대기함/내보내기가 전부 말풍선 단위라 특히 위험. → 조각을 토큰화하는 호출자는 `tokenizeParagraphs(조각, lastParagraphSource(앞부분))` 로 앞 문단을 넘긴다. 새 표시 경로를 추가하면 `tests/translate-key-scope.test.ts` 에 등가 검사를 함께 넣는다(전체 기준 키 == 조각 기준 키).

- **앞 문맥 참고 블록에 번역 없는 문단을 담지 않는다.** — 청크를 동시에 띄우면 바로 앞 문단이 아직 번역 중이라 짝이 비는데, 그대로 담으면 **"이미 번역된 직전 문단 — 절대 번역하지 마라"** 는 머리말을 달고 원문만 6줄 나갔다. 지시와 자료가 모순이고 번역 대상 JSON 바로 옆에 같은 언어 원문 덩어리가 붙어, 짧은 문장일수록(챗·폰) 대상이 흐려졌다(2026-07-29). → `collectTranslationContext` 는 active 번역이 있는 문단만 담고, 담을 게 없으면 블록을 안 붙인다. "참고자료가 없는 것"보다 "못 믿을 참고자료"가 나쁘다.

- **폰 번역 항목 id 는 짧은 합성 id(`m0`/`p0`/`c0`…)를 쓴다 — 긴 uuid 를 번역 id 로 넘기지 않는다.** — `translateItems` 는 항목을 id 로 매칭하는데(모델이 응답에 id 를 되돌려줌), 채팅 항목의 긴 uuid 를 그대로 번역 id 로 넘겼더니 모델이 정확히 재현하지 못해 매칭이 전부 실패 → **스텔라튜브 채팅 번역이 통째로 안 됐다**(2026-07-22 사고). → `runChatTranslation` 이 `c${i}` 짧은 id 로 요청하고 결과를 채팅 id 로 되돌린다(문자 `m${i}`·SNS `p${i}` 와 같은 규칙). 새 번역 대상을 추가할 때 반드시 짧은 순번 id 를 쓴다.

- **`translateItems` 는 청크 하나가 실패해도 나머지 청크를 계속 번역한다(부분 성공 최대화).** — 한 청크가 형식 오류/호출 실패면 예전엔 그 자리에서 `ok:false` 로 **뒤 청크를 통째로 버려** "번역 보기 눌러도 뒷부분이 다 원문"이 됐다. → 실패 청크는 건너뛰고 계속, 하나라도 성공하면 `ok:true`(부분 성공 보존), 전부 실패해야 `ok:false`. 취소(`isCancelledError`)만 즉시 중단. 못 채운 항목은 "번역 보기" 재토글(미번역만 다시 시도)이나 개별 재번역으로 메운다. **폰 전용 경로**(세션 `translateParagraphs` 는 이 함수와 무관).

- **생성 시점 1회 시도로 끝나는 자동 처리는 "표시 시점 보충" 경로를 반드시 함께 둔다 (폰 SNS·방송 일부만 번역 사고).** — 폰 자동 번역은 문자/SNS/방송이 **만들어지는 그 순간에만** 돌아서, ①설정을 켜기 전에 쌓인 글 ②청크 실패로 빠진 항목 ③라이브 중 인라인 번역이 놓친 채팅이 영구히 원문으로 남았다. 문서상 복구 수단이던 "번역 보기 재토글"은 **자동 번역이 켜져 있으면 이미 번역 보기 상태라 사용자가 누를 일이 없어** 무용지물이었다(2026-07-26 사고). 방송에 있던 "열 때 자동 채우기"도 `방송당 1회` 절대 가드라 라이브 추가분을 못 채웠다. → `phone-view` 의 `shouldFillTranslations(key, pendingIds)` 게이트: 렌더 시 미번역 항목이 있으면 채우되, **직전 시도와 미번역 집합 지문이 같으면**(= 그 시도로 아무것도 못 채움) 재시도하지 않는다. 절대 1회 가드(진전이 있어도 다시 안 함)도, 무조건 재시도(실패 루프)도 아니다. 수동 토글은 지문을 기록한 뒤 강제 실행해 뒤따르는 렌더가 같은 요청을 또 쏘지 않게 한다. **원문이 바뀌면 옛 번역을 지워** 보충 대상이 되게 한다(라이브 방송 게시글 `syncLiveStreams`).

- **번역 계획(`translationBlocks`)을 본문보다 낡은 채로 쓰지 않는다 — 낡은 계획 하나가 이어쓰기를 영구히 막았다 (2026-08-17 사용자 제보).** — 계획은 그릴 때의 `baselineText` 스냅샷인데, **번역 패널이 숨어 있는 동안(원문 보기)에는 아무도 다시 그리지 않는다.** 원문 보기로 돌아가 이어 쓰고 고치는 동안 계획은 계속 낡고, 그 계획을 소비하는 두 경로가 함께 터졌다: ① 위젯 배치(`placeInlineInTranslation`/`placeNotesInTranslation`/`placeSpeakerLabelsInTranslation`/`renderAiStartMarkerTranslation`)가 `editEl.insertBefore(el, block.el)` 의 기준으로 낡은 span 을 넘겨 **NotFoundError**(`Failed to execute 'insertBefore' … not a child of this node`) — 이 예외가 `renderBodySpans` → `commitPending`/`rebuildBaselineAndRender` 를 타고 올라가 `handleContinue` 의 catch 에 잡혀 **"이어쓰기를 시작하지 못했습니다" 토스트만 반복되고 생성이 아예 안 됐다**(계획을 고칠 렌더도 그 예외로 못 돌아 스스로 풀리지 않는 영구 고장). ② `collectProOps` 가 낡은 `offset`/`source` 로 op 를 만들어 `convertAndSplice` 의 `expect` 검증에 매번 걸렸다("그 사이 본문이 바뀌어 반영을 취소했습니다" — 대기함은 성공해야 비므로 이것도 영구 고장). → 계획 유효성은 `translationPlanMatches()`(문단 해시/원문/offset 일치 **+ 각 블록 el 이 편집면의 직계 자식**) 한 곳에서 판정하고, 계획을 소비하기 전에 `ensureTranslationPlan()`(어긋났으면 숨어 있어도 패널 재구성, 조합/생성 중이면 보류)을 부른다. 삽입 기준 노드는 `translationInsertRef()` 로만 얻는다 — **`contains` 로 검사하면 안 된다**(브라우저가 span 을 다른 span 안으로 옮기면 contains 는 참인데 `insertBefore` 는 던진다). 번역 패널의 계획을 읽는 새 경로를 만들면 이 두 함수를 먼저 태운다.

- **막힌 상태에는 사용자가 스스로 풀 수단이 있어야 한다 (반영 대기 취소).** — 대기함은 **성공했을 때만** 비워진다(재시도 보장). 그래서 끝내 반영될 수 없는 건이 하나 끼면 이어쓰기가 매번 그 반영부터 시도하다 멈추는데, 그걸 비울 방법이 화면 어디에도 없었다 — 사용자에게는 "양방향 번역이 꼬였는데 풀 방법이 없다"가 된다. → 번역 패널(양방향 섹션)의 **'반영 대기 취소'**(`discardPendingReflections`): 대기함·초고를 비우고 대기 문단의 `user-edit` variant 를 `authored` 로 바꾼다(**저자가 쓴 문장은 화면에서 사라지지 않는다** — 되돌리면 유실로 접수된다). AI 를 부르지 않는 순수 변환이라 어떤 실패 상태에서도 성공한다. 열려 있는 세션창은 `handleExternalTranslationsChange` 에서 초고를 다시 받아 온다 — 안 그러면 다음 flush 가 초고를 대기함에 되써서 취소가 되살아난다(미저장 타이핑이 있으면 건드리지 않는다). **재시도 보장을 위해 상태를 남기는 구조를 새로 만들면 그 상태를 버리는 수단도 같이 만든다.**

- **번역 편집 커밋 방어를 무너뜨리지 않는다.** — 브라우저 정규화로 편집 영역에서 떨어져 나간 스팬이나 "내용 있던 문단 → 빈 값" 변경을 커밋하면 번역이 통째로 지워진다. → 그런 변경은 커밋하지 않는다. 빈(공백뿐인) active 번역은 `hasTranslation()` 이 없는 것으로 취급 → 원문 표시 + 재번역 대상으로 남겨 복구 가능.

- **일괄 번역 버튼은 원문/번역/split 어떤 모드에서도 항상 같은 자리에 보인다.** — 모드나 드래그 선택에 따라 버튼이 뜨고 사라지지 않는다. 선택 영역 번역으로 기능이 바뀌면 아이콘(`list-plus`→`list-checks`)과 강조색(`.is-selection`)으로만 표시.

- **번역 뷰는 단일 contenteditable 영역 구조를 유지한다.** — 문단별 contenteditable 섬으로 바꾸면 문단을 넘는 드래그 선택이 막힌다(Chromium 제약). 구분자/재번역 버튼은 `contenteditable=false` 원자 위젯으로 caret 침입 차단.

- **초고(이어쓰기) 영역은 "span 하나 + 진짜 개행"이고, 변환 중에도 편집 가능해야 한다 (양방향 번역 한글 입력 사고).** — 두 결함이 겹쳐 "초고 칸에 입력이 안 되고, 어쩌다 써져도 변환·생성 뒤에 엉뚱한 자리에 끼어 있다"로 접수됐다(2026-07-31 사용자 제보). ① **줄바꿈**: Chromium 은 `.ggai-tr-propending` 안에서 개행이 생기면 그 span 을 **형제 span 으로 쪼갠다**(클래스·id 까지 복제, 개행 문자는 남지 않는다 — 실측). 뷰는 첫 조각만 초고로 읽었으므로 **둘째 줄부터 쓴 글이 어디에도 저장되지 않고** 다음 재렌더(`root.empty()`)에 통째로 사라졌다. ② **변환 중 잠금**: `lockProOps` 가 초고 프리픽스를 `contenteditable=false` span 으로 감쌌는데, 초고 **전체**가 실려 가는 경우(커서가 초고 밖일 때·이어쓰기 flush)엔 초고 영역에 **커서를 놓을 자리가 없어져** 클릭·타이핑이 **바로 앞 문단 속으로** 들어갔다(실측). 그 문단은 수정본으로 커밋되고 다음 변환이 문단을 통째로 다시 써 "사이에 다른 문장이 끼어듦"이 된다. 변환은 수 초~수십 초라 그동안 글을 못 쓰는 것 자체도 집필 도구로선 치명적. → ① 개행은 `beforeinput`(`insertParagraph`/`insertLineBreak`)에서 가로채 `\n` 문자로 직접 넣고(끝 개행은 레이아웃에서 접히므로 `<br>` 필러 — `textContent` 에는 안 잡히고, 이어 쓰기 시작하면 브라우저가 스스로 치운다), 붙여넣기 등으로 쪼개지면 `normalizeProPending` 이 한 span 으로 되돌린다(조합 중이면 미룬다 — 읽는 값은 `readProPendingText` 가 조각을 개행으로 이어 붙여 항상 온전). ② 초고는 **잠그지 않는다** — 변환에 보낸 프리픽스 문자열(`proInFlightTailPrefix`)만 기억했다가 성공 시 `startsWith` 로 덜어낸다(문단 잠금은 유지). **초고 영역의 DOM 구조를 바꿀 때는 "지금 커서를 놓을 자리가 항상 있는가", "브라우저가 이 요소를 쪼개지 않는가"를 먼저 확인한다.**

- **문단/초고 span 의 경계를 넘는 편집(Enter·Backspace·Delete)은 브라우저에 맡기지 않는다 — 브라우저가 우리 영역을 합치고 지운다 (양방향 번역 "입력창이 사라짐" 사고).** — "끝부분을 지우고 내가 쓸 문장을 이어 쓰면 매번 에러 — 입력창이 사라지고 위로 입력 포커스가 가서 붙고, 아래에 입력을 할 수 없다"(2026-08-13 사용자 제보). 실측(Chromium, `contenteditable=plaintext-only`): ① 초고 머리에서 **Backspace** → `.ggai-tr-propending` **span 자체가 제거되고 초고 글이 앞 문단 span 안으로 합쳐진다**(빈 초고에서도 span 이 사라진다 — 빈 초고를 클릭하면 커서가 span 안이 아니라 편집 영역 레벨에 놓이는 경우도 같다). 그 뒤 타이핑은 앞 문단 수정으로 들어가 그 문단이 통째로 다시 쓰인다. ② 마지막 문단 끝에서 **Delete** → 같은 병합. ③ 문단 안 **Enter** → 편집 영역을 `<div>` 로 쪼개며 **문단 꼬리와 초고 span 을 그 div 로 끌고 들어간다** → 문단 스팬은 잘린 채 커밋되고(문단 뒷부분 유실), div 안에 친 글은 초고에도 문단에도 안 잡혀 다음 재렌더에 사라진다. → 경계 편집은 전부 우리가 수행한다: `handleProPendingBoundaryDelete`(초고 머리 Backspace = 앞 문단 마지막 글자만 `deleteData`, 문단 끝 Delete = 초고 첫 글자만 — 서로게이트 쌍 보호, 본문이 개행으로 끝나면 앞이 구분자라 아무것도 안 지운다) + `onTranslationNewline`(Enter 는 항상 `\n` 문자, 문단은 `white-space: pre-wrap` 으로 보이게). `preventDefault` 했으면 `input` 이 안 오므로 `scheduleTranslationCommit`/`onProPendingInput` 을 직접 부른다. **번역 편집면에 새 키 동작을 붙일 때는 "브라우저 기본 동작이 우리 span 을 지우거나 합치지 않는가"를 실제로 눌러 확인한다** — 텍스트 유실은 조용해서 다음 재렌더까지 안 보인다.

- **번역 편집면을 다시 그릴 때 커서와 함께 `focus()` 도 되돌린다 — selection 만으로는 키 입력이 아무 데도 안 들어간다.** — `renderTranslationBlocks` 는 `root.empty()` 로 편집면을 통째로 새로 만드는데 커서를 되돌리지 않았다. 백그라운드 이벤트(외부 번역 저장 → `handleExternalTranslationsChange` → `applyDisplayMode`, 자동 번역 진행 `renderTranslatedSoFar`, 외부 세션 변경)는 **사용자가 쓰는 도중에** 이 경로를 태우므로, 초고를 치던 사람은 "칸은 그대로인데 글이 안 써지는" 상태가 된다 — 발동 시점이 백그라운드 작업이라 재현 조건이 안 보이고, 사용자에게는 "고쳤다더니 또 재발"로 보인다(2026-08-13 제보). 실측: `root.empty()` 후 `document.activeElement` 는 `body` 가 되고, **selection 만 다시 놓으면 커서는 보이는데 타이핑이 들어가지 않는다** — `edit.focus({preventScroll:true})` **먼저**, 그 다음 selection 이어야 한다. → `captureTranslationCaret`(초고 offset / 문단 hash+offset + 그때 포커스 여부) → `restoreTranslationCaret`. **재렌더 직전에 편집면이 포커스를 갖고 있었을 때만** 되돌린다(다른 입력칸 타이핑 중이면 뺏지 않는다). 편집면을 empty() 하는 새 경로를 만들면 이 한 쌍을 반드시 함께 태운다.

- **삭제 후 커서를 문단 머리로 되돌리지 않는다 — 부분 삭제는 잘라낸 자리가 커서 자리다.** — `setCaretAfterProDelete` 가 "삭제 지점 이후 첫 문단"을 찾아 그 **머리**로 커서를 보냈는데, 문단 뒷부분만 지운 경우 그 문단이 곧 "이후 첫 문단"이라 커서가 지운 자리가 아니라 문단 맨 앞으로 튀어 올랐다("위로 포커스가 가서 붙는다"의 다른 원인). → 부분 삭제로 살아남은 첫 문단은 `{hash, offset}` 을 넘겨 `setCaretWithin` 으로 그 자리에 놓는다(중첩 마커 span 대응 TreeWalker). 통째 삭제일 때만 다음 문단 머리(없으면 초고 영역).

- **타이핑 중인 편집면에서 텍스트 노드를 갈아치우지 않는다 — 다음 첫 글자가 삼켜진다.** — 위 ①의 첫 구현이 개행을 넣을 때 `textContent` 재설정(노드 교체) + `removeAllRanges()` → `addRange()` 로 커서를 다시 꽂았더니, **엔터 다음에 친 첫 글자가 사라졌다**(2026-07-31 사용자 제보). 브라우저의 편집·IME 상태는 그 텍스트 노드와 선택에 물려 있어서, 조합이 시작되려는 순간 노드가 사라지거나 선택이 잠깐 비면 그 입력이 통째로 버려진다(영문 타이핑은 멀쩡해 보여 자동 테스트로는 안 잡힌다). → 커서가 있는 **그 노드에 `insertData` 로 글자만 끼워 넣고**, 커서는 `Selection.collapse(node, offset)` 로 옮긴다(선택이 비는 순간을 만들지 않는다). 노드 합치기(`normalize()`)도 같은 이유로 타이핑 경로에서 쓰지 않는다 — 브라우저는 타이핑 중 텍스트 노드를 여러 개로 남기므로(실측), 위치 계산은 `proPendingCaretTarget` 처럼 **노드를 건드리지 않고 offset 만 환산**해서 한다.

- **contenteditable 편집 영역은 `user-select: text` 를 명시한다 (번역 보기 클릭-커서 안 생김 사고).** — 옵시디언 앱은 UI 크롬 기본값으로 `user-select: none` 을 상속시킨다. `.ggai-tr-edit`(번역 편집면)가 이걸 안 되살려, 클릭으로 caret 을 놓는 것이 막혔다(JS 로 `setCaretOffset` 하면 들어가지만 마우스 클릭은 안 됨 — user-select:none 의 정확한 동작). 렌더/생성/분기이동 직후엔 코드가 caret 을 꽂아줘 편집이 되다가, **분기 패널에서 하위라인 삭제**처럼 편집면이 비활성인 채 패널이 재구성되면 caret 이 안 꽂혀 커서가 사라지고, 클릭으로도 못 만들며, 세션 탭을 껐다 켜도(열 때 번역면에 caret 을 안 꽂음) 안 풀렸다. → 새 contenteditable 편집 표면은 `user-select: text`(+`-webkit-`)를 반드시 켠다. 원문 본문(`.ggai-session-body`)은 편집 호스트 자체라 UA 가 caret 을 허용해 안 걸렸을 뿐, 중첩 span 으로 편집면을 구성하는 표면(번역/파생)은 명시가 필요하다.

- **번역 결과는 "요청한 대상 문단"만 기록한다 — 참고로 보여준 앞 문맥은 기록 금지.** — 앞 문맥/앞 번역은 `{{main}}` 밖(로어북 위치)에 `collectTranslationContext`/`formatTranslationContext` 로 "번역 금지" 참고 블록으로만 붙는다({{main}} 에는 대상 문단만 — 예전처럼 `context` 세그먼트를 {{main}} 에 끼워 보내지 않는다). 그래도 응답에 대상 아닌 문단이 섞여 오면 무조건 `recordTranslationVariant` 하지 말 것 — 요청하지 않은 옆 문단(특히 집필 프로 저자 원고 `authored`)을 기계 번역으로 덮어쓴다(2026-07-21 사고). → `translateParagraphs`/`previewTranslateRange` 는 `chunk` 의 대상 해시 집합에 든 응답만 기록/미리보기에 담는 방어를 유지한다.

- **사용자가 시키지 않은 순간에 AI 를 부르지 않는다 — 특히 글 쓰는 도중에는.** — 양방향 번역(소설)의 집필 변환이 "타이핑이 3초 멈추면 자동"이었다. 저자에게는 **발동 조건이 안 보인다**: 문장을 고민하다 멈춰도, 보기(원문↔번역)를 전환했다 돌아와도(재렌더가 반영을 재예약했다) 변환이 터졌고, 끝나면 화면이 다시 그려지며 **초고 입력란이 비워져** "에러가 났나? 내가 잘못 쓰고 있나?"가 됐다(2026-08-01 사용자 제보). 덤으로 쓰다 만 문장이 그대로 AI 에 나가고(오요청), 타이핑 중 재렌더가 커서·한글 조합을 끊었다. → 대기분은 **대기함에만 쌓고**, 반영은 **사용자가 생성을 요청할 때**(이어쓰기/미리보기) `runProConvert()` 한 번으로 처리한다. **자동 실행을 새로 넣을 때는 "사용자가 그 순간 무엇을 눌렀는가"로 정당화되는지 본다** — 재렌더·보기 전환·타이머는 사용자의 의사 표시가 아니다. 화면 안내 문구(placeholder·? 도움말)도 발동 조건의 일부이므로 함께 맞춘다.

- **양방향 반영은 "생성 락"이 아니라 "생성 요청 시 일괄 처리"다 (2026-07-28 사용자 교정).** — 전송/이어쓰기 버튼은 항상 눌리는 상태여야 하고, 누르면 그 자리에서 필요한 반영(말풍선 수정·초고 변환)을 전부 처리한 뒤 출발한다. **과거 실패를 이유로 요청을 거부하지 않는다** — 누를 때마다 새로 시도하고, 방금 그 시도가 실패했을 때만 멈춘다. 알림은 반영 실행부가 띄운 1회로 충분(게이트에서 중복 토스트 금지). 버튼 비활성화·조용한 no-op 로 생성을 막아 두면 사용자에겐 "왜 생성이 안 되지"로 보인다.

- **집필 프로 "반영 대기"는 반영이 끝나면 옛 문단에서도 풀어 준다.** — 대기 판별은 `active 번역 variant.kind === "user-edit"` 하나뿐인데, 한국어 수정을 영어판에 반영하면 **새 영어 해시**만 authored 로 바뀌고 수정 전(옛 영어) 문단의 user-edit variant 는 그대로 남았다. 그 뒤 전 노드로 되돌리기(undo)를 하면 옛 문단이 활성 경로로 돌아와 다시 대기로 잡히고, `renderTranslationBlocks` 의 자기회복 `scheduleProConvert` 가 영어판을 재변환(재생성)했다(2026-07-21 사고 — "되돌리기만 했는데 수정으로 오인해 영어판 재생성"). → `convertAndSplice` 성공 시 교체된 옛 영어 문단들의 해시로 `resolvePendingEditVariants` 를 호출해 active 를 직전 비-user-edit variant(없으면 미번역)로 되돌린다. variant 는 지우지 않는다(다이어트 몫). append(초고 끝쓰기)는 옛 문단이 없어 대상 아님.

- **화면 요소를 만들어 내는 설정 토글은 `applySettingsOnlyChange` 에 반영 경로를 함께 넣는다.** — 설정 패널의 저장은 `kinds:["settings"]` 로 오고, 세션창은 그 경로에서 본문을 다시 그리지 않는다(픽셀 보존이 목적). '양방향 번역'을 켜도 번역 보기 끝의 초고 입력란이 **그 자리에서는 안 생기고**, 보기 토글·생성·문단 편집 같은 다른 이유로 `renderTranslationBlocks` 가 돌 때만 뒤늦게 나타났다 — 사용자에겐 "켰는데 안 뜬다 / 가끔은 뜬다"로 보인다(2026-07-30 수정). → `applySettingsOnlyChange` 가 직전 양방향 상태(`proRenderedBidirectional`)와 비교해 바뀐 순간에만 번역 블록을 다시 그리고, 켤 때는 대기함 초고를 복원·끌 때는 화면에서 지우기 전에 대기함에 남긴다. **설정 하나가 UI 유무를 좌우하면 그 토글의 반영 경로를 직접 확인한다**(출력 방식 전환만 처리하던 자리다).

## 요약

- **요청 1번 = 앵커 1개.** — 한 구간을 몰래 여러 요청으로 쪼개 결과를 앵커 하나로 합치는 릴레이 배치는 "요청이 끝났는데 카드가 안 뜨는" 블랙박스였고 폐기됨. 구간이 입력 한도를 넘을 것 같으면 요청을 나누지 말고 `planSummaryBoundaries` 의 maxChars 로 **앵커 경계를 더 잘게** 나눈다. 요청 하나가 끝나면 앵커 하나가 즉시 저장·표시, 진행 n/m = 실제 요청 수 = 카드 수. 별도 체크포인트 없음(`summaries.pending` 은 deprecated, 발견 시 삭제만).

- **요약 재생성 시 직전 앵커 탐색에서 대상 노드 자신의 앵커를 제외한다.** — 제외하지 않으면 자기 앵커가 "직전 요약"으로 잡혀 새 패시지가 항상 0 → 재생성이 영원히 skip 되는 버그(2026-07-04 수정). `summarize()` 의 `prior = chain.filter(a => a.nodeId !== target)`, recentEvents 도 동일.

- **"매크로를 썼으니 자동 삽입 생략" 판정은 매크로가 확장되는 자리 전부를 본다.** — `{{summary}}` 배치 판정(`summaryExplicit`)이 프리셋 **텍스트 항목만** 훑어서, 작가노트·세션 메모·마커 가공 템플릿(wrap)·로어북 항목·본문에 쓴 `{{summary}}` 는 배치로 안 쳐줬다. 매크로는 그 자리에서 확장되는데 자동 삽입도 그대로 돌아 **같은 요약이 두 번** 들어갔다(사용자에겐 "위치를 옮기는 게 아니라 하나 더 붙는" 것으로 보임, 2026-08-13 수정). → `summaryMacroTexts()` 로 확장 대상 원문을 모아서 판정한다. 새 매크로 확장 지점(새 필드·새 wrap)을 추가하면 이 목록에도 넣는다. **같은 모양의 판정이 페르소나(`presetUsesPersonaMacro`)에도 남아 있다** — `{{persona}}` 를 wrap/작가노트 등에 쓰면 페르소나가 두 번 들어간다.

## 시리즈/그룹/메뉴

- **화 번호 = 만드는 화 + 1.** — max+1 로 건너뛰지 않는다(3화는 2화에서 만들어야 3화). 이미 같은 번호가 있으면 **다른 루트**가 된다(`resolveSeriesPlan` alternates → 모달 경고 + `(루트 N)` 접미사 + `prevId` 계보). **세션 복제는 시리즈 연결을 끊는다**(`copySession` 이 `meta.series` 삭제 — 같은 id+index 중복으로 화 목록이 꼬이는 것 방지).

- **그룹 세션은 GROUPS/ 밑에 별도 세션 트리를 만들지 않는다.** — 세션 파일은 호스트 시나리오 폴더(SCENARIOS/<host>/SESSIONS/)에 그대로 살고 `meta.groupId` 로만 링크. 세션 배관 전체가 SCENARIOS 경로를 가정하므로 병렬 트리는 대형 리팩터. group.json 은 멤버/설정 메타만 보유.

- **새 그룹 세션의 `groupId`·페르소나는 첫 세션 쓰기에 함께 넣는다.** — 일반 세션을 먼저 만든 뒤 `meta.groupId` 를 두 번째 저장으로 붙이면 중간 실패 때 그룹챗이 아닌 세션만 남는다. 그룹 파일 뒤 세션 생성이 실패하면 만든 그룹도 롤백한다.

- **그룹 초대 UI 를 세션뷰에 추가하지 않는다.** — 진입은 대시보드 홈 [그룹 만들기] + 좌측 사이드바 시나리오 우클릭 "현재 세션에 초대"뿐 (2026-07-11 결정, 시나리오 탭 진입 폐기).

- **세션 우클릭/롱프레스/⋮ 메뉴는 `session-menu.ts` `buildSessionMenu` 한 곳에서만 구성한다.** — 화면마다 메뉴가 달라 기능이 숨는 문제 방지. 새 세션 액션은 여기에만 추가. ⋮ 버튼은 상시 노출(우클릭/롱프레스는 사실상 숨김). 세션은 "이름"이 아니라 **"제목"** 용어(폴더형 엔티티만 "이름 변경").

## 임포트

- **임포트한 내용은 "세션을 만든 뒤 두 번째 저장으로" 채우지 않는다.** — 실리태번 채팅(.jsonl) 임포트가 빈 세션을 먼저 만들고(`createSession(seed="")`) 노드 트리를 메모리에만 심은 뒤 `saveSession` 으로 채웠다. 그 사이(활성 페르소나 해석, store 이벤트 리스너, 두 번째 쓰기)에서 무엇이든 던지면 **대화가 하나도 없는 빈 세션만 디스크에 남는다** — 사용자에겐 "임포트는 됐는데 세션이 텅 빔"으로 보인다(실제 잔해: 노드 1개·patches 0개·personaFile 없음). → `createSession(..., prefill)` 로 **노드 트리·활성 리프·페르소나를 생성 첫 쓰기에 함께** 넣는다. 실패하면 아무것도 안 남는 게 맞다(반쯤 만들어진 세션 금지). 파서/빌더는 무죄일 수 있으니(실제 파일로 검증됨) 같은 증상이 또 나오면 **쓰기 순서**부터 본다.

## 대시보드/에디터

- **대시보드 프롬프트 편집기는 우측 디테일 `PromptsSection` 을 재사용하지 않는다.** — PromptsSection 은 "현재 세션의 활성 세트" 화면이라 대시보드에서 쓰면 세트를 만질 때 세션 설정까지 바뀐다. → 전용 `PromptSetEditorSection`(세트 파일만 읽고 쓰며 활성 설정·세션 불변). CSS 클래스는 공유하되 코드는 분리.

- **편집기 진입은 단일 경로 `plugin.openStellaEditor(kind, file)`.** — 구 standalone 에디터 뷰 3종 + `openStellaView`/`mountStellaNav` 는 제거됨. 다시 만들지 않는다.

## 빠른 답장(QR)

- **`||` 는 파이프 값을 지우는 게 아니다.** — ST 원본에서 `||` 가 막는 것은 "맨 인자를 안 적었을 때 앞 결과를 자동으로 끼워 넣기"뿐이고, 손으로 적은 `{{pipe}}` 는 값을 그대로 읽는다. 우리는 `state.pipe` 를 통째로 비웠고, 그래서 `/input … || /setvar key=x {{pipe}} || /if left={{getvar::x}} right="" rule=eq {: /abort :}` 라는 **가장 흔한 대필/입력 QR 관용구**가 항상 "취소"로 빠졌다(입력값이 저장 전에 증발). → 파이프 값은 항상 이어지고, `breaksPipe` 는 `implicitPipe` 플래그로 **자동 주입만** 끈다(`services/qr-runner.ts`). 남의 문법을 우리 직관으로 요약하지 말고 원본 문서를 확인한다(CLAUDE.md 8).

- **세션에 글을 남기는 새 경로를 만들면 "마무리 파이프"를 반드시 같이 태운다.** — 생성이 끝날 때 도는 후처리는 (1) 저장 원문 정규식 `applyRawRegexToGeneration` (2) `extensions.runGenerationComplete`(자동 번역·삽화·요약·로어북 자동 생성·반복 표현·폰·알림) 두 단계인데, 오랫동안 **소설 뷰·챗 뷰·선채팅 세 곳에만** 복사돼 있었다. 그래서 QR 이 남긴 글(`/sendas` `/comment` `/impersonate`)은 요청 쪽은 같은 `planSessionRequest` 를 쓰면서 결과 쪽만 통째로 갈라져, 마법 신문·가십지·대필에 정규식도 번역도 삽화도 붙지 않았다. → QR 쪽 진입점은 `qr-runner.ts` 의 `appendSessionMessage` + `runGenerationPipe` **한 쌍**. 앞으로 "본문에 글을 붙이는" 경로를 새로 만들면 이 두 단계를 빼먹지 않았는지부터 확인한다. 확장마다 별도 실행 경로를 만들지 않는다.

## 타입/기타

- **`.catch(() => [])` 폴백은 반환 타입을 명시한다: `catch((): T[] => [])`.** — 타입 미지정 시 union 축소 캐시 순서가 바뀌어 **다른 파일**의 같은 패턴이 never 로 깨진 실사례(entity-actions → chat-session-view).

- **`onunload` 에서 `detachLeavesOfType()` 을 호출하지 않는다.** — 옵시디언 공식 가이드의 안티패턴(사용자 레이아웃 파괴).

- **"자동 실행 스위치"에 수동 기능 UI 를 묶지 않는다.** — 폰 번역 설정을 토글 하나로 간소화하면서 뷰의 `phoneTranslationEnabled()` 를 `isAutoTranslateOn()`(자동 번역 스위치)로 연결했다. 그 결과 자동 번역을 끄면 **번역 보기·원문 보기·개별 번역 재생성 같은 수동 기능이 메뉴에서 통째로 사라졌다**(설정만 줄이라고 했는데 기능이 없어짐, 2026-07-26 사고). 자동/수동은 별개 축이다: 자동 스위치는 "생성 후 알아서 번역할지"만 정하고, **수동 조작은 항상 노출**한다(사용자가 직접 누르는 건 언제나 가능해야 한다). 설정 항목을 줄일 때 그 값이 UI 노출 조건으로 쓰이고 있는지 먼저 `grep` 한다.

- **폰 오버레이는 `.modal` 자체 크기를 폰 루트와 같게 못박는다.** — 옵시디언 기본 `.modal` 은 `max-height: 80vh` + `overflow: auto` 인데 폰 루트(`.ggai-phone-modal .ggai-phone-root`)는 `height: min(780px, 88vh)` 였다. 창 높이가 886px 미만이면 vh 갈래가 이겨 **루트가 모달보다 8vh 크고**, 모달이 스크롤 컨테이너가 되어 **폰 프레임 바깥에 스크롤바**가 생겼다(안쪽 본문 스크롤과 겹쳐 "이중 스크롤"). 설정 화면처럼 내용이 긴 화면에서 특히 눈에 띄었을 뿐 **모든 화면에 있던 상시 버그**다(2026-07-26 수정 — 재현 HTML 로 `.modal` 이 스크롤러임을 실측해 확정). → `.modal.ggai-phone-modal` 에 루트와 **같은 `height`/`max-height`** 를 주고 `overflow: hidden`. 폰 프레임 크기를 바꿀 땐 두 곳을 같이 바꾼다. **증상이 "프레임 바깥 스크롤"이면 안쪽(body/pane)을 고치지 말고 모달·루트의 높이 관계부터 실측한다.**

- **디테일 뷰 설정 컨트롤을 폰 화면 안에 그대로 가져다 쓰면 깨진다.** — 폰은 테마 오염을 막으려고 `.ggai-phone-root .ggai-phone-screen :where(button)` 로 버튼 꾸밈(테두리·배경·패딩·반경)을 전부 지운다. 설정 앱(폰 홈)에 컨트롤 킷을 얹었더니 `ggai-preset-btn`/`ggai-model-btn` 이 납작해져 설정 화면이 깨져 보였다(2026-07-26 수정). → 리셋과 **같은 특이도(0,2,0)** 의 `.ggai-phone-screen .클래스` 규칙을 파일 **뒤쪽**에 두어 되살린다(문서 순서로 이김). 값은 디테일 뷰 원본과 동일하게 유지해 두 화면이 같은 모양이 되게 한다 — 폰용으로 새 디자인을 만들지 않는다. 폰에 새 설정 컨트롤을 얹을 때마다 이 복원 목록에 추가했는지 확인.

- **폰 화면 안의 버튼 꾸밈은 클래스 하나(0,1,0)로 주면 리셋에 먹힌다 — `.ggai-phone-screen` 접두 필수.** — SNS 리스트 칩(`.ggai-phone-sns-listchip`)이 테두리·배경·패딩을 클래스 하나로만 줘서 테마 격리 리셋(`.ggai-phone-root .ggai-phone-screen :where(button)`, 0,2,0)에 전부 지워졌고, 칩이 맨 글자로 뭉개져 리스트 줄이 깨져 보였다(2026-07-26 발견 — `.is-active` 는 (0,2,0)이라 활성 칩만 멀쩡해서 오래 안 보였다). 폰 안에 버튼 모양을 새로 만들 때는 **반드시 `.ggai-phone-screen .클래스`** 로 쓴다(설정 컨트롤 복원 목록과 같은 규칙).

- **모바일 툴바 여백은 `env(safe-area-inset-*)` 직접 사용**(옵시디언 `var(--safe-area-inset-bottom)` 는 환경에 따라 0). 키보드에 가린 커서는 `visualViewport` 로 키보드 위까지만 스크롤.

- **엔진 onLayoutReady 끝에서 `window` CustomEvent `ggai-stella-engine-ready` 를 반드시 쏜다.** — 이걸 지우면 BRAT/hot-reload 로 엔진만 리로드됐을 때 새 엔진이 휴면으로 시작하고 옛 인스턴스의 설정 패널·라우팅이 통째로 사라져, 의존 플러그인(개인 PRO)이 새 엔진에 재배선할 트리거를 잃는다(개인 플러그인을 껐다 켜야 복구). onLayoutReady 는 리로드 시 즉시 재실행되므로 신호도 즉시 나간다. 의존 플러그인 쪽은 자기 소유 `registerDomEvent(window, ...)` 리스너로 받아 `connectEngine`(idempotent: 옛 배선 teardown 후 재활성화)을 돌린다.

- **SNS 데이터는 `feed.posts` / `accounts.accounts` 를 통짜로 읽지 않는다 — 반드시 활성 서버 필터를 거친다.** — 스텔라 네트워크 리스트는 V3-7 부터 **서로 단절된 별개 서버**다(글·계정에 `listId` 도장). 새 코드가 원본 배열을 그대로 순회하면 다른 서버의 글·주민이 피드·알림·프롬프트·세션 주입에 새어 들어가고, 사용자가 리스트를 나눈 목적(세션별 지식 격리)이 조용히 무너진다 — 화면은 멀쩡해 보여서 발견이 늦다. 읽기는 `phone.snsPostsOfActiveList(feed)` / `phone.snsAccountsOfActiveList(accounts)`, 세션 쪽 게이트는 `phone.isSessionInActiveServer(session)`, 쓰기는 생성 시점의 `phone.activeSnsListId()` 로 스탬프. id 로 한 건을 집는 경로(좋아요·답글·번역·삭제)는 예외 — 화면에 보이는 것만 집을 수 있으므로 필터가 필요 없다.

- **"정리용 후보 목록"을 자격 게이트로 만들지 않는다 — 판정 조건은 화면에도 적는다.** — 폰 연락처 후보를 `session.meta.personaFile`(세션당 페르소나 **하나**, 나중 선택이 덮어씀) 하나로 판정했다. 그래서 플레이 도중 페르소나를 바꾸면 그 캐릭터가 옛 페르소나 폰에서 **사라지고** 새 페르소나로 옮겨 갔고, 페르소나 기록이 없는 옛 세션은 어느 계정에도 안 떴다 — 유저에게는 "지금 대화 중인 캐릭터는 없고 엉뚱한 캐릭터가 뜨는" 무작위 동작으로 보였다(2026-08-25 제보). 이런 목록의 목적은 **많아서 헷갈리는 걸 줄이는 정리**이지 참가 자격 심사가 아니다 → 판정은 넉넉하게(이력을 **추가만** 하며 누적, 기록 없음 = 배제 아니라 통과), 좁히는 건 사용자가 직접 하는 등록/체크 단계에 맡긴다. 그리고 남는 조건 하나(여기선 폰 로그인 계정)는 목록·빈 화면 문구에 **계정 이름과 함께 적어** 유저가 규칙을 추측하지 않게 한다. 페르소나·계정 참조는 **경로가 아니라 id** 로 남긴다(이름 변경에 강건).

- **한 세션에 쓰기 주체가 둘 이상 붙을 수 있게 만들었으면, 겹침 방어를 그 자리에서 함께 만든다.** — 선채팅은 원래 `isViewingSession` 으로 "보고 있는 세션엔 안 끼어든다"고 판정했다. 이 판정을 사용자 요청으로 제거했더니(v0.0.38, "보고 있어도 온다"), **그게 겸사겸사 맡고 있던 겹쳐 쓰기 방어까지 같이 사라졌다** — 사용자가 말풍선을 고치는 중이거나 세션창이 스트리밍 중인데 백그라운드 서비스가 같은 세션 객체(store 캐시는 뷰와 **같은 참조**다)에 노드를 붙이고 저장한다. 게다가 `sendInner` 는 발화 직전 `flushSessionEdits` 로 **열린 세션창의 편집을 강제 커밋**시키므로, 사용자가 문장을 지우는 한복판에 저장이 끼어든다. → 판정 하나를 지울 때 그 판정이 **부수적으로** 지키던 불변식이 뭐였는지 먼저 적는다. 겹침 방어는 `session-host.ts` 의 `isSessionGenerating` / `isSessionBeingEdited` 로 명시한다: 스케줄러 틱은 바쁜 세션을 **건너뛰되 예약(nextAt)은 그대로 둬** 손을 떼면 다음 틱에 바로 오게 하고(예약을 미루면 요청받은 동작이 사라진다), `tick()` 에서 걸러 바쁜 세션 하나가 다른 세션의 차례까지 막지 않게 한다. 모델 응답을 기다리는 몇 초 사이에도 생성이 시작될 수 있으므로 **저장 직전에 한 번 더** 확인한다.
- **NAI 캐릭터별 프롬프트(`char_captions`)는 "글만" 보내면 500 이 난다.** — 삽화 프롬프트의 `|` 를 캐릭터별 프롬프트로 나눠 보내자마자 이미지 생성이 전부 500 으로 죽었다. 실제 NAI 산출물 PNG 메타데이터를 뜯어 보니 공식 클라이언트는 좌표를 **안 쓸 때도**(`use_coords:false`) 캐릭터마다 `centers`(우리 기본값은 인원수만큼 가로 균등 배치)를 넣고, `v4_negative_prompt.caption.char_captions` 를 **같은 길이·같은 좌표**로(캐릭터 UC 가 없으면 빈 문자열) 맞춰 보낸다. → 보정은 Core `providers/novelai.ts` 가 한다(플러그인은 `charCaptions` 만 넘기면 됨). `use_coords` 자동 판단은 **호출자가 준 좌표**만 세고 이 기본 좌표는 세지 않는다. 남의 API 페이로드는 문서가 아니라 **실제로 나간 요청/산출물 메타데이터**를 기준으로 맞춘다.
