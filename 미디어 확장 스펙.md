# 미디어 확장 스펙 — 번역 / 삽화

> 세션의 번역·삽화 확장의 **확정 설계와 불변 원칙**. media 관련 변경 전 필독.
> 현재 동작·진입점은 `구현현황.md`("번역"/"삽화"/"문단 재생성"), 사고 교훈은 `회귀금지.md`.

---

## 대원칙 (절대 불변)

- **원문 노드 불변.** 번역/삽화 재생성은 `session.json` 원문 트리를 절대 수정하지 않는다.
- **저장과 표시 분리.** 생성물은 JSON(`translations.json`/`illustrations.json`)에 저장하고, 출력 방식은 그 데이터를 어떻게 보여줄지의 표시 계층 선택일 뿐이다. 표시 계층은 store(`getSessionTranslations`/`saveSessionTranslations` + 이벤트)와 해당 JSON 만 읽고 쓴다 — JSON 만으로 외부 UI 플러그인을 만들 수 있을 만큼 분리를 유지한다.
- **variant 자동 삭제 금지.** 같은 키에 재생성하면 새 variant 를 쌓고 active 만 표시한다. 사용자는 예전 variant 로 되돌아갈 수 있고, 최종 active 만 남기고 정리(다이어트)할 수 있어야 한다. 표시 정책 때문에 숨겨진 variant 를 자동으로 지우지 않는다 — 삭제는 명시적 다이어트 기능에서만.
- **UI 범위 게이트.** 구현 지시가 저장 구조/설정에 한정되면 세션창 표시 UI(치환·분할·인라인·모달·툴바 버튼)를 앞당겨 만들지 않는다. 미싱링크가 생기면 새 버튼·탭·모달을 임의로 만들지 말고 확인한다.

## 저장 단위

```
GGAI/SCENARIOS/<scenario>/SESSIONS/<session>/
├── session.json        # 원문 노드 트리 (불변)
├── translations.json   # 문단+원문 해시 기준 번역 + 표시 상태(displayMode)
├── illustrations.json  # 노드 기준 삽화
└── assets/             # 삽화 이미지 파일
```

**귀속 기준이 다른 이유**: 번역은 "이 문장의 번역"이라 원문이 한 글자라도 바뀌면 키가 바뀌어야 한다 → 문단 내용(해시) 기준. 삽화는 "그때 그 장면의 그림"이라 원문 워딩을 다듬어도 유효하다 → 생성 사건(노드) 기준.

스키마: `src/types/media.ts`. `키 → variants → activeVariantId` 구조와 원문 불변 원칙은 유지하되 필드명은 조정 가능.

`translations.json`: `{ schemaVersion, displayMode: "source"|"translation", paragraphs: { <원문해시>: { source, activeVariantId, variants: { <id>: { kind: "ai-translation"|"translation-regen"|"user-edit", text, modelProfileId, promptId, ... } } } } }`

`illustrations.json`: `{ schemaVersion, nodes: { <sourceNodeId>: { activeVariantId, variants: { <id>: { kind, imageProfileId, promptId, assetId, path, ... } } } } }`

## 번역

**입출력** (문단 기준, 순수 로직 `src/util/translate-paragraphs.ts`): 최종 본문을 줄바꿈 기준 문단으로 나누고, **문단 원문 내용의 해시**가 키다. 대상 문단을 문서 순서대로 JSON 배열로 보내되, 각 대상 바로 앞 문단이 대상이 아니면 연속성용 `role:"context"` 로 끼운다. 응답도 `[{id, translation}]` JSON 배열. 응답에 포함된 각 문단의 active 번역을 최신 결과로 갱신(context 문단의 개선 번역도 반영). 문단을 고치면 해시가 바뀌어 그 문단만 "번역 안 됨"이 되고, 같은 내용 문단은 분기를 넘나들어도 번역을 재사용한다.

**번역문의 문단 구조는 자유**: 원문 한 문단의 번역이 대사/서술 사이 줄바꿈으로 여러 줄이 되어도 통째로 그 슬롯에 저장·표시한다. 다시 쪼개 장부를 맞추지 않는다.

**대량 번역 안전장치**: 수동 일괄 번역은 분량이 크면(문단 6개 또는 약 2,000자 초과) 확인 다이얼로그(`취소`/`최근 문단 6개만`(기본)/`전체 번역`, 전체가 20문단↑면 재확인) — 긴 세션에서 번역을 처음 켤 때 전체 전송 사고 방지. 실제 전송은 청크(문단 8개 또는 약 3,000자) 단위 순차 요청 + 청크마다 즉시 저장(중간 실패해도 받은 번역 보존, "N/M개 저장 후 중단" 안내).

**자동 번역**: 켜져 있으면 생성 완료 직후 **이번 생성으로 새로 생긴 구간의 미번역 문단만**(`collectUntranslatedParagraphsFrom`, 생성 시작 지점 경계) 번역한다. 과거 미번역 본문 전체를 자동 전송하지 않는다.

**표시**: "원문 치환"은 세션창에서 번역문만 노출하는 표시 모드 — on/off 토글로 즉시 원문 표시로 돌아가 편집/이어쓰기 가능. 번역 보기 본문은 최종 본문을 문단 토큰으로 분해해 구분자(줄바꿈)는 원문 그대로 두고 active 번역이 있는 문단만 치환. 번역이 하나도 없으면 원문 보기와 동일. `displayMode` 는 `translations.json` 에 세션별 저장, 생성 중에는 원문 보기로 임시 전환 후 자동 번역 성공 시 번역 보기 복귀.

**번역 보기도 직접 편집이 기본**(읽기 전용 아님). 본문처럼 고치면 그 문단의 `user-edit` translation variant 로 즉시 저장·active 선택(원문 노드 불변). 커밋 방어(빈 값·정규화 스팬 무시)는 회귀금지.md.

**출력 방식** (`TranslationOutputMode`): `replace`(원문 치환 토글) / `split-h`(좌우 2분할, 드래그 분할바). 이 둘만 구현·유지한다.

## 삽화

**생성** (`plugin.illustration.generateForNode`, NovelAI 이미지 프로필 전용): 본문 끝 첨부량 → 프롬프트 생성 LLM → 메인+UC → Core `image()` → assets PNG → variant 기록. 생성된 프롬프트의 `|` 구분은 요청 직전 `util/image-char-prompts.ts` `splitImagePrompt` 로 나눠 첫 덩어리는 메인, 나머지는 캐릭터별 프롬프트(NAI `char_captions`)로 보낸다(스텔라 폰 카메라도 같은 경로). variant 에는 나누기 전 원본 한 줄을 그대로 저장 — 재생성 모달에서 다시 편집·분리된다. variant 프롬프트는 PNG 메타가 아니라 variant(`prompt`/`negativePrompt`)에 저장. 같은 노드 안에서 삽화만 재생성 가능(새 variant 쌓기, active 표시).

**출력 위치**: `출력 뷰`(독립 leaf `illustration-output-view.ts`, 활성 세션 최신 삽화를 letterbox 표시, 팝아웃 가능) / `인라인`(본문 흐름에 삽입). 정규화 `resolveIllustrationOutput`(미지정 기본 `inline`, 레거시 `top`→`panel`, `source-inline`/`translation-inline`→`inline`).

**표시 정책**: 인라인 = 활성 경로 삽화 누적 / 출력 뷰 = 최신 1개. 출력 위치에 따라 자연 귀결 — 별도 정책 옵션 UI 없음.

**인라인 앵커는 저장하지 않고 렌더 시점 계산** (`src/util/illustration-anchors.ts` `computeIllustrationAnchors`): 삽화 노드가 마지막으로 기여한 글자 기준 — 문단을 끝냈으면 그 문단 뒤, 문단 중간·본문 끝 미완 꼬리에서 끝났으면 그 문단 앞의 문단 경계로 스냅("마지막으로 끝난 문단 뒤, 미완 문단 앞"). 기여 텍스트가 편집으로 전부 사라진 노드는 인라인 미표시(갤러리엔 유지). 단 **자기 글자를 애초에 쓴 적이 없는 노드**(끝을 지우기만 한 `delete` 편집)는 그 편집 지점에 표시한다 — 그건 '지워진 대목의 삽화'가 아니라 '지우기 동작에 잘못 매달린 삽화'라, 숨기면 출력 뷰에만 뜨는 분리가 난다. 애초에 그런 노드에 안 붙도록 `resolveIllustrationTargetNode` 가 대상 노드를 보정한다. 위젯 원자 블록·풀 재사용 규칙은 회귀금지.md.

**자동 삽화 밀도 게이트**: 자동+인라인은 마지막 삽화 앵커 이후 **완성 문단 ≥ 5개**(`INLINE_AUTO_MIN_PARAGRAPHS`)일 때만 생성. "마지막 삽화 이후 문단 수"는 저장하지 않고 매번 현재 브랜치에서 계산(분기 이동/편집에 강건). 수동(툴바 탭)은 게이트 없이 즉시.

**표시 컴포넌트**: 캐러셀 `illustration-carousel.ts`(variant 좌우 슬라이드, hover 화살표/카운터/재생성, 클릭→라이트박스) — 출력 뷰·인라인·챗 말풍선이 모두 재사용. 갤러리 `gallery-modal.ts`(세션 전체 그리드). **프롬프트는 재생성 모달에서만 노출**(캐러셀/라이트박스/갤러리 어디에도 안 보임). 재생성 = `IllustrationRegenModal`(프롬프트/UC 수동 편집) → `regenWithPrompt`(LLM 생성 skip). 툴바/자동은 프롬프트 생성부터.

## 세션 툴바 트리거 버튼

세션 툴바에 **번역 / 삽화 / 문단 재생성** 세 버튼. 공통: 단일 탭=생성 실행, 꾹 누르기(500ms)=자동 on/off 토글(활성 시 스타일 변경). 우측 설정의 `~ 사용` 마스터 토글(`enabled`)이 off 면 비활성. `enabled`(기능 사용)와 `auto`(자동 생성)는 별도 설정이며 둘 다 활성 설정(세션 메타/PluginData/프리셋)에 저장.

## 문단 재생성

번역/삽화와 같은 트리거 패널의 세 번째 버튼이지만 미디어가 아니라 **원문 편집** — 자세한 동작은 `구현현황.md` "문단 재생성". 핵심: AI 보조 문단 편집기(원문·결과 합친 편집 영역, 범위 ▲/▼ 조절, 저장 프롬프트/직접 입력), [적용] 시에만 해당 구간 replace 의 user-edit 노드 파생(적용 전 원문 일치 검증 — 원문 불변). 번역 사용 중이면 [재번역] 버튼(선택 범위 재번역 = 개별 문단 재번역의 통합 위치).

## 남은 단계

- variant 정리(다이어트) UI — 최종 active 만 남기고 숨은 variant 삭제. 순수 함수(`listTranslationVariants`/`setActiveTranslationVariant`/`pruneTranslationVariants`)와 데이터는 이미 있고 조작 UI 위치만 미정.
