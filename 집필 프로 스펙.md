# 집필 프로(PRO) 스펙

> 동시 집필 모드(소설 PRO) 확정 설계. PRO 관련 작업 전 필독.
> 상태: **P0~P6 구현 완료** (P2 는 입력 바 대신 "대기 문단 모델" — 한국어 보기 본문에서 직접 쓰기/수정, 멈추면 영어판 반영. §3.4 문단 쌍 예시, §5 집중 디테일뷰, §8 번역 용어집 자동 수집까지 완료). 남음 = P7(타임라인). 배선은 `구현현황.md` "집필 프로" 섹션, 이 문서는 설계 결정만 유지한다.
>
> **이중 원고 파이프라인의 후속 진화는 `양방향 번역 스펙.md` 가 진실 소스다** — "반영 대기" 상태는 화면/추론이 아니라 **반영 대기함**(translations.json `pendingReflections`/`proDraft`, 구현 완료)이 담당하고, 파이프라인 자체를 번역 확장의 공개 기능 '양방향 번역'으로 이관할 예정. §3.3 의 대기 판별 서술 중 대기함과 충돌하는 부분은 그 문서가 우선한다.

---

## 0. 목표와 사용자

- 사용자: 저자 본인(초고 제작 도구). 일반 배포 사용자(AI RP 용도)에게는 **어떤 표면도 노출하지 않는다.**
- 핵심 컨셉: "원본 + 번역"이 아니라 **같은 소설의 영어판/한국어판 두 원고가 나란히 자라는** 동시 집필.
  - 저자는 한국어로 읽고 쓴다. 영어판은 AI 지능을 위한 진짜 원고로 뒤에서 유지된다.
  - 저자가 쓴 한국어는 **글자 그대로** 한국어판에 남는다(왕복 번역으로 뭉개지 않음).
- 쿼터 전제: 저자 개인 환경(플랜 API)이라 AI 호출 비용을 아끼기 위한 기능 축소는 하지 않는다.
  단, 일반 배포 시 재사용될 함수는 호출량을 설정으로 조절 가능하게 만든다.

## 1. 본체/개인 플러그인 분리 원칙

**"능력은 본체, 스위치와 취향은 개인 플러그인."**

### 스텔라 엔진 본체 (공개 레포)
- PRO 의 무거운 기계 전부: 집필 뷰, 이중 원고 파이프라인, 메모 보드, 타임라인, 번역 품질 로어북, 원고 조회 API.
- **휴면 원칙**: 외부에서 활성화 호출이 오기 전까지 뷰 타입·명령·세션 생성 선택지·설정 패널을 **등록 자체를 하지 않는다.** 설정 화면의 확장 목록(`builtinExtensions`)에도 넣지 않는다. 코드를 읽는 사람만 존재를 알 수 있다.
- 활성화 이음새: `plugin.pro.activate()` (개인 플러그인이 onload 에서 호출). 반환값은 해제 핸들. 내부적으로는 기존 `syncExtensionResource` 패턴과 동일하게 등록/해제 일괄 처리.

### 개인 플러그인 (별도 레포, 스타터 템플릿 기반)
- `activate()` 호출 + 진입 명령/버튼("집필 세션 만들기", "메모 보드 열기", "타임라인 열기") — 기존 확장 API(옵시디언 명령, 세션 트레이 액션)로 등록.
- **정책 전부**: 마크다운 미러의 폴더/파일명 규칙, 메모 저장 폴더 오버라이드 등. 엔진은 콘텐츠를 줄 뿐 파일 배치를 모른다.
- 설정 UI: 기존 확장 설정 패널 + `getPanelData`/`setPanelData` 격리 저장칸 재사용.
- 본체와의 접점은 최소 3종으로 고정: **활성화 호출 / 조회 API / store 이벤트 구독.** 이 밖의 내부 함수에 의존하지 않는다(본체 업데이트 내성).

### 우아한 강등 (필수 보장)
- PRO 세션은 개인 플러그인이 없거나 활성화가 안 된 환경에서도 **일반 소설 세션으로 정상 동작**해야 한다.
- 따라서 PRO 표시는 별도 mode 가 아니라 `session.meta` 의 플래그로 한다 (아래 §2). vault 를 옮기거나 플러그인을 꺼도 세션이 죽지 않는다.

## 2. 세션 형식 — 일반 소설 세션과의 차이

| | 일반 소설 세션 | PRO 집필 세션 |
|---|---|---|
| mode | `novel` | `novel` (동일) + `meta.proWriting: true` |
| 라우팅 | 소설 뷰 | PRO 활성화 시 집필 뷰, 아니면 소설 뷰 (session-host 분기) |
| 진짜 원고(세션 노드) | 사용자 언어 그대로 | 영어판 (컨텍스트/로어북 매칭/분기 트리의 기준) |
| translations.json | 기계번역 캐시 | **한국어판 원고** (문단 짝 + 출처 구분) |
| 입력 | 그대로 저장 | 한국어 입력 → 문체 이어받기 영어 변환 후 저장 + 원문 한국어를 짝으로 보존 |
| 기본 화면 | 원문 | 한국어판 (영어판은 토글) |
| 한국어 편집 | 번역 다듬기(영어 불변) | **영어판 자동 반영** (§3.3) |

- 데이터 스키마는 세션/시나리오 공용 형식을 그대로 쓴다. 새 엔티티는 메모(§6)와 타임라인(§7)뿐.
- 기존 확장(번역/삽화/요약/로어북 자동생성/AI 선별/정규식/알림/폰)은 전부 그대로 적용된다 — 같은 세션 형식, 같은 `planSessionRequest` 위에서 돌기 때문. PRO 를 위해 확장별 특수 배선을 만들지 않는다.

## 3. 이중 원고 파이프라인

### 3.1 핵심 프리미티브 — "변환-접합-페어링" 한 함수

전송/문단 편집/통째 붙여넣기가 전부 **같은 프리미티브 하나**를 지난다:

```
convertAndSplice(한국어 문단들, 영어판의 교체 구간):
  1. 한국어 문단들을 JSON 세그먼트(1:1, id별)로 AI "집필 변환" 호출
     - 첨부: 영어판 꼬리 N자(문체 참조) + 최근 문단 쌍 M개(예시) + 번역 로어북
     - 지시: 번역이 아니라 앞 원고의 서술 문체·시점·시제를 이어받은 영어로. 문단 1:1 유지.
  2. 받은 영어를 영어판의 교체 구간에 접합
     - 끝에 덧붙임(전송) = append / 기존 구간 대체(편집·붙여넣기) = replace 패치 노드
  3. 문단별 페어링: 영어 문단 해시 밑에 입력된 한국어를 `authored` variant 로 저장(active)
```

- 문단 정렬은 프로토콜로 보장(세그먼트 id 1:1, 병합/분할 금지 지시 + 개수 불일치 시 재시도).
- **장면 전환(`***`) 리터럴 통과**: 별표 구분선 문단(공백 무시 별표 3+)은 언어 중립이라 변환하지 않는다 — write 세그먼트로 보내지 않고 영어판에도 같은 기호를 그대로 접합(en=ko 짝). op 전체가 리터럴뿐이면 AI 호출 자체를 건너뛴다(왕복 변환·개수 씨름 방지).
- 실패 시 원고를 잃지 않는다: 전송이면 입력창에 한국어 유지 + 알림, 편집이면 한국어판 편집만 보존하고 영어 반영 대기 상태로 표시. 생성 요청은 변환 성공 전에 나가지 않는다.

### 3.2 전송 흐름
1. 한국어 입력 → `convertAndSplice`(끝에 덧붙임) → user-write 노드(영어) + authored 짝 저장.
2. 이후 생성은 일반 세션과 완전 동일 (`planSessionRequest`).
3. AI 영어 응답 → 기존 자동 번역 훅이 en→ko 실행. PRO 세션에서는 번역 요청에 **한국어판 직전 문단들 + 최근 문단 쌍 예시**를 문체 참조로 첨부(§3.4).

### 3.3 한국어 편집 → 영어판 자동 반영
- 집필 뷰의 한국어 직접 편집은 지금처럼 즉시 variant 로 저장하되(입력 안정성 규칙 그대로), 바뀐 문단들을 `convertAndSplice`(replace) 로 영어판에 반영한다. **반영 시점 = 사용자가 생성을 요청할 때**(이어쓰기/미리보기) — 편집 디바운스 자동 반영은 폐기됐다(`양방향 번역 스펙.md` §1.2).
- 반영 전까지 해당 문단은 "영어판 반영 대기" 상태 — 이 상태에서 생성/전송을 시작하면 먼저 반영을 flush 한다(전송본 단일 진실 소스 대전제 유지).
- 여러 문단 통째 교체(붙여넣기): 별도 알고리즘이 아니라 같은 프리미티브다. 교체된 한국어 블록 전체를 한 번의 `convertAndSplice`(replace, 여러 문단) 로 처리 — 블록 안 정렬은 세그먼트 프로토콜이 보장하므로 재연결 문제가 생기지 않는다. **이 때문에 프리미티브를 "끝에 덧붙임"이 아니라 "임의 구간 교체"로 처음부터 설계한다.**
- **여러 문단 선택 삭제 = 영어판 구조 삭제** (AI 소설 편집의 기본기 — 마음에 드는 곳까지 남기고 뒤를 지우기). 한국어 보기에서 문단들을 선택해 지우면: 통째 선택 문단은 영어판 해당 구간을 delete 패치 노드로 즉시 제거(변환 불필요·AI 호출 없음·undo 가능), 양끝의 부분 선택 문단은 남은 한국어가 수정본이 되어 §3.3 대기→변환 흐름에 합류, 초고 영역에 걸친 선택은 초고도 같이 잘린다. 한 문단 안의 부분 삭제는 일반 문단 수정과 동일.

### 3.4 문체 유지 장치
- 1차: 슬라이딩 윈도 — 직전 문단 쌍(영어+한국어) M개를 양방향 프롬프트에 예시로 첨부. 저자가 직접 쓴 한국어가 예시에 섞이므로 쓸수록 AI 한국어가 저자 문체를 닮는 자기강화 구조. 별도 "문체 학습" 장치 없음.
- 첨부량(꼬리 글자 수 N, 쌍 개수 M)은 PRO 설정 패널의 숫자 입력. 하드코딩 금지.
- 지시문은 전부 편집 가능한 미디어 프롬프트 버킷: `proConvert`(한→영 집필 변환) 신설, en→ko 는 기존 `translation` 버킷 프롬프트에 문체 참조 자리(`{{style}}` 류 치환자)를 더한 PRO 기본 프롬프트 추가.

### 3.5 페어링 데이터
- 번역 variant 에 출처 필드 추가: `machine`(기계번역) / `authored`(저자 원고) / `edited`(저자 수정). 기존 파일은 필드 없음 = `machine` 으로 해석(마이그레이션 불필요).
- 한국어판 완성도 표시(어느 문단이 내 글인지)와 번역 로어북 추출(§8)의 원천 데이터가 된다.

## 4. 마크다운 미러 (단방향)

- 방향: **세션 → md 단방향.** md 는 산출물이며 수정은 집필 뷰에서 한다. (양방향은 보류 — §10. 다만 §3.3 의 "임의 구간 교체" 프리미티브가 양방향의 핵심 난제를 미리 풀어두는 포석이다.)
- 엔진 몫:
  - 조회 API: `plugin.pro.getManuscript(sessionFile)` → `{ markdown, meta }`. markdown = 한국어판 활성 경로 전문(+ frontmatter 에 세션 id/이름/시나리오/시리즈).
  - 변경 신호: 기존 store 이벤트(`session-changed`, `session-translations-changed`) 그대로 — 새 이벤트를 만들지 않는다.
- 개인 플러그인 몫:
  - 이벤트 구독 → 디바운스 → `getManuscript` → **자기 규칙으로** 경로 결정(세션별/시나리오별/시리즈 통합 등) → md 파일 덮어쓰기.
  - 파일 쓰기는 GGAI/ 밖 개인 폴더이므로 StellaStore 경유 대상이 아니다(개인 플러그인 자기 책임).

## 5. 집중 디테일뷰

- 문제: 디테일 뷰는 설정이 많아 작업 중 원하는 항목을 찾기 불편.
- 설계: **핀 카탈로그** 방식. 공용 컨트롤 킷의 각 컨트롤(모델 픽커, 파라미터 슬라이더, 프롬프트 세트 픽커, 번역/삽화/요약 토글, 로어북 픽커 등)을 "핀 가능한 항목"으로 카탈로그화하고, 집중 뷰는 **저자가 고른 항목만 고른 순서로** 세로 나열해 그린다.
- 배치: 우측 사이드바의 별도 뷰 타입(디테일 뷰와 같은 위치 후보군, 워크스페이스에서 자유 배치). PRO 활성화 시에만 등록.
- 저장: 핀 목록/순서 = `PluginData` 의 PRO 전용 칸(전역). 항목 추가/제거/순서는 집중 뷰 안에서 편집.
- 구현 원칙: 기존 디테일 섹션을 뜯지 않는다. 컨트롤 킷 함수를 재호출할 뿐, 렌더러를 복제하지 않는다. 킷으로 안 그려진 항목이 핀 대상으로 필요하면 그 컨트롤을 먼저 킷에 편입한 뒤 핀한다.

## 6. 메모 보드 — 엔진에 넣지 않는다 (개인 플러그인 소관)

- 메모 보드(아이디어 카드 보드)는 **오픈소스 스텔라에 코드·상세 설계를 두지 않는다.** 이 문서에는 경계만 기록한다.
- 엔진에 추가되는 것: **없음.** 개인 플러그인이 기존 공개 이음새만으로 자체 구현한다 —
  - AI 호출: GGAI Core API 직접 (스텔라 경유 아님).
  - 데이터: vault 파일 직접 읽기(세션/로어북 = json, 메모 = 자기 폴더 .md). PRO 세션의 한국어판 원고만 `plugin.pro.getManuscript` 재사용.
  - 자동 트리거: 공개 확장 레지스트리(`plugin.extensions.register` 의 `onGenerationComplete`).
- 상세 설계는 개인 플러그인 레포 문서(비공개)에 둔다.

## 7. 타임라인 정리 툴

### 7.1 데이터
- **사건 카드** = 시나리오별 .md (`GGAI/SCENARIOS/<이름>/TIMELINE/<slug>.md`):
  frontmatter(`time`(작중 시간, 정렬 가능한 표기 규약), `characters[]`, `place`(위치 경로 문자열, 예 `a대륙/a국가/a도시/a저택`), `importance`, `sessionId`, `nodeId?`, `tags`) + 본문(행적 요약, 중요 정보/중요 대사 인용).
- **공간 트리** = 시나리오별 단일 파일 `TIMELINE/locations.md` — 중첩 목록(들여쓰기)으로 상위→하위 (대륙[국가[도시[저택/시장/도서관]]]). 사람이 직접 고치기 쉬운 형식 + 파싱 단순. 카드의 `place` 는 이 트리의 경로를 가리킨다(없는 경로는 뷰어가 미등록 표시).
- 둘 다 store 엔티티로 편입(사건 카드/공간 트리 읽기·쓰기 + `timeline-changed`).

### 7.2 AI 유지보수
- 로어북 자동 생성과 같은 **앵커 방식**: 생성 완료 훅에서 마지막 앵커 이후 새 본문을 스캔 → 새 사건 카드 제안 / 기존 카드 갱신(시간·위치 확정, 인물 추가). 중복 방지를 위해 기존 카드 제목+시간+장소 목록을 `{{lorebook}}` 자리처럼 첨부.
- 지시문 = 편집 가능한 `timelineExtract` 버킷. 주기/모델/첨부량 설정.
- 수동: 뷰어에서 카드 추가/수정 + [지금 스캔].

### 7.3 뷰어
- 엔진 내 커스텀 뷰(HTML 렌더): 가로축 = 작중 시간, 행 = 인물(또는 장소 그룹), 카드 클릭 = 상세/편집. 장소는 트리 경로 브레드크럼으로 표시해 거리감·위치감을 시각화.
- 목적: 시간/공간/인물 모순 방지. 뷰어는 표시 계층일 뿐 — md 데이터만으로 외부 툴이 재구현 가능해야 한다(미디어 분리 원칙과 동일).

### 7.4 컨텍스트 주입
- `contributeContext` 확장 슬롯으로 "현재 시점 근방 + 등장 중 인물 관련" 카드를 골라 주입. 선택 로직은 1차 = 규칙 기반(최근 시간순 + 활성 인물 매칭), 2차 = AI 선별(로어북 AI 선별 패턴 재사용) 검토.
- **요약 기능과의 관계는 보류**(§10): 초기에는 공존(요약 = 서사 압축, 타임라인 = 사실 대장). 실사용 후 타임라인이 요약을 대체할 수 있는지 판단.

## 8. 번역 품질 로어북 자동 제작

- 목적: 쌍방향 변환 품질 — 고유명사 표기, 캐릭터 말투, 반복 표현의 일관성.
- 원천 데이터: §3.5 의 문단 쌍(특히 `authored` 쌍 = 저자가 승인한 정답 표기).
- 동작: 로어북 자동 생성과 같은 앵커/주기 메커니즘으로 새 문단 쌍을 스캔 → "영↔한 표기 대응, 말투 특징" 항목 제안 → **시나리오 공유 번역 로어북**(`stella.translationLorebookIds` 의 지정 북)에 중복 없이 추가. 기존 로어북 자동 생성과 별개 실행(대상 북·버킷·지시문 분리, 버킷 `translationGlossary`).
- 이 로어북은 이미 번역 실행 경로(로어북 허브)에 합류하므로 추가 배선 없이 양방향 변환 품질에 반영된다. `proConvert` 호출도 같은 허브를 지나게 한다.

## 9. 재사용 원칙 (PRO 밖 활용 대비)

- 순수 로직(변환 세그먼트 조립/파싱, 메모 JSON 파싱, 타임라인 스캔/중복 제거, 공간 트리 파싱)은 전부 `util/` 순수 함수로 — 뷰·PRO 게이트 비의존.
- 서비스(타임라인 유지보수, 변환 파이프라인)는 PRO 게이트 **바깥**에 두고, 등록(뷰/명령/훅)만 게이트 안에 둔다. 메모 보드는 엔진 밖(§6). 나중에 일반 개방할 후보: 타임라인, 집중 디테일뷰 — 개방 시 `builtinExtensions` 편입만으로 가능해야 한다.
- 미디어 프롬프트 버킷 신설: `proConvert` / `timelineExtract` / `translationGlossary`. 전부 기본 프롬프트 제공 + 편집 가능, 숨은 지시문 없음(엔진 고정 IO 규약만 결합 표시).

## 10. 보류 (지금 결정하지 않음)

- **md 양방향 동기화**: 옵시디언 일반 에디터에서 초고 수정 → 세션 역반영. §3 프리미티브가 "임의 구간 교체"라 기술적 토대는 마련됨. 실사용 후 필요하면 별도 단계로.
- **타임라인의 요약 대체**: 공존으로 시작, 실물 보고 판단.
- **일반 배포 개방**: PRO 기능 중 일반 사용자에게 열 것 선별 — 전부 보류.

## 11. 구현 순서

각 단계는 빌드→하네스→수동 검증까지 끝내고 다음으로 (국소 완성 원칙).

```
P0  기반: meta.proWriting + session-host 분기 + plugin.pro.activate() 휴면 게이트
    + PRO 설정 패널 뼈대(집필 뷰 안 전용 표면). 완료 기준: 활성화 전 표면 0개,
    활성화 후 집필 세션 생성/열기 가능(내용물은 아직 소설 뷰와 동일).
P1  이중 원고 파이프라인: convertAndSplice 프리미티브 + 전송 흐름 + authored 페어링
    + 한국어 기본 화면. 완료 기준: 한국어로만 쓰고 읽는 왕복이 성립, 내 문장이
    글자 그대로 보존, 영어판 토글 확인 가능.
P2  편집 반영 + 문체 참조: 한국어 편집 디바운스 반영(통째 교체 포함) + 양방향
    문단 쌍 예시 첨부 + 첨부량 설정.
P3  마크다운 미러: getManuscript API → 개인 플러그인 v1 (활성화/명령/미러 정책/설정).
P4  집중 디테일뷰: 핀 카탈로그 + 집중 뷰.
P5  메모 보드 (개인 플러그인 트랙, 엔진 변경 없음 — 상세는 개인 레포 문서.
    P3 의 개인 플러그인 골격이 선행 조건).
P6  번역 품질 로어북: 문단 쌍 스캔 → 시나리오 번역 로어북 자동 추가.
P7  타임라인: 데이터/store → AI 유지보수 → 뷰어 → 컨텍스트 주입.
```

P4~P7 은 독립적이라 실사용 체감에 따라 순서 조정 가능. P1~P2 가 심장이므로 먼저 완성한다.
