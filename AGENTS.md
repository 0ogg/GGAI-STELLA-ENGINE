# AGENTS.md — Codex 진입점

> 이 레포의 작업 규칙은 **`CLAUDE.md` 가 단일 진실 소스**다. 과거에는 이 파일이 규칙 사본을 따로 들고 있었지만, 두 문서가 어긋나는 문제로 폐기했다 — 이 파일은 얇은 진입 안내만 남긴다. 규칙을 이 파일에 다시 복붙하지 않는다.

## 작업 시작 순서

1. **`CLAUDE.md` 를 읽는다** — 프로젝트 개요, 작업 원칙 1~10, 로드맵, 데이터 형식, 빌드 루틴 전부 그 파일에 있다.
2. **코드를 수정하기 전 `회귀금지.md` 에서 만지는 영역의 항목을 확인한다** — 실제 재발 사고에서 나온 "하지 마라" 목록.
3. **기존 기능을 수정/확장할 때는 `구현현황.md` 의 해당 도메인 섹션을 읽는다** — 진입점·배선·확정 결정.

## 문서 갱신 규칙

- 프로젝트 규칙이 바뀌면 `CLAUDE.md` 를 갱신한다 (이 파일은 손대지 않는다 — 규칙 본문이 없으므로 동기화 대상도 아니다).
- 사고 교훈은 `회귀금지.md`, 구현 상태 변화는 `구현현황.md` 에 기록한다.
- 어떤 문서에도 날짜 붙은 서사·변경 이력을 쌓지 않는다 (git 이 이력을 담당).
