# 제작 도구 스펙 — 어시스턴트 · 도구 · 작업 이력

> **상태**: 설계 확정. 슬라이스 1 미착수.
> 「대화로 시나리오·로어북·프롬프트·페르소나를 만들고 고치는」 트랙의 단일 진실 소스.
> 로드맵의 **M1/M2 Built-in Agents** 자리를 이 문서가 대체한다.
>
> **착수 전 필독.** 특히 §3 도구 목록 — 도구 이름과 인자는 AI 와 맺는 계약이라,
> 한 번 확정하고 나면 프롬프트·카드·다른 플러그인이 그 이름에 기대게 된다. 함부로 바꾸지 않는다.

---

## 0. 무엇을 만드는가

사용자가 어시스턴트와 **대화하다가** 시나리오·로어북·프롬프트 세트·페르소나가
**실제 vault 파일로** 생긴다. 기존 자료를 고치는 것도 같은 대화에서 한다.

- 어시스턴트의 성격은 **캐릭터카드가 결정한다** — 짱돌이 같은 제작 어시스턴트 카드를
  임포트해서 씌운다. 우리는 성격을 코드에 넣지 않는다.
- 승인 절차 없이 **바로 적용**한다. 대신 AI 가 건드린 모든 변경을 **작업 이력**에
  남겨 원하는 시점으로 되돌리고, 특정 시점을 파일로 내보내거나 가져올 수 있다 (§5).

---

## 1. 왜 SillyTavern 방식이 아닌가 (설계 근거)

참고한 원본: 짱돌이 3.0.1 카드 + QR 세트 + 어시스트 프롬프트 1.0.

ST 의 흐름은 이렇다 — QR 이 `/inject` 로 지시를 밀어넣고 `/trigger` 로 생성시킨다 →
AI 가 코드펜스 안에 카드 JSON 을 **텍스트로 그린다** → `/re-replace` 로
`USERTOKEN`→`{{user}}`, `CHARTOKEN`→`{{char}}` 치환 → `/download` 로 파일을 뱉는다.

**ST 가 파일을 만들 수 없어서 나온 우회다.** 우리는 플러그인이므로 그럴 필요가 없다.
GGAI Core 의 `agent()` + `registerTool()` 을 쓰면 AI 가 도구를 호출하고, 도구가
`StellaStore` 를 불러 파일을 직접 만든다.

여기서 따라 나오는 결정 두 개:

1. **토큰 치환 트릭을 쓰지 않는다.** 도구 인자는 매크로 치환 파이프를 타지 않으므로
   AI 는 `{{user}}` / `{{char}}` 를 **그대로** 적으면 된다.
   다만 임포트한 카드가 AI 에게 `USERTOKEN` 습관을 가르치므로, 검사대(§4)가
   `USERTOKEN`/`CHARTOKEN` 을 발견하면 각각 `{{user}}`/`{{char}}` 로 자동 치환한다.
2. **AI 가 그린 코드펜스를 우리가 파싱하는 경로는 만들지 않는다.** 결과물은 오직
   도구 인자(구조화된 JSON)로만 받는다. 자유 텍스트 파싱은 깨지는 지점이라 쓰지 않는다.

---

## 2. 어디서 쓰는가 — 어시스턴트

### 2.1 어시스턴트 = 챗 세션 + 도구

**새 대화 엔진을 만들지 않는다.** `mode: "chat"` 그대로 두고 `chat-session-view` 를
재사용한다. 집필 프로(PRO)의 선례와 같은 방식 — mode 는 본문 구조(소설/채팅)를
가리키는 값이므로 건드리지 않고, 플래그로만 구분한다.

### 2.2 분류 — 어시스턴트는 따로 관리한다

| 대상 | 표식 | 없으면 |
|---|---|---|
| 시나리오 | `data.extensions.stella.kind: "assistant"` | `"story"` 로 간주 (기존 전부) |
| 세션 | `meta.assistant` 객체 존재 | 일반 챗 세션 |

- **대시보드에 「어시스턴트」 탭 신설** (9번째, 빠른 답장 다음).
- 어시스턴트 시나리오·세션은 **시나리오 탭 / 세션 탭 / 좌측 사이드바 목록에서 빠진다.**
  일반 놀이 자료와 섞이지 않는다.
- 시나리오 편집기에 「어시스턴트로 사용」 토글 하나. 켜면 어시스턴트 탭으로 옮겨간다.

**폴더는 `GGAI/SCENARIOS/` 를 그대로 쓴다.** 새 최상위 폴더를 만들면 store 의
시나리오 경로·세션·미디어 로직을 통째로 복제해야 하고, 어시스턴트도 결국 캐릭터카드다.
분류는 플래그로 충분하다.

### 2.3 세션에 붙는 설정

```
meta.assistant?: {
  enabled: boolean;                        // 도구 사용 on/off
  groups: ("read" | "create" | "edit")[];  // 켠 능력 묶음
  maxTurns: number;                        // AI 가 도구를 돌 수 있는 최대 턴
}
```

기본값: `enabled: true`, `groups: ["read", "create"]`, `maxTurns: 12`.
`edit` 는 사용자가 직접 켠다 — 기존 자료를 고치는 권한이라 기본 꺼짐.

### 2.4 같은 도구, 다른 입구

도구는 `onload()` 에서 `api.registerTool("ggai-stella-engine", ...)` 로 Core 에 등록한다.
그러면 `pluginIds: ["ggai-stella-engine"]` 를 넘기는 **누구든** 같은 도구를 쓴다:

- 어시스턴트 세션 (§2.1) — 1차 소비자
- 대시보드 「컨셉으로 한 번에 만들기」 (슬라이스 4)
- Core 어시스턴트나 다른 플러그인 — 배선 없이 그대로 사용 가능
- QR 에서 호출 — **이 문서에서 정하지 않는다.** 필요해지면 그때 `QR 스펙.md` 에 추가.

`onunload()` 에서 반드시 unregister.

---

## 3. 도구 목록 (계약)

이름은 `stella_` 접두어로 통일 — 다른 플러그인 도구와 한 목록에 섞여도 구분된다.

**공통 규칙**

- 대상은 **경로가 아니라 id** 로 지정한다. 파일 이름이 바뀌어도 살아남는다.
- 인자는 **최소한만 받는다.** 로어북 항목은 필드가 25개지만 AI 에게는 5개만 묻고,
  나머지는 `defaultLorebookEntry()` 가 채운다. 안 그러면 AI 가 필드를 지어내고
  토큰만 먹는다.
- 실패는 throw 하지 않고 `{ ok: false, errors: [...] }` 를 사람이 읽을 문장으로 돌려준다
  — AI 가 그걸 읽고 스스로 고쳐 다시 부른다.
- 모든 쓰기는 **store 경유**. 도구가 `vault.create`/`modify` 를 직접 부르지 않는다.

### 3.1 읽기 (`read`)

| 도구 | 인자 | 돌려주는 것 |
|---|---|---|
| `stella_list_scenarios` | — | `{id, name, kind}[]` |
| `stella_list_lorebooks` | — | `{id, name, entryCount}[]` |
| `stella_list_prompt_sets` | — | `{id, name, itemCount}[]` |
| `stella_list_personas` | — | `{id, name}[]` |
| `stella_read_scenario` | `{scenarioId}` | 카드 필드 전체 + 연결된 로어북 id |
| `stella_read_lorebook` | `{lorebookId, entryUids?}` | 항목 전체 또는 지정한 것만 |
| `stella_search_lorebook` | `{query, lorebookId?}` | 매칭 항목 요약 (본문은 잘라서) |
| `stella_read_prompt_set` | `{promptSetId}` | 항목 배열 (identifier/name/role/enabled/순서) |
| `stella_read_persona` | `{personaId}` | 페르소나 전체 |

### 3.2 만들기 (`create`)

| 도구 | 인자 | 돌려주는 것 |
|---|---|---|
| `stella_create_scenario` | `{name, description, personality?, scenario?, first_mes?, mes_example?, alternate_greetings?, tags?, creator_notes?, character_version?, lorebook?}` | `{scenarioId}` |
| `stella_create_lorebook` | `{name, entries[]}` | `{lorebookId}` |
| `stella_create_prompt_set` | `{name, items[]}` | `{promptSetId}` |
| `stella_copy_prompt_set` | `{sourcePromptSetId, newName}` | `{promptSetId}` |
| `stella_create_persona` | `{name, description}` | `{personaId}` |

`stella_create_scenario` 의 `lorebook` 은 카드 전용 로어북을 함께 만들어
`stella.defaultLorebookId` 에 연결한다 (캐릭터카드 임포트와 같은 동작).

**로어북 항목 최소 shape**: `{name, keys[], content, constant?, position?, order?}`.
나머지는 기본값. `uid` 는 우리가 발급한다 (AI 가 정하지 않는다).

**프롬프트 항목 최소 shape**: 텍스트는 `{kind:"text", name, role, content, enabled?}`,
마커는 `{kind:"marker", identifier}`. `identifier`/`id` 는 우리가 발급하되,
ST 표준 identifier(`main`/`nsfw`/`jailbreak` 등)를 AI 가 지정하면 존중한다.

### 3.3 고치기 (`edit`)

| 도구 | 인자 |
|---|---|
| `stella_update_scenario_fields` | `{scenarioId, fields{…일부}}` |
| `stella_add_lorebook_entries` | `{lorebookId, entries[]}` |
| `stella_update_lorebook_entry` | `{lorebookId, entryUid, patch{…일부}}` |
| `stella_remove_lorebook_entry` | `{lorebookId, entryUid}` |
| `stella_add_prompt_item` | `{promptSetId, item, afterIdentifier?}` |
| `stella_update_prompt_item` | `{promptSetId, identifier, patch{…일부}}` |
| `stella_reorder_prompt_items` | `{promptSetId, identifiers[]}` |
| `stella_attach_lorebook` | `{scenarioId, lorebookId, slot:"default"\|"extra"}` |
| `stella_update_persona` | `{personaId, patch}` |

패치는 **준 필드만 바꾸고 나머지는 그대로 둔다.** 통째로 덮어쓰는 도구는 없다.

### 3.4 만들지 않는 도구

- **파일 삭제 전부** — 시나리오/로어북/프롬프트/페르소나/세션. 지우는 건 사람이 한다.
  (로어북 *항목* 제거는 있다 — 이력으로 되돌릴 수 있고, 「특정 요소 수정」의 일부다.)
- **이름 바꾸기** — 경로가 바뀌면 열려 있는 뷰·참조가 흔들린다. 필요해지면 그때.
- **세션 본문 쓰기** — 어시스턴트가 놀이 세션의 원고를 건드리지 않는다.
- **설정/프리셋 변경** — 활성 모델·파라미터는 사용자 손에 둔다.

---

## 4. 검사대 (하네스)

쓰기 도구는 store 를 부르기 **전에** 순수 검증 함수를 통과해야 한다.

```
validateX(input) -> { ok: true, value: 정규화된값 } | { ok: false, errors: string[] }
```

- **순수 함수 + JSON 직렬화 가능 입출력.** vault 를 모른다. `tests/` 에서 단위 테스트한다.
- 하는 일: 필수 필드 확인 → 기본값 채우기(`defaultLorebookEntry()` 등 기존 팩토리 재사용)
  → id/uid 발급 → 토큰 치환(§1) → 길이·개수 상한 확인.
- 실패하면 파일을 **건드리지 않고** 사람이 읽을 오류 문장을 AI 에게 돌려준다.
  예: `"entries[2].keys 가 비어 있습니다. 이 항목이 언제 활성화될지 정할 키워드가 필요합니다."`
  AI 는 그걸 읽고 고쳐서 다시 부른다. **깨진 JSON 이 vault 에 남는 경로가 없다.**
- 임포트 경로의 정규화 로직과 같은 코드를 쓴다 — 두 벌 만들지 않는다.

---

## 5. 작업 이력 · 되돌리기 · 시점 내보내기

승인을 안 받는 대신 여기가 안전망이다.

### 5.1 저장 위치

```
GGAI/HISTORY/
├── journal.json          # 작업 목록 (가벼움 — 목록 렌더용)
└── snapshots/<opId>.json # 그 작업이 건드린 파일들의 before/after 전체 사본
```

`journal.json` 한 줄:

```
{
  id, at,                      // 작업 id / 시각
  source: { kind: "assistant" | "dashboard" | "external", sessionFile?, pluginId? },
  tool,                        // 도구 이름
  summary,                     // 사람이 읽는 한 줄: "로어북 '왕국사'에 항목 3개 추가"
  targets: [{ kind, id, name }],
  snapshot: true | false       // 다이어트로 스냅샷이 지워졌으면 false (되돌리기 불가)
}
```

`snapshots/<opId>.json` = `[{ path, kind, before: JSON|null, after: JSON|null }]`
(`before: null` = 신규 생성, `after: null` = 삭제 — 현재는 로어북 항목 제거뿐).

### 5.2 기록 지점

**도구 계층이 감싼다.** 쓰기 도구를 부를 때: 대상 파일을 읽어 `before` 보관 →
store 호출 → 다시 읽어 `after` 보관 → 저널에 한 줄 추가.
store 내부를 건드리지 않으므로 **사람이 직접 편집한 것은 이력에 안 들어간다** —
의도한 대로다. 이건 "AI 작업 되돌리기"지 vault 전체 버전 관리가 아니다.
사람이 손으로 고친 건 옵시디언의 파일 복구가 맡는다.

### 5.3 되돌리기

- **한 건 되돌리기**: 그 작업의 `before` 로 복원.
  뒤에 같은 파일을 건드린 작업이 있으면 알린다 — *"이후 작업 3건이 같은 파일을
  고쳤습니다. 함께 되돌립니다."* (부분 되돌리기는 만들지 않는다. 결과가 예측 불가다.)
- **시점으로 되돌리기**: 고른 지점 이후 전부 **역순**으로 복원.
- **되돌리기도 저널에 남는다** (`tool: "revert"`). 되돌리기를 되돌릴 수 있다.
- 복원도 **store 경유**. 열려 있는 뷰는 store 이벤트로 국소 갱신된다.

### 5.4 시점 내보내기 / 가져오기

- **내보내기**: 고른 지점에서 그 작업이 건드린 파일들의 사본을 한 파일로.
  `GGAI/DOWNLOADS/<이름>.stella-changeset.json`
  = `{ createdAt, label, files: [{ kind, name, json }] }`
  → 그 시점의 결과물을 남에게 주거나 다른 vault 로 옮길 수 있다.
- **가져오기**: 이 파일을 읽어 현재 vault 에 적용. 이름이 겹치면 **새로 만들기 /
  덮어쓰기**를 묻는다. 덮어쓰기도 저널에 남으므로 되돌릴 수 있다.

### 5.5 다이어트

이력은 무한정 쌓이면 안 된다.

- 기본 **최근 200건**의 스냅샷만 보관. 초과분은 오래된 것부터 스냅샷 파일만 지우고
  저널 줄은 남긴다 (`snapshot: false` — 목록에는 보이되 되돌리기 불가).
- 개수는 설정에서 조절.

### 5.6 UI

어시스턴트 탭 안의 「작업 이력」 페이지 하나. 목록(시각·요약·출처) + 각 줄에
`되돌리기` / `여기까지 되돌리기` / `이 시점 내보내기`. 새 뷰 타입을 만들지 않는다.

---

## 6. 제약 (사용자에게 미리 알릴 것)

- **못 쓰는 프로필은 NovelAI 하나뿐이다.** Anthropic / OpenAI / OpenAI 호환 / Google /
  Vertex 는 전부 도구를 지원한다 (Vertex 는 `vertex.ts` 가 주소·인증만 맡고 요청 본문은
  `google.ts` 의 `runGeminiChat` 을 공유하므로 Gemini 와 동일하게 동작한다 — 파일에
  "tools" 문자열이 없다고 미지원으로 판단하지 말 것).
  NovelAI 는 대화 API 가 아니라 글 이어쓰기 전용(Core `supports = { text, image }`)이라
  주고받는 도구 호출 구조 자체가 성립하지 않는다.
  **OpenAI 호환은 연결한 모델에 달렸다** — 엔드포인트가 함수 호출을 받아주지 않으면
  실패한다. 이 경우는 미리 알 수 없으니 실패 메시지를 그대로 보여준다.
  어시스턴트 세션에서 NovelAI 프로필이 골라져 있으면 도구 스위치 옆에 안내를 띄우고
  도구 없이 대화만 되게 한다 (조용히 실패하지 않는다).
- **토큰을 평소보다 쓴다.** 한 번 요청에 AI 가 여러 턴을 돈다. `maxTurns` 로 상한을 둔다.
- **진행 토스트는 Core 가 띄운다.** 우리는 `label` 만 채운다
  (예: `"제작 어시스턴트 (짱돌이)"`). 우리가 또 띄우면 토스트가 두 개 뜬다.
- 도구가 여러 파일을 만드는 도중 실패하면 **거기서 멈춘다.** 이미 만든 것은 남고
  이력에 기록돼 있으므로 되돌릴 수 있다. 중간 롤백을 하지 않는다.

---

## 7. 슬라이스

각 슬라이스는 빌드 → 하네스 → 테스트 vault 수동 확인까지 끝나야 다음으로 간다.

| | 내용 | 끝나면 되는 것 |
|---|---|---|
| **S1** | 도구 계층 + `registerTool` 배선 + 읽기 도구 9종 + 어시스턴트 탭/표식 + 세션 도구 스위치 | 짱돌이를 임포트해 어시스턴트 탭에서 대화, *"내 로어북 뭐 있어?"* 가 실제로 대답됨. **쓰기 없음 = 안전** |
| **S2** | 만들기 도구 5종 + 검사대 + 저널 기록 | 대화로 시나리오·로어북·프롬프트·페르소나가 실제로 생김. 결과 카드에 「열기」 |
| **S3** | 고치기 도구 9종 + 작업 이력 페이지 + 되돌리기 | 기존 자료를 대화로 수정, 마음에 안 들면 되돌림 |
| **S4** | 시점 내보내기/가져오기 + 다이어트 + 대시보드 「컨셉으로 한 번에 만들기」 | 컨셉 텍스트 한 덩어리 → 묶음 생성, 결과를 파일로 공유 |

---

## 8. 원본과 다르게 한 점 / 지원하지 않는 것

| 항목 | ST(짱돌이) | 우리 |
|---|---|---|
| 결과물 전달 | 코드펜스 텍스트 → `/download` | 도구가 vault 파일을 직접 생성 |
| 매크로 보호 | `USERTOKEN`/`CHARTOKEN` 치환 | 불필요. `{{user}}`/`{{char}}` 직접 사용 (검사대가 옛 토큰을 자동 치환) |
| 어시스턴트 성격 | 캐릭터카드 | 같음 — 카드를 임포트해 씌운다 |
| QR 버튼 | `/inject` + `/trigger` | 그대로 동작 (이미 구현됨). 마지막 내보내기 단계만 도구 호출로 바뀐다 |
| 실행 취소 | 없음 | 작업 이력 + 되돌리기 + 시점 내보내기 |
| 삭제 | `/download` 뿐이라 해당 없음 | 파일 삭제 도구를 **만들지 않음** |

**지원하지 않기로 확정**: 파일 삭제·이름 바꾸기 도구, 세션 본문 쓰기,
AI 자유 텍스트에서 JSON 긁어오기, QR 에서의 도구 호출(필요해지면 별도 결정).
