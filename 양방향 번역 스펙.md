# 양방향 번역 스펙

> 집필 프로의 이중 원고 파이프라인(사용자 언어 ↔ 원문 원장)을 번역 확장의 공개 기능 **'양방향 번역'** 으로 이관하는 확정 설계 + **반영 대기함** 구조. 관련 작업 전 필독.
> 상태: **슬라이스 1~3, 5 + 4 일부 구현 완료** (대기함 · 출력 언어=원고 언어 · 토글/도움말/게이트 · 설정 공유/용어집 연동 · 챗 입력란+말풍선 수정 · 계획 자동 복구/반영 대기 취소 §2.7). 남음 = 슬라이스 4 잔여(반영 실패 문단 배지·수동 재시도).

## 0. 배경과 대전제

- **원장(원문 세션 노드) = 생성의 진실 소스**라는 대전제는 불변 — 원장이 있는 이유는 그것으로 다음 내용을 생성하기 위해서다.
- 사용자 언어 쪽(한국어판)은 "번역본"이 아니라 **저자 원고**다: 쓰거나 고친 내용은 어떤 실패(변환 실패·재렌더·재시작·분기 이동)에도 유실·회귀되지 않아야 한다.
- 기존 문제: "영어판 반영 대기" 상태가 파일 어디에도 없고, 화면 상태 + variant 종류 추론(active kind === user-edit)으로만 존재했다. 추론이 어긋나는 지점마다 수정이 조용히 사라졌고, 재생성은 반영 없이 출발했다.

## 1. 반영 대기함 (슬라이스 1 — 구현 완료)

**대기 상태를 파일 실체로 만든다.** `translations.json`:

- `pendingReflections` — key = 대상 문단 원문(영어) 해시, 값 = `{ ko, en, createdAt, updatedAt }`. "수정했지만 아직 원장에 반영 안 된 것"의 단일 진실 소스.
- `proDraft` — 본문 끝 이어쓰기 초고(한국어). 앱 재시작에도 살아남고, 반영 성공 시 소비된 프리픽스만큼 잘린다.

동작 규칙:

- **쓰기**: 한국어 문단 수정 커밋·부분 삭제 수정본 → `upsertPendingReflection`(같은 문단 재수정은 갱신, createdAt 유지). 초고 입력 → `proDraft` 동기화(플러시 디바운스, 소스는 DOM 이 아니라 뷰의 draft 값).
- **판별/수집**: 대기 문단 = 대기함 1순위. 대기함 도입 전에 남은 수정(active user-edit)은 폴백 추론으로 계속 잡는다(자동 흡수). 변환에 보낼 한국어도 대기함 우선.
- **정리는 성공 시에만**: `convertAndSplice` 성공 → 반영된 해시 제거 + `proDraft` 소비 프리픽스 잘라냄. 구조 삭제로 사라진 문단의 대기 건도 제거(유령 방지). **실패 시 대기함 불변** = 재시도 보장, "조용한 유실" 소멸.
- **재생성 예외 (구조적 이유)**: 재생성은 반영을 기다리지 않는다 — patch 노드는 항상 activeLeaf 밑에 붙으므로, 반영 노드를 만들면 activeLeaf 가 AI 노드가 아니게 되어 재생성 자체가 불가능해진다. 대신 대기함 덕에 수정은 유실되지 않고 **재생성 직후 자동 반영**된다(재렌더 → 대기 재예약). 이어쓰기/미리보기는 기존대로 반영 완료 후에만 출발.

한계(알고 두기):

- 문단 식별이 내용 해시라 같은 내용의 문단 두 개는 대기 건을 공유한다(기존 번역 메모리와 동일한 성질).
- 대기함이 남은 채 분기를 옮기면 그 건은 휴면 — 문단이 활성 경로에 다시 나타날 때 반영이 재개된다.

## 1.2 반영 시점 — 사용자가 생성을 요청할 때만 (구현 완료, 소설)

**쓰는 동안에는 AI 를 부르지 않는다.** 예전엔 타이핑이 3초 멈추면 자동으로 집필 변환이 돌았는데, 사용자에게는 "내가 시키지도 않은 순간에 AI 가 돌고, 그 결과로 화면이 다시 그려지며 초고 입력란이 비워지는" 것으로 보였다 — 보기 전환·재렌더처럼 글쓰기와 무관한 동작에도 터져서 **발동 조건을 예측할 수 없었다**(2026-08-01 사용자 제보: "대체 조건이 뭔지… 에러가 난 것 같은데 내가 잘못 쓰고 있나?").

- 규칙: 초고·문단 수정은 **대기함에만 쌓인다**(파일 실체라 재시작·분기 이동에도 안전). 원고 반영은 **이어쓰기(생성)·미리보기 flush** 에서 `runProConvert()` 한 번으로 전부 처리한다.
- 부수 효과(의도한 것): 쓰다 만 문장으로 요청이 새어 나가지 않고, 한 번의 호출로 묶여 오요청·중복 호출이 사라지며, 타이핑 중 재렌더가 없어 커서·조합이 끊기지 않는다.
- 화면 안내가 곧 규칙이다: 초고 영역 placeholder 와 '양방향 번역' ? 도움말이 "이어쓰기를 누를 때 반영"이라고 말한다. 반영 시점을 바꾸면 이 문구도 같이 바꾼다.
- 대기 표시는 유지: 수정 문단 `.ggai-tr-propending-block`, 초고 영역 강조색 — "아직 원고에 안 들어간 것"을 눈으로 구분.
- 챗(말풍선 수정)은 아직 디바운스 반영(§2.5) — 소설과 같은 규칙으로 맞출지는 실사용 후 결정.

## 1.5 출력 언어 규칙 — 원고 언어를 따른다 (구현 완료)

**집필 변환의 출력 언어를 설정값이나 지침의 하드코딩으로 정하지 않는다. 원장(context 세그먼트 = 기존 원고)이 곧 언어의 진실 소스다.**

- 이유: 기본 프롬프트와 엔진 IO 규약이 "English manuscript / composed in English"로 영어를 못박고 있어서, 원고가 영어가 아닌 작품에서도 영어가 튀어나왔다. 언어는 작품마다 다르므로 설정 항목이 아니라 **원고에서 읽어내는 값**이어야 한다.
- 규칙: `PRO_CONVERT_IO_INSTRUCTIONS` 와 `proConvert`/`translationGlossary` 기본 프롬프트는 특정 언어명을 쓰지 않는다 — "context 가 원고의 언어를 정의하며 출력은 그 언어로"라고만 지시한다. context 가 없으면(원고가 비어 언어 미확립) 저자 초고의 언어를 그대로 쓴다.
- 문단 쌍의 `en`/`ko` 는 **데이터 키 이름일 뿐 언어 선언이 아니다**(`en` = 원고 쪽, `ko` = 저자 초고 쪽). 프롬프트 문구도 그렇게 설명한다.
- 하네스 가드: 문체 예시 블록과 IO 규약에 `English`/`Korean` 문자열이 없어야 한다(하드코딩 회귀 방지).
- 주의: 사용자가 기본 프롬프트를 복사·수정해 **자기 사본**을 쓰고 있으면 그 사본이 우선한다(`resolveMediaPrompt` 의 id 오버라이드) — 사본 쪽 영어 못박기는 사용자가 직접 고쳐야 한다.
- 범위 밖: `translation` 버킷 기본 프롬프트(원문→한국어 읽기용)와 `paragraphRegen` 의 "한영 번역" 프리셋은 **의도적으로 언어를 지정한 것**이라 그대로 둔다.

## 2. '양방향 번역' 이관 (슬라이스 2~ — 미착수)

- **위치**: 번역 확장 설정 패널에 **'양방향 번역' 토글** 신설(소설 세션 전용). 켜면 그 세션의 번역 보기가 집필 모드처럼 동작 — 번역 화면에서 직접 쓰기/수정 → 원장(원문) 자동 반영, 대기 문단 모델 전체.
- **도움말**: 토글 옆 **동그란 물음표(?) 버튼** — 누르면 Notice(토스트)로 기능 설명("번역 화면에서 직접 쓰고 고치면 원문에 자동 반영됩니다" 류). 도움말 버튼은 공용 컨트롤 킷에 패턴으로 추가해 재사용.
- **용어집 연동**: 양방향 번역을 켜면 **번역 용어집 자동 수집**(현 PRO P6)도 함께 활성(기본값, 끌 수 있음). 관련 설정(주기/모델/프롬프트/[지금 스캔])도 번역 패널의 양방향 섹션으로 노출.
- **집필 변환 설정**(모델/프롬프트/문체 꼬리 글자 수/문체 예시 쌍 수)도 같은 섹션으로 — 집필 프로 패널과 같은 ActiveSettings 를 공유(중복 저장 금지).
- **PRO 관계**: 집필 프로 세션은 양방향 번역을 기본 켠 소비자가 된다. PRO 휴면 원칙은 PRO 고유 표면(집중 디테일뷰·마크다운 미러·용어집 외 기능)에만 남는다.
- **게이트**: 뷰의 `proWritingMode()` 게이트를 "양방향 번역 활성" 게이트로 일반화. **챗 세션도 대상에 포함**(아래 §2.5).

## 2.5 챗 모드 입력란 (슬라이스 5)

**양방향 번역을 켜면 챗 세션의 입력란도 같은 원리로 동작한다** — 저자는 자기 언어로 치고, 세션에 저장되는 원장 메시지는 스토리 언어가 된다.

- 흐름: 입력 → 전송 시 집필 변환(같은 `proConvert` 프롬프트·용어집·문체 예시) → **변환 성공분만** 유저 메시지 노드로 저장 → 입력 원문은 그 메시지의 `authored` 짝으로 보존(번역 보기에서 내가 친 그대로 보임).
- **새 메시지(전송)만 챗 전용 경로**: 끝 append 는 마지막 메시지에 텍스트를 덧붙일 뿐 새 메시지 노드가 되지 않는다("노드 1개 = 메시지 1개"). 그래서 전송은 `convertChatMessage`(변환+짝 저장만, 노드는 뷰가 append)로 처리하고, `convertAndSplice` 는 챗에서 **끝 append op 를 거부**한다.
- **실패 시**: 메시지를 보내지 않고 **입력란에 원문을 그대로 유지** + 안내(소설의 "초고 대기"에 대응). 전송이 조용히 사라지는 경우를 만들지 않는다.
- **말풍선 편집(기존 메시지 수정)도 같은 규칙** — 챗의 번역 말풍선 편집(`makeTranslationBubbleEditable`)은 소설 번역 뷰와 같은 커밋 기계라, 수정본을 **대기함에 올리고 디바운스 후 `convertAndSplice`(replace op)로 원문 메시지에 반영**한다. 구간 교체는 메시지 경계를 건드리지 않으므로(메시지 신원은 노드가 정한다) 소설과 같은 프리미티브를 그대로 쓴다 — **챗 전용 접합 알고리즘을 만들지 않는다.** 오프셋 환산은 순수 함수 `collectChatReflectionOps`(테스트로 고정: 계산 구간이 평탄화 본문의 그 문단과 정확히 일치).
- **생성 게이트 = 락이 아니라 일괄 처리**: 전송/이어쓰기를 누르면 그 자리에서 대기 반영을 전부 처리한 뒤 출발한다(수정 전 원문으로 답이 이어지는 것 방지). 과거 실패로 요청을 거부하지 않는다 — 누를 때마다 새 시도, 방금 시도가 실패했을 때만 멈춤(알림 1회). 활성 경로에 없는 대기 건(분기 이동)은 휴면이라 게이트와 무관.
- 그룹/선채팅 경로는 AI 발화라 무관(변환 대상은 저자 입력뿐).

## 2.7 막혔을 때 푸는 법 (슬라이스 4 — 구현 완료)

대기함은 **성공했을 때만** 비워진다(§1). 그 대가로 "끝내 반영될 수 없는 건"이 하나 끼면 이어쓰기가 매번 그 반영부터 시도하다 멈추고, 사용자에게는 **푸는 방법이 없는 고장**이 된다. 그래서 두 겹으로 막는다.

- **자동 복구(원인 쪽)**: 번역 계획(`translationBlocks`)이 본문보다 낡으면 반영 op 도 위젯 배치도 어긋난다 — 계획을 소비하기 전에 `ensureTranslationPlan()` 이 유효성을 확인하고, 어긋났으면 번역 패널이 **숨어 있어도** 다시 만든다. 낡은 계획으로 만든 op 가 `expect` 검증에 걸려 영구 실패하던 경로가 여기서 사라진다(회귀금지.md).
- **수동 탈출구(결과 쪽)**: 번역 패널의 양방향 섹션에 **반영 대기 현황 + [반영 대기 취소]**(두 번 눌러 확인). 취소 = 대기함·초고를 비우고, 대기 문단의 수정(`user-edit`) variant 를 **짝(`authored`)으로 전환**한다 — 원문은 지금 상태 그대로, **저자가 쓴 문장도 화면에 그대로 남는다**(§0 의 "저자 원고 유실 금지"를 취소 동작에서도 지킨다). AI 를 부르지 않는 순수 변환이라 어떤 실패 상태에서도 성공한다.

## 3. 구현 순서

```
슬라이스 1  반영 대기함 실체화 (완료 — 배선은 구현현황.md "집필 프로")
슬라이스 1.5 출력 언어 = 원고 언어 (완료 — §1.5)
슬라이스 2  '양방향 번역' 토글 + ? 도움말 토스트 + 게이트 일반화 (완료)
슬라이스 3  용어집 자동 수집 연동 + 집필 변환 설정 공유 (완료 — 공용 렌더러)
슬라이스 4  마감: 대기함 다이어트 = '반영 대기 취소'(완료, §2.7) / 반영 실패 문단 배지·수동 재시도 — 남음
슬라이스 5  챗 모드 양방향 — 입력란 전송 + 말풍선 수정 반영 (완료 — §2.5)
```

각 슬라이스는 빌드 → 하네스 → 테스트 vault 수동 검증까지 끝내고 다음으로 (국소 완성).
